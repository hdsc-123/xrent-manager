# HANDOFF.md — Transmission du projet

Document destiné à toute personne (ou assistant IA) reprenant le projet, pour comprendre rapidement où en est XRent Manager sans avoir à relire tout l'historique.

Dernière mise à jour : 2026-08-14 — Sprint 19 (corrections de préproduction, 17 anomalies signalées) validé : recherche préalable (3 agents Explore en parallèle) ayant révélé que 4 points de l'énoncé collisionnaient avec des décisions d'architecture déjà validées — soumis au propriétaire du projet avant conception (CLAUDE.md règles 6/8), toutes tranchées avant tout code (module « dégâts » reporté entièrement ; caisse restant un singleton par tenant, solde de départ par agence purement informatif ; édition/suppression de caisse limitée aux écritures manuelles ; champs d'identité obligatoires appliqués à la conversion, pas à la réservation). Corrections livrées : notes de réservation (et 9 autres champs texte) qui ne s'effaçaient jamais à l'édition ; visibilité des réservations désormais scopée par agence de départ/retour (jusqu'ici absente) avec réception côté contrat pour l'agence d'arrivée ; permissions de groupe qui ne persistaient pas (réinjectées à chaque chargement de page) ; surclassement et vrai montant (prix + options) repris à la conversion réservation → contrat au lieu d'être silencieusement recalculés/perdus ; second conducteur sur le contrat ; override ADMIN de la machine à états des contrats (jusqu'ici aucun, même pour un ADMIN) ; séparation espèces/carte en caisse ; transfert entre agences filtré par agence de départ + kilométrage/carburant auto-remplis + bug d'accès à l'agence d'arrivée corrigé ; bon de déplacement même logique + carburant retour désormais obligatoire ; historique maintenance/mouvements ajouté à la fiche véhicule ; 4 nouvelles alertes véhicule proactives (assurance/vignette/contrôle technique/vidange, avec un bug de liste blanche trouvé et corrigé en testant) ; rapports/KPI vérifiés déjà corrects. Une seule migration de schéma, additive. 480/480 tests passants (+29), `npm run lint`/`npx tsc --noEmit`/`npm run build` validés. Voir DOMAINRULES.md section 37 et TESTREPORT.md section 3 « Tests Sprint 19 » pour le détail complet.

## 1. État actuel

Le projet est au stade **Sprint 19 — corrections de préproduction**. Sprint exclusivement dédié à la correction de 17 anomalies métier signalées avant mise en préproduction — aucun nouveau module métier selon l'énoncé, à l'exception de quelques champs additifs strictement nécessaires (voir plus bas). Le dépôt contient désormais, en plus des sprints précédents :

- **Recherche préalable et validation avant conception** : 3 agents Explore en parallèle sur l'état réel du code (reservations/agences ; caisse/permissions ; contrats/transferts/dégâts/alertes/rapports) ont révélé que 4 points de l'énoncé, tels que rédigés, entraient en collision avec des décisions d'architecture déjà validées et documentées — soumis au propriétaire du projet avant toute conception (CLAUDE.md règles 6/8) : (1) module « dégâts » (point 15) — **reporté entièrement**, aucun code, documenté comme gap `À DÉCIDER` (DOMAINRULES.md section 12) ; (2) « solde de départ de caisse par agence » (point 9) — la caisse **reste un singleton par tenant** (décision reconduite), un champ `Agency.cashStartingBalance` purement informatif est ajouté à la place ; (3) édition/suppression des écritures de caisse (point 2) — autorisée **uniquement** pour les écritures manuelles (sans lien vers un paiement), celles issues d'un paiement restent définitivement immuables ; (4) champs d'identité obligatoires (point 5) — appliqués au **formulaire de conversion** (réservation → contrat), pas à la réservation elle-même (aurait cassé l'import broker) ;
- **Notes de réservation non effacées à l'édition** (`EditReservationForm.tsx`) : `notes || undefined` (et le même motif sur 9 autres champs texte optionnels) transformait une valeur vidée en `undefined`, silencieusement ignoré côté serveur (`!== undefined`). Corrigé, ces champs envoient désormais systématiquement leur valeur courante. Bug serveur additionnel trouvé au passage : `normalizeSource("")` renvoyait `undefined` (pas `""`), qu'un `data` Prisma traite comme « non fourni » — effacer `source` n'aurait jamais fonctionné même après le correctif client sans un `?? null` supplémentaire (`updateReservation`, `src/lib/reservations.ts`) ;
- **Écritures de caisse manuelles désormais modifiables/supprimables** (voir décision ci-dessus) : `PATCH`/`DELETE /api/cash-register/[id]` (nouveau), restreints aux écritures sans `contractId`, nouvelles permissions `cash_register.edit`/`cash_register.delete`, chaque action journalisée ;
- **Visibilité des réservations par agence de départ/retour, jusqu'ici totalement absente** : `Reservation.pickupAgencyId`/`dropoffAgencyId` (nouveaux, résolus côté serveur depuis le texte libre existant) permettent désormais qu'un `MEMBER` ne voie qu'une réservation dont l'agence de départ **ou** de retour lui est accessible ; seule l'agence de départ peut modifier/convertir/supprimer ; l'agence d'arrivée gagne un accès de réception équivalent côté contrat une fois converti (`Location.dropoffAgencyId`, `PATCH /api/locations/[id]` restreint alors au statut « Terminée »/kilométrage retour uniquement) ; le widget « Retours » du dashboard compte désormais aussi les contrats dont l'agence de retour est accessible ;
- **Permissions de groupe qui ne persistaient pas** : `ensureDefaultGroups` (`src/lib/permissions.ts`) réinjectait inconditionnellement certaines clés dans les groupes `MEMBER`/`AGENCE` à *chaque* chargement de `/dashboard/permission-groups` — un ADMIN décochant l'une de ces clés la voyait silencieusement réapparaître. Pas un bug de cache (`getEffectivePermissions()` relit toujours la base) : ce mécanisme était une migration ponctuelle déjà appliquée, son invocation automatique récurrente est retirée ; tout rattrapage futur passe désormais par un script explicite (`scripts/backfill-permissions.js`) ;
- **Champs d'identité obligatoires à la conversion** (adresse/ville/pays/n° de pièce/n° de permis/dates du permis) — voir décision ci-dessus, uniquement pour un client nouvellement créé (un client déjà existant réutilisé n'est jamais re-exigé) ;
- **Surclassement à la conversion** : le sélecteur de véhicule était verrouillé sur la seule catégorie de la réservation. `ConvertReservationForm.tsx` gagne une case « Autoriser un surclassement », et un encart supplément/gratuité quand la catégorie choisie diffère, répercuté sur le total du contrat ;
- **Total du contrat recalculé à tort à la conversion** : `createLocation` ignorait systématiquement le prix négocié et les options (GPS/siège bébé/conducteur suppl.) de la réservation, recalculant toujours `pricePerDay × jours` — toute remise ou option disparaissait silencieusement. `CreateLocationInput.totalPrice` (nouveau, optionnel) prend le pas sur ce calcul quand fourni, pré-rempli à la conversion depuis le vrai montant réservation + options ;
- **Second conducteur** (`Location.secondDriverId`, nouveau, réutilise le modèle `Client` existant) — ajouté au formulaire de conversion et éditable après coup depuis la fiche contrat ;
- **Solde de départ de caisse par agence** (voir décision ci-dessus, `Agency.cashStartingBalance`, informatif) — numérotation de contrat par agence déjà correcte, vérifiée sans correction nécessaire ;
- **Aucun contournement ADMIN de la machine à états des contrats** — confirmé, un ADMIN était strictement soumis aux mêmes règles qu'un `MEMBER`. `updateLocation` gagne un paramètre `adminOverride` (dérivé du rôle côté route uniquement, jamais un champ de requête), chaque contournement réellement exercé journalisé séparément (`location.admin_override`) ; `LocationActions.tsx` distingue visuellement transitions normales et forcées, et gagne un formulaire de modification des dates réservé ADMIN (n'existait pas du tout auparavant, même pour un ADMIN) ;
- **Séparation espèces/carte en caisse** : `paymentMethod` était déjà stocké et réparti par paiement mixte — seule l'agrégation manquait. `cash`/`card` (par jour) et `monthCash`/`monthCard` (cumul du mois) exposés et affichés sur `/dashboard/cash-register` ;
- **Transfert entre agences — plusieurs correctifs** : sélecteur de véhicule désormais filtré par agence de départ (station) choisie en premier, plus une liste tenant-wide ; villes dérivées automatiquement de l'agence (lecture seule) ; date de départ pré-remplie ; kilométrage/carburant de départ pré-remplis depuis le dernier retour connu du véhicule (`getVehicleLastKnownState`, nouveau, réutilisé aussi pour les alertes vidange ci-dessous) ; carburant présenté via une jauge métier à 5 valeurs (Vide/1-4/2-4/3-4/Plein) ; **bug d'accès corrigé** : lancer un transfert exigeait à tort l'accès à l'agence d'arrivée en plus de l'agence de départ, empêchant un agent d'une agence satellite d'envoyer un véhicule vers une agence à laquelle il n'est pas rattaché — seul l'accès à l'agence de départ est désormais requis ;
- **Bon de déplacement** : même pré-remplissage automatique et même jauge carburant que les transferts ; carburant retour devient obligatoire (auparavant optionnel, contrairement au kilométrage retour) ;
- **Historique maintenance/mouvements ajouté à la fiche véhicule** (`/dashboard/vehicles/[id]`) : deux nouvelles sections en lecture seule (historique `Maintenance`, historique fusionné `VehicleTransfer`/`VehicleTrip`), absentes jusqu'ici (seules les locations associées étaient affichées) ;
- **Dégâts** — reporté, voir décision ci-dessus ;
- **Alertes véhicule proactives** (assurance/vignette/contrôle technique/vidange) : cinq nouveaux champs optionnels `Vehicle`, quatre nouvelles vérifications (`src/lib/scheduled-tasks.ts`, fenêtre de 30 jours, vidange par date ou par kilométrage via `getVehicleLastKnownState`), quatre nouvelles valeurs `AlertType`. **Bug trouvé et corrigé en écrivant les tests** : la liste blanche de `GET /api/alerts?type=...` avait été oubliée lors de l'extension de l'enum, rejetant ces valeurs pourtant valides en base ;
- **Rapports/KPI vérifiés** : les 7 fonctions de `src/lib/reports.ts` interrogent toutes des données réelles, aucune valeur figée trouvée — aucune correction nécessaire ;
- Une seule migration de schéma, additive (`add_sprint19_second_driver_agency_vehicle_alerts`) : `Reservation.pickupAgencyId`/`dropoffAgencyId`, `Location.dropoffAgencyId`/`secondDriverId`, `Agency.cashStartingBalance`, cinq champs `Vehicle` (alertes proactives), quatre valeurs `AlertType` ; deux nouvelles routes (`PATCH`/`DELETE /api/cash-register/[id]`, `GET /api/vehicles/[id]/last-known-state`) ; `src/__tests__/{reservations,cash-register,permissions,locations,vehicle-transfers,vehicle-trips,vehicles,maintenances,agencies,vehicle-mobility-alerts}.test.ts` étendus (+29, aucun nouveau fichier de test) — 480/480 tests passés au total, voir TESTREPORT.md section 3 « Tests Sprint 19 » et DOMAINRULES.md section 37 pour le détail complet ;
- `npm run lint` / `npx tsc --noEmit` / `npm run test` / `npm run build` validés. Aucune vérification manuelle navigateur ce sprint (aucun outil d'automatisation disponible en session — voir la limite documentée depuis le Sprint 12B, levée ponctuellement certains sprints) : le parcours complet a été vérifié par la suite de tests d'intégration HTTP contre un vrai serveur `next dev` de test, pas par une interaction navigateur réelle.

### Sprint 18 (rappel)

Le projet est passé par le stade **Sprint 18 — pilote réel et validation terrain**. Sprint exclusivement dédié à valider le SaaS en conditions d'usage réel avant préproduction — aucun nouveau module métier, conformément à l'énoncé. Le dépôt contenait déjà, en plus des sprints précédents :

- **Méthode, inédite pour ce sprint** : (1) baseline lint/tsc/test/build revérifiée avant toute chose (437/437 tests déjà verts) ; (2) 4 agents de revue en parallèle (même découpage par domaine que les Sprints 15-17 : agences/utilisateurs-permissions/véhicules/réservations-import ; contrats-locations/factures/paiements/PDF/numérotation ; retour véhicule/maintenance/transferts/bons de déplacement ; dashboard/permissions par rôle/audit/mobile-desktop/reports-export), chacun explicitement chargé de chercher des bugs/frictions/incohérences *d'usage quotidien réel* (pas un audit de sécurité, déjà fait Sprint 16, ni un audit de permissions granulaires, déjà fait Sprint 15/17) ; (3) **en parallèle**, un 5ᵉ agent dédié pilotant un vrai navigateur (Playwright/Chromium, installé temporairement hors du projet comme aux Sprints 12A/14D/14E, aucune dépendance ajoutée à `package.json`) — inscription d'un tenant pilote réaliste (« QA-Pilot-Sprint18 » : 2 agences avec préfixes de numérotation distincts, 4 véhicules, 3 clients, utilisateurs MEMBER/AGENCE via invitation), parcouru comme un agent d'agence découvrant l'outil en conditions réelles : réservations manuelles + import Excel (fichier `.xlsx` construit pour l'occasion), conversion en contrat, 3 contrats avec paiement complet/mixte/partiel, PDF contrat/facture, retour de véhicule, maintenance planifiée puis terminée, transfert entre agences (lancé/validé), bon de déplacement (lancé/retourné), reconnexion en tant que MEMBER/AGENCE pour vérifier ce qu'il voit réellement, viewport mobile (390×844) sur les pages principales ; (4) triage croisé des constats des 5 agents, correction des seuls bugs réels confirmés (les items nécessitant une décision produit non déjà validée restent documentés, pas corrigés — voir DOMAINRULES.md section 36) avec un test de régression par bug corrigé quand la nature du bug le permettait (les bugs purement client-side sans surface HTTP testable sont notés comme tels). Tenant pilote et tenants créés pour reproduire un signalement (6 variantes « QA-DupClientBug-Sprint18 », voir plus bas) supprimés de `xrent_dev` après coup — 7 tenants préfixés « QA », identifiés par nom, purgés modèle par modèle dans l'ordre de dépendance des clés étrangères, jamais un `deleteMany` global ;
- **Paiement enregistré depuis la fiche facture n'alimentait jamais la Caisse** (le bug le plus sérieux du sprint, impact financier direct) : `createPayment`/`createMixedPayments` (`src/lib/payments.ts`), le chemin emprunté par `POST /api/payments`, ne créaient jamais de `CashEntry` — seul le paiement intégré à la création d'un contrat (`processLocationPayment`) le faisait. Un paiement réglé après coup (« paiement au retour », flux normal documenté DOMAINRULES.md section 29) était donc invisible en Caisse alors que la facture affichait pourtant le bon statut/solde. Corrigé : la création de l'écriture de caisse est désormais centralisée dans `createPayment` lui-même (nouvelle fonction interne `recordPaymentCashEntry`, dérive `contractNumber`/`clientName` depuis la `Location` de la facture) — garantit la même couverture quel que soit le point d'entrée. `processLocationPayment` (`src/lib/location-payment.ts`) ne duplique plus cette logique (simplifiée en conséquence, `contractNumber`/`clientName` retirés de son interface, devenus inutiles) ;
- **Facture à 0 (remise intégrale) ne pouvait jamais atteindre `PAID`** (`src/lib/invoices.ts`) : `createPayment` refuse tout montant contre un solde restant nul, donc `recomputeInvoiceStatus` (qui dérive `PAID`) n'était jamais invoqué pour ce cas — la facture restait indéfiniment `SENT`. Corrigé : la transition manuelle `DRAFT → SENT` (`updateInvoice`) atterrit directement sur `PAID` quand `totalAmount` calculé est nul ;
- **Alertes non gatées par `alerts.view`** — carte « Alertes récentes » du dashboard (`src/app/dashboard/page.tsx`) et cloche/badge du `Header` (visible sur **toutes** les pages, `src/app/dashboard/layout.tsx`) : même schéma que le bug « À faire aujourd'hui » corrigé Sprint 17, mais oublié sur ces deux points d'affichage — un groupe `COMPTABILITÉ` (sans `alerts.view`) voyait le message réel de chaque alerte et un compteur, avec un lien menant à une page qui lui refuse l'accès. Corrigé aux deux endroits ;
- **`/dashboard/reports` restée en contrôle `role === "ADMIN"` strict** malgré le retrofit `can(user, "reports.view")` déjà fait au Sprint 15 sur `/api/reports/*` et sur `Sidebar.tsx` (DOMAINRULES.md section 22) — lien mort réel pour `MEMBER`/`COMPTABILITÉ` (permission accordée par défaut, entrée visible en sidebar, page bloquant avec « réservée aux administrateurs »). Corrigé, la page vérifie désormais la même permission que l'API ;
- **Kilométrage retour perdu silencieusement** (`LocationActions.tsx`) : le bouton de transition de statut n'envoyait que `{ status }`, jamais le champ « Kilométrage retour » affiché juste en dessous — seul le bouton « Enregistrer » du bloc Notes l'envoyait. Un agent saisissant le kilométrage puis cliquant directement « Terminée » (le geste naturel pour clôturer un retour) voyait la valeur disparaître sans aucun message d'erreur, le contrat passant `COMPLETED` sans kilométrage enregistré. Corrigé : `handleTransition` envoie désormais aussi `endOdometer` (un champ inchangé renvoie sa valeur déjà en base, jamais d'effacement accidentel) ;
- **Bouton « Annuler » (réservations) non gated par `reservations.edit`** (`ReservationsTable.tsx`) : contrairement à « Modifier », affiché et cliquable pour un rôle en lecture seule (ex. `COMPTABILITÉ`), systématiquement rejeté par l'API après confirmation de l'utilisateur. Corrigé, même garde `canEdit` que « Modifier » ;
- **`reservations.import` jamais accordé au groupe `MEMBER` par défaut**, contredisant DOMAINRULES.md section 22 (« réservations (hors suppression) », l'import n'a jamais été documenté comme une exception) — gap antérieur au Sprint 15 (module créé Sprint 12C), jamais un resserrement délibéré. Corrigé dans `DEFAULT_GROUPS` et rétro-appliqué aux tenants déjà existants (réutilisation du mécanisme de backfill générique, `skipDuplicates`, sans risque pour un groupe déjà personnalisé). En marge : `/dashboard/reservations/import/page.tsx` (Client Component pur) ne vérifiait aucune permission avant de rendre le formulaire — corrigé par le même découpage Server/Client Component que `convert/`/`edit/` (nouveau `ImportReservationsForm.tsx`, `page.tsx` devient un Server Component avec garde `can(user, "reservations.import")`) ;
- **Champs « obligatoires » (astérisque rouge) non appliqués à la création d'une agence/d'un véhicule** — ni le client (`noValidate` sur le formulaire, convention déjà en place sur les 24 formulaires du SaaS) ni le serveur (`POST /api/agencies`/`POST /api/vehicles` ne requièrent que le strict minimum) ne vérifiaient réellement ville/adresse/téléphone/email/responsable (agence) ni châssis/couleur/portes/places/chevaux/kW/cylindrée (véhicule) : création silencieuse d'enregistrements incomplets malgré une UI qui les présente comme requis. Corrigé par une vérification JS ciblée dans `handleSubmit` des deux formulaires, même convention que les formulaires qui le font déjà (ex. `NewClientForm.tsx`) — `noValidate` volontairement conservé (cohérence avec le reste de l'app) ;
- **Export CSV sans BOM UTF-8** (`ExportCsvButton.tsx`) : un export contenant des caractères accentués (noms de clients/agences réels) s'affiche en mojibake une fois ouvert en double-clic dans Excel (suppose l'encodage de la locale système plutôt que l'UTF-8 réel du fichier). Corrigé, BOM (`U+FEFF`) préfixé au contenu (nouvelle fonction `buildCsvFileContent`, exportée et testée unitairement comme `sanitizeCsvCell`) ;
- **En-tête mobile de `/dashboard/reservations` rogné à 390px** : seule page de liste du SaaS avec 2 boutons d'en-tête, sans `flex-wrap` — le groupe de boutons était coupé en dur (pas de scroll possible), confirmé par capture d'écran réelle. Corrigé pour cette page (les 13 autres pages de liste partagent le même motif mais n'ont qu'un seul bouton — non modifiées, hors périmètre observé) ;
- **Groupe de permissions `ADMIN` éditable sans aucun effet réel, sans avertissement** : créé « pour référence/affichage uniquement » (DOMAINRULES.md section 22, `can()` court-circuite sur `role === "ADMIN"` sans jamais le consulter), mais rien sur `/dashboard/permission-groups/[id]` ne le signalait — un ADMIN décochant des permissions croyait restreindre les comptes administrateurs, sans effet réel ni message l'indiquant. Corrigé par un avertissement explicite sur la page de ce groupe précis (informatif uniquement, pas de blocage d'édition) ;
- **Formulaire d'édition de tenant soumissible par un non-ADMIN** (`/dashboard/tenants/[id]`) : `PATCH /api/tenants/[id]` réservé ADMIN, mais la page affichait le formulaire à tout utilisateur du tenant, rejeté seulement après soumission (403). Corrigé, formulaire masqué (texte explicatif) pour un non-ADMIN ;
- **Année de véhicule sans plafond haut** : seule une borne basse (1900) était vérifiée à la création/l'édition — corrigé, plafond `année courante + 1` ;
- **Un signalement non retenu comme bug après reproduction réelle dédiée** : le bouton « Utiliser ce client » du dialogue de doublon (conversion réservation → contrat) avait été signalé non-fonctionnel par l'agent de walkthrough navigateur. Relecture statique du code (`ConvertReservationForm.tsx`/`DuplicateCheck.tsx`/route de conversion) : logique correcte, aucun défaut identifié. Reproduction réelle dédiée par un second agent (Playwright, 6 conditions : clic standard, clic immédiat avant fin d'animation, viewport mobile 375×667, réouverture après annulation, Chromium et Firefox) : succès à chaque tentative, jamais reproduit. Conclusion retenue : artefact de la session de test originale (mis-clic d'un outil d'automatisation), pas un bug réel — **aucune modification de code**, documenté pour éviter de re-signaler ce même point sans preuve nouvelle ;
- **Gaps identifiés, documentés plutôt que corrigés** (décisions produit hors périmètre d'un sprint de correction de bugs, voir DOMAINRULES.md section 36 pour le détail) : possibilité de créer plusieurs factures pour une même location (déjà « À DÉCIDER » depuis le Sprint 6, confirmé concrètement atteignable en 3 clics via `/dashboard/invoices/new`) ; notes de réservation non éditables via l'UI pour une réservation `CONVERTED`/`CANCELLED` malgré le backend qui le permet explicitement depuis le Sprint 17 (`canEdit` de la page détail masque tout le bloc Actions, y compris les Notes) ; aucune alerte ne se résout automatiquement quand sa cause disparaît réellement ; filtrage limité (statut seulement) sur les listes Transferts/Bons de déplacement malgré un support déjà présent côté `lib` ; absence de coordination entre Maintenance planifiée et disponibilité affichée à la réservation (cohérent avec `Vehicle.status` resté manuel, décision déjà actée) ;
- Aucune migration de schéma, aucun nouveau module métier, aucune nouvelle route API ; un fichier scindé (`reservations/import/page.tsx` → Server Component + nouveau `ImportReservationsForm.tsx`) ; `src/__tests__/{ui,permissions,invoices,payments,vehicles,csv-export-sanitization}.test.ts` étendus, `{audit,e2e,e2e-full,reports}.test.ts` retouchés uniquement pour la purge `CashEntry`/`CashRegister` en `afterAll` (conséquence directe du correctif caisse ci-dessus) — 451/451 tests passés au total (+14, aucun nouveau fichier de test), voir TESTREPORT.md section 3 « Tests Sprint 18 » ;
- `npm run lint` / `npx tsc --noEmit` / `npm run test` / `npm run build` validés. Vérification manuelle complète avec un vrai navigateur (tenant pilote réaliste construit de bout en bout, tous les flux métier listés ci-dessus, desktop + mobile 390×844) — méthode ci-dessus.

### Sprint 17 (rappel)

Le projet est passé par le stade **Sprint 17 — tests intensifs et régressions**. Sprint exclusivement dédié à la validation de la stabilité du SaaS après les Sprints 14A à 16 (permissions, sécurité, dashboard, numérotation, réservations) — aucun nouveau module métier, conformément à l'énoncé. Le dépôt contenait déjà, en plus des sprints précédents :

- **Méthode** : (1) baseline lint/tsc/test/build revérifiée avant toute chose (430/430 tests déjà verts) ; (2) 4 agents de revue en parallèle, lecture intégrale du code (pas un échantillonnage) contre HANDOFF/DOMAINRULES et les tests existants, un par groupe de parcours critiques (agence/utilisateur/véhicule/réservation/import ; contrat/paiements/PDF ; mobilité/export/reset/audit ; permissions/dashboard/devises + cas limites) ; (3) vérification manuelle avec un vrai navigateur (Chromium/Playwright, installé temporairement hors du projet comme aux Sprints 12A/14D/14E, aucune dépendance ajoutée à `package.json`) — inscription→connexion→28 pages du dashboard inspectées (200, aucune erreur console), tables peuplées de données réelles (sources texte libre inconnues, options mixtes, devise d'options différente de la devise principale) vérifiées visuellement en desktop et mobile, formulaire de réservation avec astérisques dynamiques (voucher optionnel seulement si `source === "DIRECT"`) confirmé ; (4) correction des seuls bugs réels révélés, avec un test de régression par bug corrigé. Tenants QA créés pendant la vérification manuelle supprimés de `xrent_dev` après coup (4 tenants préfixés « QA », identifiés par nom, jamais un `deleteMany` global) ;
- **Régression Sprint 15 — groupe `AGENCE` incomplet** (`src/lib/permissions.ts`, `DEFAULT_GROUPS`/`SPRINT15_BACKFILL_PERMISSIONS`) : contrairement à `MEMBER`, le groupe par défaut `AGENCE` n'avait jamais reçu `agencies.view` ni les 4 clés `cash_register.*` — ni à sa définition Sprint 15, ni dans le backfill des tenants déjà existants. Sans `agencies.view`, `GET /api/agencies` renvoyait 403 pour ce groupe, vidant silencieusement le sélecteur d'agence de « Créer un véhicule »/« Lancer un transfert »/« Créer une réservation » et empêchant de fait toute soumission malgré des permissions `*.create` par ailleurs accordées — régression fonctionnelle réelle, pas une simple omission de test (`permissions.test.ts` testait déjà ce groupe mais sans ces deux clés). Corrigé dans les deux listes (nouveaux tenants et backfill des tenants existants) ;
- **`PATCH`/`DELETE /api/agencies/[id]` — `canAccessAgency()` manquant** (`src/app/api/agencies/[id]/route.ts`) : seul `GET` vérifiait `canAccessAgency()` en plus de `can()`, pas `PATCH`/`DELETE` — un MEMBER avec un groupe personnalisé accordant `agencies.edit`/`agencies.delete` (scénario plausible : déléguer la gestion d'une seule agence) pouvait modifier/supprimer n'importe quelle agence du tenant, y compris celles auxquelles il n'a aucun accès `UserAgency`. Corrigé, même garde que `GET` ;
- **`PATCH /api/reservations/[id]` — deux gaps par rapport à `POST`** : (1) aucune validation de `pickupAgency`/`dropoffAgency` contre les agences réelles du tenant (contrairement à `POST`, Sprint 14A) — une ville qui serait refusée à la création pouvait être écrite silencieusement via l'édition ; (2) aucun verrou sur les réservations `CONVERTED`/`CANCELLED` — seule l'UI (page d'édition, bouton « Modifier ») empêchait leur modification, un `PATCH` direct sur n'importe quel champ (prix, dates, identité client) était accepté sans erreur alors que le contrat déjà généré à la conversion n'est jamais mis à jour rétroactivement. Corrigé : même validation ville que `POST` ; nouveau `ReservationLockedError` (`src/lib/reservations.ts`, 409) sur toute réservation terminale, à l'exception explicite des notes (toujours éditables à tout statut via `ReservationActions.tsx`, comportement préexistant conservé) ;
- **Prix d'option non effacé quand l'option est décochée** (`updateReservation`, `src/lib/reservations.ts`) : `hasGps: false` sans nouveau `gpsPrice` laissait l'ancien `gpsPrice` en base (même bug pour siège bébé/conducteur suppl.) — « Prix final » (`ReservationsTable.tsx`, somme inconditionnelle) continuait d'inclure un montant dont le badge affichait pourtant « Non ». Devenu concrètement atteignable depuis le formulaire d'édition complet du Sprint 15 (jusqu'ici seuls statut/notes étaient éditables). Corrigé : le prix correspondant est mis à `null` si l'option est décochée sans prix explicite fourni dans la même requête ;
- **Paiement mixte non atomique depuis la fiche facture** (`InvoiceActions.tsx`) : contrairement au paiement intégré à la création d'un contrat (`processLocationPayment`, corrigé Sprint 14B), le paiement mixte de la fiche facture validait le total contre un `remainingBalance` figé au chargement de la page, puis postait chaque ligne séparément (`POST /api/payments` × 2) — si le solde réel avait changé entretemps (autre onglet, autre agent), la première ligne pouvait s'écrire avant que la seconde échoue, laissant un paiement partiel orphelin, exactement le bug que le Sprint 14B avait corrigé ailleurs. Corrigé par une nouvelle fonction `createMixedPayments` (`src/lib/payments.ts`) qui revalide le solde réel juste avant d'écrire quoi que ce soit, exposée via `POST /api/payments` (nouveau champ optionnel `lines`, une seule requête au lieu d'une boucle côté client) ;
- **Reset de données non pleinement atomique** (`resetTenantData`, `src/lib/data-reset.ts`) : la purge métier principale était bien transactionnelle (`$transaction` array), mais la purge conditionnelle de l'`AuditLog` (option « réinitialisation complète »), le recalcul du solde de caisse et l'entrée d'audit finale `data.reset` s'exécutaient après, hors de toute atomicité — un échec sur l'une de ces trois étapes laissait les données déjà purgées sans que l'entrée d'audit « reset réussi » ne soit jamais écrite, violation silencieuse de la garantie documentée (DOMAINRULES.md section 31, SECURITY.md section 17). Corrigé : transaction interactive (`prisma.$transaction(async (tx) => …)`) englobant purge + recalcul de caisse (posé directement à 0, déterministe après une purge complète des `CashEntry`) + purge conditionnelle de l'AuditLog + entrée d'audit finale — tout ou rien ;
- **Dashboard « À faire aujourd'hui » non gated par permission** (`src/app/dashboard/page.tsx`) : les tuiles Retours/Maintenances/Factures en retard (comptages `Location`/`Maintenance`/`Invoice`) s'affichaient sans vérifier `locations.view`/`maintenances.view`/`invoices.view`, contrairement aux tuiles Réservations/Véhicules juste au-dessus (gated depuis le Sprint 15) — un groupe `COMPTABILITÉ` (sans accès locations/maintenances par choix délibéré, DOMAINRULES.md section 22) voyait quand même un compte agrégé réel de ces modules. Corrigé, chaque tuile (et son comptage) gated individuellement, carte entière masquée si aucune des trois permissions n'est accordée ;
- **`getReservationsByStatus` (rapports) sous-comptait les sources broker en texte libre** (`src/lib/reports.ts`) : le graphique broker/direct de `/dashboard/reports` cherchait encore la seule valeur littérale `"BROKER"` pour le bucket broker — héritage de l'ancien enum fermé, jamais mis à jour lors du passage de `Reservation.source` en texte libre (Sprint 15, précisément pour ne plus perdre les codes broker réels type `TJS`/`DCH`/`CT`). Toute réservation avec un vrai code broker était donc silencieusement exclue du graphique. Corrigé : toute source non-`DIRECT`/non-nulle est désormais comptée comme broker ;
- **Deux gaps mineurs corrigés en passant** : import Excel des réservations — la copie d'affichage des en-têtes attendus (`columns.ts`, page `/dashboard/reservations/import`) omettait « Devise des options » (colonne pourtant supportée par le parseur depuis le Sprint 15) ; aucune conséquence fonctionnelle, juste une instruction incomplète ;
- **Un gap identifié, documenté plutôt que corrigé (hors périmètre proportionné de ce sprint)** : `validateVehicleTransfer`/`cancelVehicleTransfer`/`returnVehicleTrip`/`cancelVehicleTrip` (`src/lib/vehicle-transfers.ts`/`vehicle-trips.ts`) lisent le statut existant hors transaction avant de vérifier `canTransition`, puis écrivent sans condition de statut dans le `where` — une vraie fenêtre de course entre deux requêtes quasi simultanées (ex. `validate` et `cancel` sur le même transfert) peut désynchroniser `VehicleTransfer.status` et `Vehicle.agencyId`/`status`. Motif de ne pas corriger ce sprint : le même schéma (lecture hors transaction, écriture sans condition) est déjà présent ailleurs dans le projet (ex. `locations.ts`), une correction ponctuelle et isolée à ces quatre fonctions serait incohérente sans revoir le patron plus largement — hors périmètre d'un sprint de tests/corrections ciblées. À traiter comme un chantier dédié si la volumétrie réelle le justifie. Voir DOMAINRULES.md section 35 ;
- Aucune migration de schéma, aucun nouveau module métier ; une seule extension de contrat d'API existante (`POST /api/payments` accepte désormais un `lines?: {amount, method}[]` optionnel, rétrocompatible avec `amount`/`method` simples) ; `src/__tests__/{agencies,permissions,reservations,payments}.test.ts` étendus (+7, aucun nouveau fichier) — 437/437 tests passés au total, voir TESTREPORT.md section 3 « Tests Sprint 17 » ;
- `npm run lint` / `npx tsc --noEmit` / `npm run test` / `npm run build` validés. Vérification manuelle avec un vrai navigateur (Chromium/Playwright, comme indiqué ci-dessus) — première couverture UI aussi large depuis le Sprint 14E (28 pages) sur ce projet.

### Sprint 16 (rappel)

Le projet est passé par le stade **Sprint 16 — audit de sécurité approfondi et durcissement des accès**. Sprint exclusivement dédié à la sécurité, sans nouveau module métier, conformément à l'énoncé. Le dépôt contenait déjà, en plus des sprints précédents :

- **Méthode d'audit** : 4 revues en lecture seule menées en parallèle par des agents dédiés, chacune avec relecture intégrale du code concerné (pas un échantillonnage) — (1) les ~60 fichiers `src/app/api/**/route.ts*` relus méthode par méthode pour la couverture authentification/tenant/agence/permissions granulaires ; (2) les 26 fichiers `[id]` relus avec les fonctions `get*ById` qu'ils appellent, pour la protection IDOR ; (3) fuite de données sensibles, gestion d'erreurs, complétude de l'audit trail, cohérence des permissions par défaut ; (4) import Excel, export CSV, reset de données, PDF de lot. Voir SECURITY.md section 25 pour le rapport complet ;
- **Résultat principal : aucune faille d'isolation tenant/agence ni IDOR trouvée** — confirme une nouvelle fois la solidité du modèle d'isolation logique par `tenant_id` (SECURITY.md section 1) après les audits Sprint 10/11/15. Les 7 écarts réels identifiés sont des gaps de permission granulaire, de traçabilité d'audit, ou de robustesse (absence de plafond), tous corrigés ce sprint (voir DOMAINRULES.md section 34 pour le détail modèle par modèle) ;
- **`POST /api/documents/batch-pdf` — permission granulaire manquante** : contrairement à toutes les autres routes `locations`/`invoices` retrofitées au Sprint 15, cette route ne vérifiait aucune clé `can()` — un commentaire de code affirmait à tort qu'« aucune permission granulaire ne s'applique à ces modules », justification obsolète antérieure au retrofit de portée du Sprint 15, jamais mise à jour pour cette route précise. Corrigé : `can(user, "locations.view"/"invoices.view")` selon `type` ;
- **Fuite de données dans les erreurs de conflit de disponibilité** : `VehicleNotAvailableError.conflictingLocations` (`POST`/`PATCH /api/locations`, `POST /api/reservations/[id]/convert`, réponse `409`) exposait l'enregistrement `Location` complet (prix, caution, notes, `clientId`) — contournait de fait `locations.view` pour un appelant n'ayant que `locations.create`/`edit`. `findConflictingLocations` (`src/lib/vehicles.ts`) ne sélectionne désormais que `id`/`startDate`/`endDate`/`status` ;
- **Audit trail complété** : réinitialisation de mot de passe par un ADMIN sur un autre user (`PATCH /api/users/[id]`) désormais journalisée (`user.password_reset`, sans le mot de passe en métadonnée) — jusqu'ici seuls le changement de rôle/d'agences du même handler l'étaient. Tentative de reset de données avec un nom de confirmation incorrect désormais journalisée (`data.reset_failed`, sans le nom saisi) — jusqu'ici seul un reset réussi laissait une trace, aucune visibilité sur des tentatives répétées de deviner le nom exact du tenant ;
- **Injection de formule CSV (OWASP)** : l'export CSV partagé (`ExportCsvButton.tsx`, utilisé par `/dashboard/audit` et `/dashboard/reports`) n'échappait pas un champ texte libre commençant par `=`/`+`/`-`/`@` (ex. un nom d'utilisateur), qu'Excel/LibreOffice interprète comme une formule à l'ouverture — corrigé par préfixe d'apostrophe (`sanitizeCsvCell`, exporté et testé unitairement) ;
- **Durcissement DoS** : import Excel des réservations (`POST /api/reservations/import`) plafonné à 20 Mo (aucune limite jusqu'ici, fichier entièrement bufferisé en mémoire) ; lots PDF (`POST /api/documents/batch-pdf`) — les modes « identifiants » et « plage de dates » plafonnés à 500 documents comme le mode « plage de numéros » l'était déjà ;
- **Deux observations écartées après analyse, non retenues comme failles** : `DELETE /api/tenants/[id]` n'émet pas de `logAction` — `AuditLog.tenantId` a une FK obligatoire vers `Tenant` sans cascade, donc journaliser après coup échouerait systématiquement (silencieusement) et journaliser avant donnerait un log trompeur pour une suppression qui échoue quasi systématiquement de toute façon (409 FK, un tenant a toujours au moins un `User` sans cascade non plus) — documenté dans le code plutôt que « corrigé » par un log qui ne fonctionnerait pas ; le groupe `MEMBER` par défaut accorde un CRUD large plutôt qu'un minimum — déjà documenté comme un choix délibéré depuis le Sprint 15 (DOMAINRULES.md section 22), pas une régression de ce sprint ;
- **Écart documentaire relevé en marge, sans impact sécurité** : DOMAINRULES.md section 31 affirmait encore que le reset de données remet à 0 `Tenant.lastContractNumber`, alors que le code (déjà correct depuis le Sprint 15) remet à 0 `Agency.lastContractNumber` — corrigé dans la documentation uniquement ;
- Aucune migration de schéma, aucun nouveau module métier, aucune nouvelle route API (uniquement des vérifications/plafonds/journalisation ajoutés à des routes existantes) ; `src/__tests__/{vehicles,users,data-reset,batch-pdf,reservations}.test.ts` étendus, nouveau fichier `csv-export-sanitization.test.ts` (test unitaire pur, sans serveur de test) — 430/430 tests passés au total (+14), voir TESTREPORT.md section 3 « Tests Sprint 16 » ;
- `npm run lint` / `npx tsc --noEmit` / `npm run test` / `npm run build` validés. Aucune vérification manuelle navigateur ce sprint (aucune interface nouvelle — corrections serveur uniquement).

### Sprint 15 (rappel)

Le projet est passé par le stade **Sprint 15 — audit/historique, corrections métier, permissions, dashboard, réservations, fiabilisation documentaire**, validé :

- **Permissions granulaires — extension de portée (décision explicite, remplace la portée limitée actée au Sprint 12C)** : `can()` (`src/lib/permissions.ts`) est désormais vérifié côté serveur, additivement à `canAccessAgency()`/aux vérifications de rôle déjà en place, sur les routes `src/app/api/{agencies,vehicles,locations,clients,invoices,payments,maintenances,alerts,cash-register,vehicle-transfers,vehicle-trips,reports}/**` — nouvelles clés `maintenances.*`/`alerts.*`/`cash_register.*`/`vehicle_transfers.*`/`vehicle_trips.*` ajoutées au catalogue. `DEFAULT_GROUPS` révisés pour ne pas régresser le comportement déjà en vigueur (`MEMBER`/`AGENCE` complétés des clés correspondant à des actions jusqu'ici possibles sans aucune restriction ; `COMPTABILITÉ` en revanche **resserré**, décision confirmée, scopé strictement finance/reporting). Restent volontairement en contrôle de rôle strict, non convertis en `can()` : utilisateurs, invitations, groupes de permissions, tenants, reset de données, audit (DOMAINRULES.md section 4). Sidebar/pages/tables mis à jour en conséquence (`permission: "<module>.view"` sur la quasi-totalité des entrées) — voir DOMAINRULES.md section 22 pour le détail complet, y compris la liste exacte des clés par groupe ;
- **Bug critique découvert et corrigé en cours de sprint** : aucun code de l'application ne rattachait jusqu'ici un `User` nouvellement créé à un `PermissionGroup` (`permissionGroupId` reste `null` tant qu'un ADMIN ne l'assigne pas manuellement). Avant l'extension de portée ci-dessus, cela n'avait aucun effet réel ; avec elle, un `MEMBER` fraîchement invité se serait retrouvé sans accès à quasiment aucun module dès la création de son compte. Corrigé dans `getEffectivePermissions()` : un `User` non-ADMIN sans groupe assigné (valeur `null`, pas un groupe personnalisé vide) retombe désormais implicitement sur les permissions du groupe `MEMBER` par défaut. Un script SQL ponctuel a également rétro-appliqué les nouvelles clés de permission aux groupes `MEMBER`/`AGENCE` déjà existants sur `xrent_dev`/`xrent_test` (les tenants créés avant ce sprint n'auraient sinon jamais reçu les nouvelles clés, `ensureDefaultGroups` étant idempotent par nom de groupe et ne complétant pas un groupe déjà existant) — voir DOMAINRULES.md section 22, SECURITY.md section 4 ;
- **Numérotation de contrat par agence** : `Agency.contractNumberPrefix`/`lastContractNumber` (nouveaux, migration `20260813191800_add_sprint15_reservation_and_agency_numbering`) remplacent les champs équivalents sur `Tenant` (retirés du schéma) — chaque agence a désormais son propre préfixe et sa propre séquence indépendante (ex. `RAK-00011` pour Rabat, une autre séquence pour une autre agence), redéfinissable depuis `/dashboard/agencies/[id]` (déplacé depuis `/dashboard/settings`). Chaque agence redémarre à `prefix=""`/`compteur=0` (décision confirmée, pas de reprise automatique de l'ancien compteur tenant — un ADMIN doit redéfinir manuellement le compteur de chaque agence pour reprendre une séquence externe déjà en cours). `POST /api/documents/batch-pdf` (lots PDF par plage de numéros) exige désormais un `agencyId` explicite. Voir DOMAINRULES.md section 29 ;
- **Réservations — source en texte libre + devise des options** : `Reservation.source` passe d'un enum fermé (`BROKER`/`DIRECT`) à du texte libre — corrige un bug réel où tout code broker importé différent de ces deux valeurs (ex. `TJS`/`DCH`/`CT`) était silencieusement perdu (affiché en tiret). `Reservation.optionsCurrency` (nouveau, distinct de `currency`) permet d'exprimer les options (GPS/siège bébé/conducteur suppl.) dans une devise différente du prix total — le prix final n'additionne plus jamais deux montants de devises différentes sans le signaler (affichage séparé si `currency !== optionsCurrency`). Voir DOMAINRULES.md section 21 ;
- **Réservations — colonnes, tri, édition** : colonne « N° vol » repositionnée après « Catégorie » ; tri réellement fonctionnel sur Départ/Retour (et par cohérence Prix total/Catégorie/villes) via l'ajout d'un `accessorFn` (TanStack v9 ne peut pas trier une colonne sans accesseur — le bouton de tri était visuellement présent mais sans effet) ; nouveau formulaire d'édition complet (`/dashboard/reservations/[id]/edit`, découpage Server/Client Component identique à `convert/`) — jusqu'ici seuls le statut et les remarques étaient éditables depuis l'UI malgré une route `PATCH` acceptant déjà la quasi-totalité des champs. Affichage sur une seule ligne des dates/heures non modifié dans sa logique (`formatDateTimeCell`, déjà correct) — **non revérifié visuellement en navigateur réel cette session** (aucun outil d'automatisation disponible, voir TESTREPORT.md). Voir DOMAINRULES.md section 33 ;
- **Dashboard — widgets orientés métier** (`src/app/dashboard/page.tsx`) : cartes génériques « Agences »/« Utilisateurs » remplacées par « Réservations aujourd'hui » (nombre + montant, `startDate`, tenant-wide — `Reservation` n'a pas d'`agencyId`, DOMAINRULES.md section 21) et « Véhicules disponibles » (nombre + échantillon de noms, agency-scopé). Actions rapides individuellement gardées par permission. Voir DOMAINRULES.md section 33 ;
- **Audit étendu** : nouvelles actions journalisées — `agency.created`/`agency.updated` (avec anciennes valeurs de numérotation en cas de rewind)/`agency.deleted`, `tenant.updated`, `user.created` (câblé sur l'acceptation d'invitation, seul point de création réelle d'un compte hors inscription). `ACTION_LABELS`/`RESOURCE_LABELS` de `/dashboard/audit` complétés (dont des libellés manquants de sprints antérieurs, découverts en marge). Aucune nouvelle route de suppression d'audit — le mécanisme existant (`includeAuditLog` du reset de données, ADMIN-only, tenant-scopé) est jugé suffisant. Voir DOMAINRULES.md section 33, SECURITY.md section 13 ;
- **Éditeur utilisateur** : la demande (« afficher tous les rôles ») visait en réalité les groupes de permissions, pas de nouveaux rôles système (toujours seulement `ADMIN`/`MEMBER`, décision confirmée) — `EditUserForm.tsx` affiche désormais le groupe de permissions courant avec un lien vers la page dédiée (déjà existante depuis le Sprint 12C). Grille d'agences et réinitialisation de mot de passe inchangées, vérifiées fonctionnelles. Voir DOMAINRULES.md section 33 ;
- **Fiabilité de la documentation** : chaque édition de fichier de documentation ce sprint a été suivie d'une relecture complète (pas un aperçu) pour vérifier l'absence de troncature/duplication — règle appliquée en réponse à l'erreur d'édition de `TESTREPORT.md` mentionnée dans l'énoncé du sprint. Voir aussi INC-1 ci-dessous, un incident de processus distinct (pas une erreur d'édition de documentation) ;
- **Incident de processus signalé (voir INCIDENTS.md, INC-1)** : en cours de sprint, un agent de développement travaillant en parallèle sur la copie de travail a exécuté `git stash` puis a atteint sa limite d'usage avant de le restaurer, laissant temporairement la copie de travail dans un état antérieur (fichiers `src/lib/*.ts`, routes API, pages dashboard reflétant l'état d'avant ce sprint). Détecté via des échecs de tests incohérents, diagnostiqué (le stash contenait la totalité du travail perdu), restauré sélectivement sans perte de données, suite de tests complète revérifiée verte avant de poursuivre. Aucun impact final, mais souligne un risque réel du travail multi-agent sur une copie partagée non isolée (worktree) — à prendre en compte pour l'organisation des sprints futurs ;
- 1 migration de schéma (`20260813191800_add_sprint15_reservation_and_agency_numbering`), aucune nouvelle route API (uniquement des vérifications `can()`/champs de corps de requête ajoutés à des routes existantes), 1 nouvelle page (`/dashboard/reservations/[id]/edit`) ; `src/__tests__/{agencies,alerts,audit,batch-pdf,cash-register,clients,data-reset,e2e,invoices,locations,maintenances,payments,permissions,reports,reservations,tenants,vehicle-transfers,vehicle-trips,vehicles}.test.ts` étendus (+54, aucun nouveau fichier) — 416/416 tests passés au total, voir TESTREPORT.md section 3 « Tests Sprint 15 » ;
- `npm run lint` / `npx tsc --noEmit` / `npm run build` validés. **Limite documentée, comme la plupart des sprints depuis le 12B** (la levée ponctuelle aux Sprints 14D/14E ne s'est pas reproduite cette session) : aucun outil d'automatisation navigateur disponible — le parcours complet a été vérifié par les 416 tests d'intégration HTTP contre un vrai serveur `next dev` de test, pas par une interaction navigateur réelle ; en particulier, le rendu visuel du tri de `ReservationsTable`, l'affichage sur une seule ligne des colonnes Départ/Retour à largeur réelle, et le formulaire d'édition de réservation n'ont été vérifiés que par lecture de code/tests HTTP, pas par capture d'écran. Données de test nettoyées après chaque exécution.

### Sprint 14E (rappel)

Le projet est passé par le stade **Sprint 14E — harmonisation des en-têtes de tableaux**, validé :

- **Harmonisation des en-têtes de tableaux** (`src/components/layout/DataTable.tsx`) : le rendu « cassé » signalé sur Réservations (libellés « Ville de départ »/« Ville de retour »/« Conducteur suppl. » répartis sur 2-3 lignes de façon incohérente, barre d'en-têtes visuellement décalée) avait une cause racine précise, pas une simple question de classes CSS mal choisies — `columnSizingFeature` (TanStack Table v9) fusionne un `size: 150` par défaut dans le `columnDef` résolu de **toute** colonne, y compris celles qui n'en déclarent aucun (`header.column.columnDef.size` n'était donc jamais réellement `undefined`, contrairement à ce que supposait le commentaire Sprint 14A ayant introduit `whitespace-normal break-words` sans condition sur tous les en-têtes). Corrigé par `defaultColumn: { size: undefined }` sur `useTable()` — restaure le comportement voulu depuis le Sprint 14A : seule une colonne à `size` explicite reçoit une largeur contrainte et un wrap (désormais coupé aux espaces uniquement, jamais au milieu d'un mot — `break-words` retiré). `align-bottom` remplacé par `align-middle` pour que la barre d'en-têtes reste visuellement alignée entre colonnes sur une ligne et colonnes sur deux lignes. Portée : les 17 tables du SaaS utilisant `DataTable` (Réservations, Locations/Contrats, Factures, Paiements, Clients, Véhicules, Maintenances, Transferts, Bons de déplacement, etc.) héritent du correctif sans modification page par page. Deux tables hors de ce composant (tableau HTML natif) harmonisées séparément par un correctif ciblé et mineur : le journal d'audit (`/dashboard/audit`) et la vue « État des véhicules » (`VehicleStatusOverviewTable.tsx`, onglet Maintenance) — hauteur/padding alignés sur la même convention. « Alertes » (`AlertsList.tsx`) n'est pas un tableau (liste de cartes) — hors périmètre, non converti. Voir DOMAINRULES.md section 32 (nouvelle) ;
- **Alignement horizontal des en-têtes non modifié** — décision d'ingénierie documentée : l'énoncé demandait des en-têtes « visuellement centrées », interprété comme le centrage **vertical** de la barre (`align-middle`, le défaut réellement cassé), pas un centrage horizontal systématique de tous les libellés texte (qui aurait désaligné les en-têtes de leurs cellules de données, restées à gauche par la convention `meta.align` du Sprint 14D). **À reconfirmer explicitement** si un centrage horizontal est effectivement souhaité — voir DOMAINRULES.md section 32 ;
- Aucune nouvelle route API, aucune migration de schéma (correctif purement visuel/CSS, comme l'exige le périmètre du sprint) ; aucun nouveau fichier de test (le bug était un problème de rendu, pas de logique métier testable unitairement — vérifié à la place par inspection DOM/capture d'écran avec un vrai navigateur headless, voir TESTREPORT.md section 3 « Tests Sprint 14E ») — 362/362 tests toujours passés, aucune régression ;
- **Vérification manuelle** (Chromium/Playwright, installé temporairement hors du projet comme au Sprint 14D, aucune dépendance ajoutée à `package.json`) : inscription → connexion → inspection DOM de l'en-tête de `/dashboard/reservations` avant/après correctif (confirmant empiriquement le bug `columnDef.size` ci-dessus et sa résolution) → captures d'écran Réservations/Véhicules/Maintenances/Factures/Audit, rendu propre et aligné sur les cinq. Tenants de test créés pendant cette vérification supprimés de `xrent_dev` après coup (7 tenants préfixés « QA », identifiés par nom et supprimés par leurs seuls identifiants — jamais un `deleteMany` global sur la base de développement, même principe que la note Sprint 14D ci-dessous).

### Sprint 14D (rappel)

Le projet est passé par le stade **Sprint 14D — réalignement documentaire, reset de données de test depuis le SaaS, harmonisation UI**, validé :

- **Réalignement documentaire** : `CLAUDE.md` section 1 affirmait à tort que le projet était au « Sprint 0 (fondation), aucun module métier n'existe encore » — corrigé pour refléter l'état réel (Sprints 0 à 14C livrés, voir ce document et [PROJECT_MAP.md](./PROJECT_MAP.md)). Sections 4/5/6/8 également corrigées : le type des montants financiers (entier + devise) et le modèle d'isolation tenant (`tenant_id` partagé) étaient déjà tranchés depuis le Sprint 1 mais encore marqués « À DÉCIDER » ; `npm run test` manquait du tableau des commandes disponibles alors qu'il existe depuis le Sprint 2. **Constat** : [HANDOFF.md](./HANDOFF.md) (ce document, section 1), [PROJECT_MAP.md](./PROJECT_MAP.md), [DOMAINRULES.md](./DOMAINRULES.md) et [SECURITY.md](./SECURITY.md) étaient déjà à jour jusqu'au Sprint 14C — seul `CLAUDE.md` avait été laissé au stade Sprint 0 malgré l'avancement réel du projet ;
- **Reset de données métier/test depuis le SaaS** (`GET`/`POST /api/data-reset`, `src/lib/data-reset.ts`, nouveau) : un `ADMIN` peut vider les données métier/test de **son propre tenant** (jamais un autre — aucun paramètre `tenantId` accepté en entrée) depuis une carte « Zone dangereuse » sur `/dashboard/settings`. Vidé : `Client`/`Vehicle`/`Location`/`Invoice`/`Payment`/`Maintenance`/`VehicleTransfer`/`VehicleTrip`/`Alert`/`Invitation`/`Reservation`/`CashEntry`, plus `AuditLog` si l'option « réinitialisation complète » est cochée. Conservé : `Tenant`/`Agency`/`User`/`UserAgency`/`PermissionGroup`/`GroupPermission`/`UserPermission`/`ExpenseCategory`/`CashRegister` (solde recalculé à 0)/`Account`/`Session`/`VerificationToken`. Confirmation obligatoire : l'ADMIN doit saisir le nom exact du tenant (revalidé côté serveur) avant que le bouton ne s'active ; comptages réels affichés avant confirmation (`GET /api/data-reset`) ; action toujours journalisée après coup, y compris en mode « réinitialisation complète » ; verrou best-effort en mémoire de process contre un double-clic/une double exécution concurrente (`DataResetInProgressError`, 409) ; **techniquement impossible en environnement de production** (`process.env.NODE_ENV === "production"`, vérifié côté serveur, non contournable depuis le client) — complète une décision de sécurité validée dès le Sprint 1 (SECURITY.md section 17) dont le mécanisme précis restait ouvert. Interprétation du « super admin » de l'énoncé : aucun rôle transverse à plusieurs tenants n'existe dans ce schéma (décision reconduite depuis le Sprint 3/9, point 16 ci-dessous) — c'est donc le rôle `ADMIN` existant, scopé à un tenant, qui est utilisé. Voir DOMAINRULES.md section 31 (nouvelle) et SECURITY.md section 17 pour le détail complet ;
- **Harmonisation UI des tableaux** (`src/components/layout/DataTable.tsx`) : nouveau champ optionnel `meta.align` (`"left" | "center" | "right"`), typé via `metaHelper` de TanStack Table v9, appliqué à la fois à l'en-tête et à la cellule d'une colonne — corrigé une seule fois au niveau du composant partagé plutôt que dans chaque page. La colonne `id: "actions"` (kebab menu) est désormais alignée à droite **automatiquement dans toutes les tables**, sans qu'aucune page n'ait besoin de le déclarer — l'en-tête ne correspondait jusqu'ici pas à la cellule, déjà right-alignée manuellement par chaque page. Les colonnes monétaires/numériques réellement affichées (prix, montants, total, coût, jours) sont désormais explicitement alignées à droite dans `VehiclesTable`/`ReservationsTable`/`InvoicesTable`/`LocationsTable`/`MaintenancesTable`/`PaymentsTable`/`EntriesTable`/`ExpensesTable`. L'alignement bas des en-têtes multi-lignes (Sprint 14A) n'a pas été modifié (comportement déjà éprouvé, orthogonal à l'alignement horizontal traité ici). Voir DOMAINRULES.md section 27 (Design System, addendum Sprint 14D) ;
- Une seule nouvelle route API (`GET`/`POST /api/data-reset`) ; aucune migration de schéma (reset de données uniquement, comme l'exige l'énoncé du sprint — schéma et migrations intacts) ; nouveau fichier `src/__tests__/data-reset.test.ts` (+11 : autorisation ADMIN-only, aperçu des comptages, confirmation par nom, reset complet vérifié modèle par modèle avec isolation inter-tenant, option `includeAuditLog`, verrou de double-exécution, garde de production) — 362/362 tests passés au total, voir TESTREPORT.md section 3 « Tests Sprint 14D » ;
- `npm run lint` (dont l'ajout de `scripts/**` aux ignores ESLint pour l'outil CLI `scripts/reset-dev-data.js`, présent sur la copie de travail sans être commis jusqu'ici — voir note ci-dessous) / `npx tsc --noEmit` / `npm run test` / `npm run build` validés. **Première levée de la limite documentée depuis le Sprint 12B** : Chromium headless (Playwright, installé temporairement hors du projet, aucune dépendance ajoutée à `package.json`) a permis une vérification manuelle avec un vrai navigateur — inscription → connexion → ouverture de la boîte de dialogue de reset → comptages réels affichés → bouton de confirmation vérifié désactivé par défaut et avec un nom incorrect, activé uniquement avec le nom exact → reset exécuté → toast de succès → page rafraîchie (aucune erreur console) ; puis un véhicule créé avec un prix confirme visuellement (capture d'écran) que l'en-tête « Prix / jour » et sa cellule sont désormais alignés à droite, sans régression sur les colonnes texte. Données de test créées pendant cette vérification supprimées de `xrent_dev` après coup (tenants identifiés par préfixe, suppression scopée à leurs seuls identifiants — jamais un `deleteMany` global sur la base de développement) ;
- **Note sur `scripts/reset-dev-data.js`** : ce fichier existait déjà sur la copie de travail au démarrage de ce sprint (non commis dans Git jusqu'ici) — outil CLI distinct de la fonctionnalité SaaS ci-dessus, répondant à un besoin différent (reset **global**, tous tenants confondus, d'un environnement `xrent_dev`/`xrent_test` local pendant le développement, garde-fouté par nom de base `_dev`/`_test`). Conservé tel quel (CLAUDE.md règle 5 : ne pas supprimer de fichier existant sans instruction explicite) ; les deux mécanismes partagent le même ordre de suppression et le même périmètre conservé par cohérence, voir DOMAINRULES.md section 31.

### Sprint 14C (rappel)

Le projet est passé par le stade **Sprint 14C — transferts entre agences, bons de déplacement, revue maintenance, alertes métier**, validé :

- **Transfert entre agences (`VehicleTransfer`, nouveau)** : flux distinct de `Location`/`Reservation` — sert uniquement à déplacer un véhicule d'une agence à l'autre du même tenant, jamais de client ni de facturation impliqués. Champs : véhicule, agence/ville de départ (`fromAgencyId`/`fromCity`, toujours dérivés du véhicule côté serveur, jamais fournis par le client — même principe que `Location.agencyId`), agence/ville d'arrivée (`toAgencyId`/`toCity`, validées comme appartenant au même tenant et différentes de l'agence de départ), dates/kilométrages/niveaux de carburant de départ et d'arrivée (pourcentage 0-100, `Int` — aucune convention de jauge en usage ailleurs dans le projet), responsable (`responsibleUserId`, obligatoire), motif. Le transfert est « lancé » dès sa création (`status` par défaut `IN_TRANSIT`, pas d'étape brouillon distincte) : le véhicule doit être `AVAILABLE` au moment du lancement (409 `VehicleNotAvailableForTransferError` sinon — empêche un transfert incohérent sur un véhicule déjà loué/en maintenance/déjà en mobilité), et passe alors automatiquement `TRANSFERRING`. La validation (réception, `PATCH /api/vehicle-transfers/[id]/validate`, réservée à un user ayant accès à l'agence d'arrivée) rattache le véhicule à `toAgencyId` et le repasse `AVAILABLE` ; bloque (400) si le kilométrage d'arrivée est inférieur au départ ou si la date d'arrivée précède le départ. Annulation possible tant que `IN_TRANSIT` (le véhicule reste à son agence de départ, repasse `AVAILABLE`). Historique complet conservé (aucune suppression, `COMPLETED`/`CANCELLED` terminaux) ;
- **Bon de déplacement interne (`VehicleTrip`, nouveau)** : déplacement professionnel d'un véhicule par un employé du tenant, sans passer par `Location`/`Reservation` (aucun client, aucune facturation). Départ automatique à la création (`departureDate` = maintenant, `status` par défaut `IN_PROGRESS`) — kilométrage de départ obligatoire, carburant de départ optionnel ; véhicule/employé/motif/destination requis, `agencyId` toujours dérivé du véhicule. Le véhicule doit être `AVAILABLE` pour démarrer un déplacement (409 sinon), passe alors `ON_TRIP`. Au retour (`PATCH /api/vehicle-trips/[id]/return`) : le kilométrage retour doit être **strictement supérieur** au départ (`400` `InvalidVehicleTripOdometerError` sinon — contrainte explicite du sprint, différente du transfert où `>=` suffit), le véhicule repasse `AVAILABLE`. Annulation possible tant que `IN_PROGRESS`. Historique complet conservé ;
- **`VehicleStatus` étendu** (`TRANSFERRING`/`ON_TRIP`, migration `20260813143449_add_sprint14c_transfers_trips`) — seule dérogation à la règle « `Vehicle.status` reste un champ manuel » (DOMAINRULES.md section 5) : ces deux valeurs sont posées/retirées automatiquement par `src/lib/vehicle-transfers.ts`/`vehicle-trips.ts` (transaction Prisma partagée avec la création/validation/retour, pour éviter une course entre deux mobilités concurrentes sur le même véhicule), jamais assignables manuellement via `POST`/`PATCH /api/vehicles*` (400 explicite si tenté) — nécessaire pour que les sélecteurs `?status=AVAILABLE` déjà utilisés partout (`/dashboard/locations/new`, `/dashboard/reservations/[id]/convert`) excluent automatiquement un véhicule en mobilité, sans dupliquer cette vérification dans chaque formulaire ;
- **Nouvel annuaire minimal `GET /api/users/directory`** (id + name uniquement, jamais email/rôle) — ouvert à tout user authentifié du tenant, contrairement à `GET /api/users` (réservé ADMIN depuis le Sprint 4) : nécessaire pour qu'un `MEMBER` puisse désigner un « responsable »/« employé » à la création d'un transfert/déplacement sans lui donner accès à l'annuaire complet ;
- **Revue de l'onglet Maintenance** : `/dashboard/maintenances` affiche désormais une vue « État des véhicules » (`VehicleStatusOverviewTable.tsx`, nouveau) — immatriculation, état réel (incluant `TRANSFERRING`/`ON_TRIP`), date de retour (si une location `ACTIVE` existe pour ce véhicule), disponibilité calculée (`status === AVAILABLE` **et** aucune location `ACTIVE` — indépendante du seul champ `status`, pour donner un signal fiable même en cas de désynchronisation, voir `STOCK_INCONSISTENCY` ci-dessous). Un véhicule loué reste visible pour anticiper une maintenance à son retour, conformément à l'énoncé. Filtres directement dans les en-têtes (recherche immatriculation/nom, état, disponibilité) — filtrage client léger, cette vue n'a pas besoin d'être partageable par URL comme les autres listes du dashboard. L'historique des `Maintenance` (table existante depuis le Sprint 7, inchangée) reste affiché en dessous ;
- **Six nouvelles alertes métier** (`src/lib/scheduled-tasks.ts`, appelées par `POST /api/tasks/check-alerts`, toujours réservé ADMIN et scopé au tenant déclencheur, inchangé) : `CONTRACT_AT_RISK` (location `ACTIVE` dont le retour approche/est dépassé avec facture non soldée), `PAYMENT_DUE` (location `COMPLETED` avec facture encore `SENT`/`PARTIALLY_PAID`, y compris sans `dueDate` renseigné — comble un angle mort d'`INVOICE_OVERDUE`, qui exige un `dueDate` dépassé), `VEHICLE_UNAVAILABLE` (véhicule bloqué en `MAINTENANCE`/`TRANSFERRING`/`ON_TRIP` depuis plus de 3 jours), `RETURN_OVERDUE` (location `ACTIVE` dont le retour est déjà dépassé, pas seulement « aujourd'hui » comme `RETURN_TODAY`), `DOCUMENT_EXPIRED` (client au permis expiré ayant une location `PENDING`/`CONFIRMED`/`ACTIVE`), `STOCK_INCONSISTENCY` (véhicule `AVAILABLE` avec une location `ACTIVE` en cours, ou `RENTED` sans aucune location `ACTIVE` — désynchronisation entre le champ manuel et la réalité, jamais corrigée automatiquement). Chacune utilise un `entityType` distinct des vérifications existantes (`LocationAtRisk`, `InvoicePaymentDue`, `VehicleUnavailable`, `LocationOverdueReturn`, `ClientExpiredDocument`, `VehicleStockInconsistency`) — `hasUnresolvedAlert()` ne distingue pas par `AlertType`, seulement par `entityType`+`entityId`, donc réutiliser un `entityType` déjà pris par une autre vérification (ex. « Location ») aurait empêché deux alertes différentes de coexister sur la même ressource. Consultation/traitement (acknowledge/resolve) inchangés, `/dashboard/alerts` déjà ouvert à tout user du tenant ;
- Une seule migration de schéma (`20260813143449_add_sprint14c_transfers_trips`) ; huit nouvelles routes API (`GET`/`POST /api/vehicle-transfers`, `GET /api/vehicle-transfers/[id]`, `PATCH /api/vehicle-transfers/[id]/validate`, `PATCH /api/vehicle-transfers/[id]/cancel`, `GET`/`POST /api/vehicle-trips`, `GET /api/vehicle-trips/[id]`, `PATCH /api/vehicle-trips/[id]/return`, `PATCH /api/vehicle-trips/[id]/cancel`, `GET /api/users/directory`) ; deux nouvelles sections de sidebar (Transferts, Déplacements) ;
- Nouveaux fichiers `src/__tests__/vehicle-transfers.test.ts` (+11 : lancement, dérivation `fromAgencyId`, exclusion des sélecteurs `AVAILABLE`, blocage agence identique, blocage second transfert concurrent, isolation multi-tenant, accès MEMBER, validation + mise à jour agence, blocage kilométrage incohérent, refus de revalidation, annulation), `vehicle-trips.test.ts` (+9 : départ automatique, exclusion des sélecteurs `AVAILABLE`, blocage second déplacement concurrent, isolation multi-tenant, accès MEMBER, blocage kilométrage retour ≤ départ, retour valide, annulation), `vehicle-mobility-alerts.test.ts` (+2 : génération `STOCK_INCONSISTENCY`/`RETURN_OVERDUE` + consultation/traitement, rendu de la vue « État des véhicules ») — 351/351 tests passés au total, voir TESTREPORT.md section 3 « Tests Sprint 14C » ;
- `npm run lint` / `npx tsc --noEmit` / `npm run test` / `npm run build` validés. **Limite documentée, identique à tous les sprints depuis le 12B** : aucun outil d'automatisation navigateur disponible dans cette session — le parcours complet (création d'un transfert → véhicule exclu des sélecteurs disponibles → validation → agence mise à jour → bon de déplacement → retour avec kilométrage invalide refusé → retour valide → onglet Maintenance affichant l'état réel → génération et traitement d'une alerte métier) a été vérifié par requêtes HTTP directes contre un vrai serveur `next dev` de test (tests d'intégration ci-dessus), pas par une interaction navigateur réelle — en particulier, les filtres en en-tête de la vue « État des véhicules » (comportement purement client) n'ont été vérifiés que par lecture de code, pas par capture d'écran. Données de test nettoyées après chaque exécution.

### Sprint 14B (rappel)

Le projet est passé par le stade **Sprint 14B — numérotation de contrat, verrouillage, PDF contrat/facture/lots, paiement mixte**, validé : `Location.contractNumber` séquentiel (`"{préfixe}-{5 chiffres}"`) généré automatiquement à la création de toute location, à partir d'un compteur persisté par tenant (`Tenant.lastContractNumber`/`contractNumberPrefix`, redéfinissable depuis `/dashboard/settings`, réessai automatique en cas de collision) — jamais dérivé d'un `COUNT()` comme `Invoice.number`, précisément parce que l'énoncé exige de pouvoir reprendre une numérotation existante ; les dates d'un contrat (`startDate`/`endDate`) deviennent verrouillées (`409`) dès que son statut quitte `PENDING`, alors qu'elles restaient modifiables à tout statut jusqu'ici (comportement déjà testé sur `PENDING` depuis le Sprint 5, non régressé) ; nouveau PDF contrat (`GET /api/locations/[id]/pdf`, `ContractPdf.tsx`) et nouveaux PDF de lot (`POST /api/documents/batch-pdf`, un seul document multi-pages, sélection par identifiants/plage de dates/plage de numéros de contrat) pour les contrats et les factures, avec sélection de lignes dans `LocationsTable`/`InvoicesTable` ; le PDF facture affiche désormais le numéro de contrat associé ; correctif du paiement mixte — un paiement mixte dont la somme dépasse le solde restant dû n'écrit plus aucun `Payment` partiel avant d'échouer (validation du total avant toute écriture) et le message d'erreur est désormais toujours exprimé dans la devise de la facture (`formatMoney`), jamais un entier brut de centimes ; la même option de paiement mixte est désormais disponible depuis la fiche facture (`InvoiceActions.tsx`, pas seulement à la création de la location). Une seule migration de schéma (`20260813150025_add_sprint14b_contract_numbering`) ; deux nouvelles routes API (`GET /api/locations/[id]/pdf`, `POST /api/documents/batch-pdf`). `src/__tests__/locations.test.ts` étendu (+9 : numérotation séquentielle, respect des paramètres tenant, réessai sur collision, verrouillage des dates hors `PENDING`, PDF contrat) ; `location-payment.test.ts` étendu (+1 : paiement mixte en dépassement sans écriture partielle) ; `invoices.test.ts` étendu (+2 : PDF facture, jusqu'ici non testé) ; `tenants.test.ts` étendu (+2 : validation des nouveaux champs) ; nouveau fichier `batch-pdf.test.ts` (+10) — 329/329 tests passés au total, voir TESTREPORT.md section 3 « Tests Sprint 14B ». `npm run lint` / `npx tsc --noEmit` / `npm run test` / `npm run build` validés. Vérification manuelle complémentaire, comme tous les sprints depuis le 12B : aucun outil d'automatisation navigateur disponible dans cette session — scénario complet vérifié par requêtes `curl` directes contre un serveur `next dev` réel (paramétrage du préfixe/dernier numéro → 2 contrats consécutifs numérotés correctement → paiement mixte en dépassement refusé proprement, sans écriture partielle, message formaté en devise → verrouillage des dates confirmé après confirmation du contrat, toujours libre en `PENDING` → PDF contrat/facture/lots par identifiants/plage de dates/plage de numéros tous générés avec succès → pages `/dashboard/locations`, `/dashboard/locations/[id]`, `/dashboard/invoices`, `/dashboard/settings` répondent 200). Données de test nettoyées après vérification.

### Sprint 14A (rappel)

Le projet est passé par le stade **Sprint 14A — socle véhicules/réservations, visibilité, filtres**, validé : `Vehicle.pricePerDay` devient optionnel/informatif (migration `20260813112824`), le prix réel est désormais toujours saisi explicitement à la location/au contrat (`MissingPriceError` sinon) ; `getSessionUser()` relit `role` frais en base à chaque requête — un changement de rôle s'applique immédiatement sans reconnexion, fermant une fenêtre de sécurité réelle (un ADMIN rétrogradé gardait un accès complet jusqu'à expiration du JWT, 30 jours) ; la sidebar est réalignée sur les vérifications réellement appliquées par chaque page/route (correctif d'un sur-masquage pour les groupes de permissions non-ADMIN et d'un sous-masquage pour Invitations/Permissions) ; import Excel des réservations robuste aux dates Excel sérialisées, aux dates `DD/MM/YYYY` et corrige le bug de « date parasite » sur les heures ; villes de départ/retour validées contre les agences réelles du tenant (import et saisie manuelle) ; sélection multiple + suppression en masse sur `ReservationsTable` ; nouveaux filtres villes/catégorie ; astérisque rouge sur les champs obligatoires dans tout le SaaS. Visibilité véhicules/agence/ville déjà conforme, vérifiée sans changement de code. Aucune nouvelle route API (uniquement des champs de corps de requête étendus sur des routes existantes) ; une seule migration de schéma (`Vehicle.pricePerDay` nullable). `src/__tests__/{users,vehicles,locations,reservations}.test.ts` étendus (+18) : rafraîchissement de rôle sans reconnexion (promotion **et** rétrogradation, même cookie de session qu'avant la mutation) ; véhicule créé sans prix + `MissingPriceError`/prix explicite prioritaire à la location et à la conversion ; import Excel (numéro de série Excel, date `DD/MM/YYYY`, message d'erreur distinguant date manquante/invalide, correctif de l'heure stockée comme cellule `Date`, ville connue/inconnue au départ et au retour) ; création manuelle avec validation de ville ; filtres `pickupAgency`/`dropoffAgency`/`vehicleCategory` — 305/305 tests passés au total, voir TESTREPORT.md section 3 « Tests Sprint 14A ». 305/305 tests passants (+18).

### Sprint 13E (rappel)

- **Écarts par rapport à l'énoncé générique du sprint, imposés par l'architecture réelle du projet (pas des choix de confort)** : l'énoncé supposait un projet sans design system existant. Ce projet possède déjà `src/components/ui/button.tsx`/`card.tsx` (shadcn/ui, base `@base-ui/react` + `class-variance-authority`) utilisés dans tout le dashboard, et Tailwind v4 (configuration exclusivement en CSS via `@theme`, aucun `tailwind.config.ts` — voir AGENTS.md). Créer `Button.tsx`/`Card.tsx` (majuscule) à côté aurait (a) littéralement collisionné avec les fichiers existants sur un système de fichiers insensible à la casse (macOS/APFS par défaut), et (b) dupliqué une abstraction déjà en place partout — contraire à CLAUDE.md section 7. Le système de boutons/cartes existant a donc été étendu, jamais remplacé ;
- `src/components/ui/button.tsx` : animations de clic/survol ajoutées à la base commune de tous les boutons (`transition-all duration-200 ease-out`, `active:scale-95` — respecte la garde `not-aria-[haspopup]` déjà en place pour ne pas appliquer l'effet d'enfoncement aux boutons qui ouvrent un menu —, `hover:shadow-lg hover:-translate-y-0.5`, désactivées automatiquement via `disabled:pointer-events-none` déjà existant) ; variante `default` (bouton principal) passée à un survol plein `hover:bg-blue-700` ; variante `destructive` (bouton danger) passée d'un style « doux » (texte rouge sur fond rouge clair) à un style plein `bg-destructive text-white hover:bg-red-700`, conforme à l'énoncé — les 21 boutons `variant="destructive"` déjà existants dans l'app en bénéficient automatiquement, sans modification de leur code appelant ;
- `src/components/ui/card.tsx` : `rounded-2xl` (au lieu de `rounded-xl`), bordure `border border-slate-100` (remplace l'ancien `ring-1 ring-foreground/10`), nouvelle prop optionnelle `hoverable` (soulèvement `hover:-translate-y-0.5 hover:shadow-lg`) — `CardHeader`/`CardFooter` alignés sur le même rayon d'arrondi ;
- **Pas de `CarCard.tsx`** : l'énoncé décrivait une carte véhicule pour un catalogue de location « grand public » (image, prix, bouton Réserver) — ce module n'existe pas dans XRent Manager, dashboard de gestion interne (tables CRUD `VehiclesTable.tsx`, pas de vitrine client). Composant non créé, aucun besoin réel identifié (CLAUDE.md section 7) ;
- `src/app/layout.tsx` : police Geist remplacée par **Inter** (`next/font/google`, variable `--font-inter`) ; `src/app/globals.css` : **correctif** — le token `--font-sans` était défini de façon circulaire (`--font-sans: var(--font-sans)`, ne référençait jamais `--font-geist-sans`), si bien que Geist n'était en réalité jamais appliqué par les utilitaires `font-sans`/`font-heading` malgré son import depuis le Sprint 0 — corrigé en le faisant pointer vers `--font-inter` ;
- `src/app/globals.css` : valeurs oklch de `--destructive`/`--warning`/`--success` resserrées sur les valeurs exactes Tailwind v4 `red-600`/`orange-500`/`green-600` (source : `node_modules/tailwindcss/theme.css`, jusque-là des approximations proches mais pas exactes depuis le Sprint 12C) ; nouveau token `--info` (`cyan-600`) ; `--foreground`/`--muted-foreground`/`--background` resserrés sur `slate-900`/`slate-500`/`slate-50` exacts. `--primary` (`blue-600`) était déjà exact depuis le Sprint 12C, inchangé. `StatusBadge.tsx` (Sprint 13B) utilise déjà des classes Tailwind littérales (`bg-orange-100`, etc.), pas ces tokens — déjà conforme à la palette demandée, non modifié ;
- `src/components/layout/BottomNav.tsx` (nouveau) : navigation rapide mobile (`md:hidden`, fixée en bas), 4 entrées (Accueil/Véhicules/Réservations/Profil, icônes `lucide-react` cohérentes avec `Sidebar.tsx` plutôt que les émojis de l'énoncé), cibles tactiles `min-h-12`. **Complète la sidebar existante, ne la remplace pas** : `Sidebar.tsx` propose déjà 18 entrées et reste accessible en mobile via le tiroir hamburger du `Header` depuis le Sprint 4 — la barre du bas ne couvre que les 4 sections les plus fréquentes ;
- `src/components/layout/DashboardLayout.tsx` : intègre `<BottomNav />`, `<main>` reçoit `pb-20` sur mobile (`md:pb-0`) pour ne pas masquer le contenu sous la barre fixe. **`Header.tsx` reste affiché sur mobile** (déviation par rapport à l'énoncé, qui le décrivait desktop uniquement) : il porte la cloche d'alertes et le menu utilisateur/déconnexion, inaccessibles autrement sur mobile ;
- Cibles tactiles ≥48px (`h-12`) sur mobile : `Sidebar.tsx` (liens du tiroir, `max-md:min-h-12 max-md:py-3`), `Header.tsx` (boutons menu/cloche, `max-md:size-12`) — appliqué uniquement aux éléments de navigation mobile visés par l'énoncé, pas à l'ensemble des boutons icône des tables denses du dashboard (aurait cassé la mise en page desktop sans bénéfice, hors périmètre) ;
- Aucune migration de schéma, aucune route API, aucune logique métier modifiée ce sprint — changements strictement visuels ;
- `npm run lint` / `npx tsc --noEmit` / `npm run test` (287/287, inchangé — aucun test automatisé nouveau, voir TESTREPORT.md) / `npm run build` validés. Vérification manuelle : rendu HTML réel inspecté via `curl` (`/login` : classe `--font-inter` bien présente sur `<html>`, `rounded-2xl border-slate-100` sur les cartes, `hover:bg-blue-700`/`active:scale-95` sur les boutons) ; `/dashboard` authentifié déjà couvert par `ui.test.tsx` (200, contenu attendu), confirmant que `BottomNav` ne casse pas le rendu serveur. Aucun outil d'automatisation navigateur disponible dans cette session (limite identique à tous les sprints depuis le 12B) : les animations elles-mêmes (survol, clic) et le rendu visuel de la Bottom Nav n'ont pas pu être vérifiés par capture d'écran réelle, seulement par lecture de code et inspection du HTML/CSS généré.

### Sprint 13D (rappel)

Le projet est passé par le stade **Sprint 13D — refonte du flux réservation → contrat**. Le dépôt contenait déjà, en plus des sprints précédents :

- Nouveau flux de conversion (voir DOMAINRULES.md section 26 pour le détail complet des décisions) : `src/app/dashboard/reservations/[id]/page.tsx` propose désormais des boutons d'action explicites selon le statut (« Confirmer la réservation » si `PENDING`, « Convertir en contrat » — lien vers le formulaire dédié — si `CONFIRMED`, « Voir le contrat »/« Télécharger le contrat PDF » si `CONVERTED`, « Annuler » si `PENDING`/`CONFIRMED`) plutôt que l'ancien bouton générique « Changer le statut » (`ReservationActions.tsx`, réécrit) combiné à une boîte de dialogue de conversion minimale (`ConvertReservationCard.tsx`, **supprimé**) ;
- `src/app/dashboard/reservations/[id]/convert/page.tsx` (nouveau, Server Component) + `ConvertReservationForm.tsx` (nouveau, Client Component) : formulaire de conversion pré-rempli depuis la réservation (identité client, dates/heures, notes) — l'utilisateur vérifie/complète les informations d'identité du client (email, adresse, CIN/permis — absentes des réservations importées en texte libre, voir DOMAINRULES.md section 21), choisit un véhicule réel parmi les `AVAILABLE` de la catégorie de la réservation (filtrable par agence, qui ne sert qu'à filtrer la liste — l'agence du contrat reste dérivée du véhicule choisi côté serveur, jamais du sélecteur, voir déviation documentée en section 26), et renseigne le paiement (même formulaire que `/dashboard/locations/new`, Sprint 13A) ;
- `POST /api/reservations/[id]/convert` (`src/app/api/reservations/[id]/convert/route.ts`, réécrit) : nouveau contrat de corps de requête (`vehicleId`, `startDate`/`endDate` ISO, `deposit`, `notes`, `client` — objet complet plutôt que les seuls champs bruts de la réservation, `useExistingClientId`/`forceCreateClient`, `payment`) ; la détection de doublons clients (section 9) et le paiement intégré (section 23) fonctionnent désormais exactement comme sur `POST /api/locations` ;
- `src/lib/location-payment.ts` (nouveau) : `validatePaymentInput`/`processLocationPayment` — logique de paiement intégré (simple/partiel/mixte/au retour, création des `Payment` + `CashEntry` correspondantes) extraite de `POST /api/locations` (Sprint 13A) pour être réutilisée telle quelle par la conversion, sans duplication ; `POST /api/locations` a été mis à jour pour l'utiliser, sans changement de comportement (couvert par les 5 tests existants de `location-payment.test.ts`, toujours passants) ;
- **Déviation documentée par rapport à l'énoncé du sprint** : la conversion reste **résiliente**, pas strictement transactionnelle (`prisma.$transaction`) malgré la demande explicite de l'énoncé — même principe déjà en vigueur depuis les Sprints 12B/13A (un échec de génération de facture ou d'enregistrement de paiement ne fait jamais échouer la conversion elle-même). Rendre l'ensemble atomique aurait nécessité de refactorer en profondeur les fonctions d'écriture de `src/lib/locations.ts`/`invoices.ts`/`payments.ts`/`cash-register.ts`/`clients.ts` (aucune n'accepte aujourd'hui de client de transaction Prisma partagé), avec un risque de régression réel contre les 287 tests déjà validés — hors périmètre proportionné de ce sprint, voir DOMAINRULES.md section 26 pour le détail ;
- La machine à états de `Reservation` (`canTransition`, `src/lib/reservations.ts`) n'a **pas changé** — `PENDING → CONVERTED` directement reste autorisé côté API (les tests existants qui l'exercent restent valides) ; seule l'interface restreint désormais l'affichage du bouton « Convertir en contrat » au statut `CONFIRMED` ;
- Aucune migration de schéma ce sprint ;
- `src/__tests__/reservations.test.ts` (extension du describe `POST /api/reservations/[id]/convert`, +2 tests) : refus sans `client.firstName`/`client.lastName` (nouveau contrat de requête) ; conversion avec paiement intégré (mode simple, complet) — facture `PAID`, `CashEntry` créée. Nettoyage `afterAll` étendu (`cashEntry`/`cashRegister` avant `tenant`, même correctif que Sprint 13A) — 287/287 tests passés au total (+2, voir TESTREPORT.md section 3 « Tests Sprint 13D ») ;
- `npm run lint` / `npx tsc --noEmit` / `npm run test` / `npm run build` validés. Vérification manuelle complémentaire, comme tous les sprints depuis le 12B : aucun outil d'automatisation navigateur disponible dans cette session — le scénario complet (inscription → agence → véhicule → réservation DIRECT → détail en PENDING affiche Confirmer/Annuler → confirmation → détail en CONFIRMED affiche Convertir en contrat → formulaire de conversion pré-rempli → conversion avec véhicule réel + identité client complète + caution + paiement partiel → détail en CONVERTED affiche Voir le contrat/Télécharger le PDF → téléchargement du PDF) a été vérifié par requêtes `curl` directes contre un serveur `next dev` réel, y compris une inspection du HTML réellement rendu pour confirmer visuellement la présence/absence des boutons selon le statut et le pré-remplissage du formulaire. Données de test nettoyées après vérification.

### Sprint 13C (rappel)

Le projet est passé par le stade **Sprint 13C — correctif agences MEMBER, indicatifs téléphone, heure de retour auto, voucher DIRECT auto**. Le dépôt contenait déjà, en plus des sprints précédents :

- **Bug réel identifié et corrigé (le diagnostic initial du sprint était erroné — voir ci-dessous)** : l'énoncé de ce sprint supposait que le bug « un `MEMBER` ne voit pas les véhicules » venait de `src/lib/permissions.ts` (mauvais groupe, mauvaise assignation). Vérification faite : `PERMISSIONS`/`DEFAULT_GROUPS`/`can()` étaient déjà corrects, et de toute façon **hors sujet** — `GET /api/vehicles` (comme `/api/locations`, `/api/clients`) ne vérifie jamais de permission granulaire, seulement `canAccessAgency`/`getAccessibleAgencyIds` (`src/lib/authz.ts`, décision de portée du Sprint 12C, voir HANDOFF.md section « Sprint 12C » et DOMAINRULES.md section 4). Le vrai bug : **aucun code de l'application, nulle part, ne créait jamais de `UserAgency`** — ce lien n'existait que dans les fixtures de test (`prisma.userAgency.create` direct). Un `MEMBER` fraîchement invité avait donc `getAccessibleAgencyIds() === []` en permanence, et le filtrage volontaire « un MEMBER ne voit que les véhicules de ses agences » (SECURITY.md section 2) le réduisait de fait à ne jamais rien voir, sans aucun moyen d'en sortir — reproduit et vérifié manuellement avant correctif (voir TESTREPORT.md) ;
- `src/lib/users.ts` : `getUserAgencyIds(userId)` et `setUserAgencies(tenantId, userId, agencyIds)` (nouveau) — remplacement complet de l'ensemble des agences assignées (pas d'ajout incrémental), valide que chaque `agencyId` appartient bien au tenant appelant (`InvalidAgencyError`, 400 sinon — isolation multi-tenant, voir SECURITY.md section 1) ;
- `PATCH /api/users/[id]` accepte désormais un `agencyIds?: string[]` optionnel (en plus de `role`/`password`, inchangés) ; `GET /api/users/[id]` renvoie désormais `agencyIds` en plus des champs existants ; nouvelle action d'audit `user.agencies_changed` (`ACTION_LABELS`, `src/app/dashboard/audit/page.tsx`) ;
- `src/app/dashboard/users/[id]/EditUserForm.tsx` : nouvelle section « Agences » (grille de cases à cocher, réutilise `Checkbox`), visible uniquement quand le rôle sélectionné est `MEMBER` (sans effet pour un `ADMIN`, qui a un accès transverse à toutes les agences du tenant indépendamment de `UserAgency` — voir `src/lib/authz.ts`) ; `src/app/dashboard/users/[id]/page.tsx` charge la liste des agences du tenant et les `agencyIds` actuels du user ciblé côté serveur ;
- `src/components/ui/phone-input.tsx` : liste étendue de 5 à 20 indicatifs pays (Maroc en premier, puis ordre alphabétique, drapeaux emoji), dropdown de recherche par nom/indicatif (composant interne, pas de librairie de type Combobox ajoutée — le projet n'en avait aucune), dernier pays choisi mémorisé en `localStorage` (`phoneInput_country`). **Point technique** : la lecture initiale de `localStorage` utilise `useSyncExternalStore` plutôt qu'un `useEffect` + `setState` au montage (qui aurait provoqué soit un flash de contenu après hydratation, soit une erreur ESLint `react-hooks/set-state-in-effect` — même règle déjà rencontrée au Sprint 5, voir TESTREPORT.md ligne du 2026-08-11) ; le pays cliqué dans le menu est gardé en état React le temps que l'utilisateur tape un chiffre (sans quoi le choix serait immédiatement écrasé par la valeur vide du champ numéro, `value` ne pouvant encoder un indicatif sans chiffres derrière) ;
- `src/app/dashboard/reservations/new/page.tsx` et `src/app/dashboard/locations/new/page.tsx` : modifier l'heure de départ recopie désormais automatiquement la même valeur dans l'heure de retour (`handleStartTimeChange`) — l'utilisateur reste libre de modifier ensuite l'heure de retour manuellement, la synchronisation ne se redéclenchant qu'au changement de l'heure de *départ* ;
- `src/lib/reservations.ts` : `generateDirectVoucherNumber(tenantId)` (nouveau) — format `Dir-{4 chiffres}`, compteur basé sur le nombre de réservations `DIRECT` déjà créées pour le tenant, avec vérification explicite d'absence de collision (pas de contrainte `@@unique` en base sur `voucherNumber`, aucune migration de schéma ce sprint — décision explicite de l'énoncé) ; `POST /api/reservations` génère ce numéro quand `source: "DIRECT"` et `voucherNumber` absent, continue d'exiger `voucherNumber` pour toute autre source (400 sinon) ; `/dashboard/reservations/new` masque le champ « N° voucher » et affiche un message informatif quand la source sélectionnée est DIRECT ;
- Aucune migration de schéma ce sprint (voir DOMAINRULES.md section 25 et CLAUDE.md section 2 point 6) ;
- `src/__tests__/users.test.ts` (extension, +6 tests) : agence absente par défaut pour un MEMBER fraîchement créé, assignation par un ADMIN, remplacement complet (pas d'ajout), isolation multi-tenant (agence d'un autre tenant refusée), et un test de bout en bout démontrant l'effet réel du correctif (`GET /api/vehicles` vide avant assignation, peuplé après) ; `src/__tests__/reservations.test.ts` (extension, +3 tests) : refus d'un voucher manquant pour BROKER, génération `Dir-0001`/`Dir-0002` pour DIRECT, respect d'un voucher DIRECT fourni manuellement — 285/285 tests passés au total (+8, voir TESTREPORT.md section 3 « Tests Sprint 13C ») ;
- `npm run lint` / `npx tsc --noEmit` / `npm run test` / `npm run build` validés. **Vérification manuelle complémentaire, comme tous les sprints depuis le 12B** : aucun outil d'automatisation navigateur disponible dans cette session — le scénario complet (inscription → agence → véhicule → invitation MEMBER → acceptation → `GET /api/vehicles` vide → assignation via `PATCH /api/users/[id]` → `GET /api/vehicles` peuplé → isolation multi-tenant refusée → génération de voucher DIRECT) a été vérifié par requêtes `curl` directes contre un serveur `next dev` réel, plus une inspection du HTML réellement rendu (`/dashboard/users/[id]`, `/dashboard/reservations/new`) pour confirmer visuellement la case à cocher d'agence et l'indicatif Maroc par défaut. Le remplissage automatique de l'heure de retour et la recherche dans le sélecteur de pays sont un comportement purement client, non vérifiable par `curl` — revus par lecture de code uniquement. Données de test nettoyées après vérification.

### Sprint 13B (rappel)

- `src/lib/reservations.ts` : `RESERVATION_IMPORT_COLUMN_MAP` remplace `RESERVATION_IMPORT_COLUMNS` comme source de vérité — fait correspondre chaque en-tête Excel **en français** (« Numéro voucher », « Date de départ », etc., 28 colonnes dans l'ordre exact de l'énoncé) au nom du champ interne `Reservation` correspondant ; `RESERVATION_IMPORT_COLUMNS` reste exporté (dérivé, `Object.keys(...)`) pour l'affichage et les tests. `REQUIRED_IMPORT_FIELDS` (nouveau) associe chacun des 5 champs obligatoires à son en-tête français, pour que `parseReservationImportRow` rapporte une erreur précise par colonne (`Colonne obligatoire manquante: Date de départ`, format identique à l'exemple de l'énoncé du sprint) plutôt qu'une liste agrégée de noms de champs anglais ;
- `src/app/api/reservations/import/route.ts` : la résolution des colonnes du fichier Excel se fait désormais par en-tête français → champ interne (`RESERVATION_IMPORT_COLUMN_MAP`) avant d'appeler `parseReservationImportRow` — le reste de la route (préview/commit, détection de doublon de `voucherNumber`, réponse `{ imported, errors, duplicates, preview? }`) est inchangé ;
- `src/lib/format.ts` (fonctions pures, sans import Prisma) : `combineDateAndTime`/`calculateDaysCount` déplacées ici depuis `src/lib/reservations.ts` (réexportées telles quelles pour les appelants serveur existants — `POST /api/reservations/[id]/convert` n'a pas changé) — nécessaire pour que `ReservationsTable.tsx` (composant client) puisse recalculer le nombre de jours à l'affichage sans importer `src/lib/reservations.ts` (qui importe `src/lib/prisma.ts`, serveur uniquement) ;
- **Décision d'affichage (déduite de l'énoncé, à confirmer si contestée)** : la colonne « Jours » de `ReservationsTable` est **recalculée à l'affichage** depuis `startDate`/`startTime`/`endDate`/`endTime` avec la même règle que `calculateTotalPrice` (`src/lib/locations.ts`, Sprint 5 : heures arrondies au jour supérieur, minimum 1 jour), plutôt que d'afficher tel quel le champ `daysCount` stocké (valeur brute importée du fichier Excel, potentiellement incohérente avec cette règle si le broker source compte différemment) — ce dernier reste néanmoins stocké sans modification (aucune réconciliation automatique). De même, `finalPrice` (= `totalPrice + gpsPrice + babySeatPrice + extraDriverPrice`) est une valeur calculée à l'affichage uniquement, **aucun champ correspondant n'a été ajouté au modèle** `Reservation` (voir DOMAINRULES.md section 24) ;
- `src/app/dashboard/reservations/ReservationsTable.tsx` : réécrite avec les 16 colonnes demandées dans l'ordre exact de l'énoncé (Voucher, Source, N° vol, Client — `clientLastName clientFirstName` concaténés dans cet ordre —, Départ, Retour, Jours, Ville de départ [`pickupAgency`], Ville de retour [`dropoffAgency`], Catégorie, GPS, Siège bébé, Conducteur suppl., Prix total, Prix final, Remarque — tronquée à 50 caractères + `...`) + une colonne Statut (non listée explicitement dans l'énoncé de la section 2, mais nécessaire pour appliquer `StatusBadge` comme demandé en section 3) ;
- `src/components/ui/StatusBadge.tsx` (nouveau) : badge de statut coloré réutilisable, couvre à la fois `ReservationStatus` et `LocationStatus` (vocabulaires partiellement communs) — couleurs exactes de l'énoncé (CONFIRMED vert, PENDING orange, CANCELLED rouge, COMPLETED bleu, CONVERTED violet). **Écart non spécifié par l'énoncé** : `ACTIVE` (propre à `Location`, absent de la liste de couleurs demandée) reçoit une couleur cyan distincte, choix d'ingénierie non validé explicitement — à confirmer si jugé nécessaire (voir DOMAINRULES.md section 24) ; appliqué à `ReservationsTable.tsx` et `src/app/dashboard/locations/LocationsTable.tsx` (remplace les `STATUS_LABELS`/`STATUS_VARIANTS` locaux à chaque table, désormais centralisés) ;
- `src/app/dashboard/reservations/page.tsx` : bascule vers `getReservations` (`src/lib/reservations.ts`, déjà existant depuis le Sprint 12C mais pas utilisé par cette page qui interrogeait Prisma directement) plutôt qu'une requête Prisma dupliquée — ajoute les filtres date (`from`/`to`, deux `<input type="date">`) demandés par l'énoncé ; le filtre de recherche texte (`search`) est étendu à `flightNumber` en plus de `voucherNumber`/`clientFirstName`/`clientLastName` (demandé explicitement : « recherche texte (voucher, client, flightNumber) ») ;
- Aucune migration de schéma ce sprint : tous les champs `Reservation` nécessaires à l'affichage/l'import (28 colonnes) existaient déjà depuis le Sprint 12C ;
- `src/__tests__/reservations.test.ts` : le helper `importRow` construit désormais une ligne d'import à partir de valeurs indexées par nom de champ interne (inchangé pour les appelants du test) mais traduites vers l'en-tête français exact via `RESERVATION_IMPORT_COLUMN_MAP` avant d'être écrites dans le fichier `.xlsx` de test — seul changement nécessaire pour que les tests existants restent valides avec le nouveau format d'en-tête ;
- `npm run lint` / `npm run test` / `npm run build` validés (277/277 tests, aucun nouveau test — voir TESTREPORT.md section 3 « Tests Sprint 13B »). **Vérification renforcée par rapport aux sprints précédents** : toujours aucun outil d'automatisation navigateur disponible dans cette session, mais le HTML **réellement rendu côté serveur** (`curl` de `/dashboard/reservations`/`/dashboard/locations` authentifiées, pas seulement les réponses JSON des routes API) a été inspecté pour confirmer visuellement les 16 en-têtes de colonne, les classes de couleur exactes des badges de statut (`bg-orange-100`/`bg-green-100`/`bg-red-100`, etc.) pour `Reservation` **et** `Location`, les badges OUI/NON, la troncature des remarques, le calcul de `finalPrice` et le recalcul de la colonne Jours ; import Excel testé avec un fichier `.xlsx` généré via `exceljs` reproduisant exactement les 28 en-têtes français, y compris une ligne avec colonne obligatoire manquante (message d'erreur vérifié identique à l'exemple de l'énoncé) et un doublon de voucher au sein du même fichier. Données de test nettoyées après vérification.

### Sprint 13A (rappel)

- `prisma/schema.prisma` : nouveaux modèles `CashRegister` (singleton par tenant, `@@unique tenantId`, pas de notion d'agence — même principe que `Reservation`, DOMAINRULES section 21), `CashEntry` (écriture append-only : pas de update/delete, même principe que `Alert`, DOMAINRULES section 19), `ExpenseCategory` (tenant-scopée, `@@unique [tenantId, name]`) + enum `CashEntryType` (`ENTRY`/`EXPENSE`) ; migration `20260813001505_add_sprint13a_cash_register` appliquée sur `xrent_dev` et `xrent_test` ;
- **Déviation documentée (décision d'ingénierie, pas un changement de règle métier)** : `CashRegister.currentBalance`/`currentMonth`/`previousBalance` ne sont **jamais incrémentés directement** — ils sont entièrement recalculés (`recomputeCashRegisterBalance`, `src/lib/cash-register.ts`) à partir des `CashEntry` réels après chaque écriture, exactement comme `Invoice.amountPaid`/`status` sont recalculés depuis les `Payment` réels (`recomputeInvoiceStatus`, Sprint 6, DOMAINRULES section 10) plutôt que d'être un compteur mutable — pour ne jamais laisser un solde de caisse dériver de la réalité des écritures. `previousBalance` = solde de toutes les écritures antérieures au 1er du mois en cours ; `monthEntries`/`monthExpenses` = sommes du mois en cours ; `currentBalance` (= `finalBalance`) = `previousBalance + monthEntries - monthExpenses` ;
- `src/lib/cash-register.ts` (nouveau) : `getOrCreateCashRegister`, `recomputeCashRegisterBalance`, `createCashEntry` (valide `amount` entier positif, dérive toujours `currency` du `CashRegister`, jamais du client), `getCashEntries` (filtres `type`/`category`/dates/`take`), `getDailyBreakdown` (répartition entrées/dépenses par jour, pour le graphique), `getExpenseCategories`/`createExpenseCategory` ; routes `GET`/`POST /api/cash-register` (solde recalculé + répartition par jour sur 30 jours + 10 dernières opérations / création d'une écriture), `GET /api/cash-register/entries`, `GET /api/cash-register/expenses`, `GET`/`POST /api/cash-register/categories` ;
- **Déviation documentée (au-delà de la liste de fichiers du sprint)** : `GET`/`POST /api/cash-register/categories` a été ajouté bien que non listé dans l'énoncé initial — sans cette route, le modèle `ExpenseCategory` n'aurait jamais été accessible en écriture depuis l'UI (le formulaire de dépense a besoin d'une liste de catégories à proposer/compléter). Volontairement minimal : pas de `PATCH`/`DELETE` (renommer/supprimer une catégorie déjà référencée en texte libre par des `CashEntry` existants romprait leur lisibilité historique, hors périmètre de ce sprint) ;
- **Déviation documentée** : `CashEntry.category` est un champ texte libre (pas un enum), y compris pour les entrées (valeurs suggérées côté UI : `VERSEMENT`/`COMMISSION`/`VIREMENT`) — même principe que `Vehicle.category`/`Alert.entityType` (DOMAINRULES section 6), pour rester extensible sans migration à chaque nouvelle catégorie ; `CashEntry.contractId` pointe en texte libre vers une `Location` (pas de contrainte de clé étrangère, même principe), pour ne jamais faire dépendre l'historique de caisse de l'existence continue de la location ;
- pages `/dashboard/cash-register` (solde actuel, report du mois précédent, entrées/dépenses du mois, solde final, graphique entrées vs dépenses par jour via `recharts`, 10 dernières opérations), `/dashboard/cash-register/entries` (liste + formulaire de saisie rapide), `/dashboard/cash-register/expenses` (liste + formulaire avec sélection/création de catégorie) ; entrée de sidebar « Caisse » ajoutée (pas de clé de permission — même traitement que maintenances/alertes/invitations, modules sans permission granulaire ce sprint, voir DOMAINRULES section 22) ; accessible à tout user authentifié du tenant (ADMIN et MEMBER), sans restriction de rôle ni notion d'agence — décision alignée sur le traitement de `/dashboard/payments`/`/dashboard/invoices` (pas `/dashboard/reports`, réservé ADMIN car agrégat financier transverse, alors que la Caisse est un outil opérationnel quotidien) ;
- `src/app/dashboard/locations/new/page.tsx` (formulaire de création de location) étendu d'une section « Paiement » : case « Paiement au retour » (aucun `Payment` créé, la facture `DRAFT` auto-générée depuis le Sprint 12B reste telle quelle), case « Paiement mixte » (2 lignes mode+montant), case « Paiement partiel » (montant payé inférieur au total, sinon le total facturé est réglé intégralement) — validation client (somme ≤ prix estimé de la location) en plus de la validation serveur ;
- **Déviation documentée** : l'énoncé du sprint désignait un fichier `src/app/dashboard/locations/LocationForm.tsx` à modifier — ce fichier n'existe pas, le formulaire de création de location vit entièrement dans `src/app/dashboard/locations/new/page.tsx` depuis le Sprint 5 (aucun composant de formulaire séparé n'a jamais été extrait). La section Paiement a donc été ajoutée directement à ce fichier plutôt que de créer une extraction non demandée par ailleurs ;
- `src/app/api/locations/route.ts` (`POST`) : accepte désormais un objet `payment` optionnel, validé avant toute création (`validatePaymentInput`) ; après la génération automatique de la facture `DRAFT` (Sprint 12B, inchangée), un ou deux `Payment` sont créés via `createPayment` (Sprint 6, **totalement réutilisé sans modification** — le calcul du statut de facture `SENT`/`PARTIALLY_PAID`/`PAID` est donc déjà entièrement géré par `recomputeInvoiceStatus`, aucune duplication de cette règle dans ce sprint) ; chaque `Payment` réussi crée en plus une `CashEntry` (`type: "ENTRY"`, `category: "VERSEMENT"`, `contractId` = id de la location) via `createCashEntry` — un contrat réglé à la création apparaît donc immédiatement dans la Caisse. Résilient par choix, même principe que la génération automatique de facture (Sprint 12B) : un échec du paiement (ex. montant incohérent détecté tardivement) ne fait jamais échouer la création de la location elle-même — l'erreur est renvoyée dans `paymentError` (réponse `201`, jamais un statut d'erreur) pour que l'UI affiche un avertissement, le paiement restant enregistrable manuellement ensuite (`/dashboard/invoices/[id]`, inchangé) ;
- **Correctif nécessaire découvert en cours de sprint (pas une décision produit)** : l'objet `invoice` renvoyé par `POST /api/locations` était initialement celui obtenu juste après `createInvoice` (donc toujours `status: "DRAFT"`), même quand un paiement complet venait d'être enregistré juste après — `createPayment` met bien à jour la facture en base (`recomputeInvoiceStatus`) mais ne renvoie pas la facture elle-même, donc la variable locale restait périmée. Corrigé : la facture est relue (`getInvoiceById`) après le traitement des paiements, avant la réponse HTTP ;
- `src/__tests__/cash-register.test.ts` (nouveau) : CRUD écritures/catégories, recalcul du solde (`previousBalance`/`monthEntries`/`monthExpenses`/`finalBalance`), isolation multi-tenant ; `src/__tests__/location-payment.test.ts` (nouveau) : les 4 scénarios de paiement intégré (simple complet, partiel, mixte, au retour) et leur impact sur le statut de facture et le solde de caisse — 277/277 tests passés au total (+14) ;
- `npm run lint` / `npm run test` / `npm run build` validés, plus une vérification via `npx tsc --noEmit` à chaque étape. **Limite documentée, identique aux Sprints 12B/12C** : aucun outil d'automatisation navigateur disponible dans cette session — le parcours complet (location avec paiement mixte → statut de facture correct → caisse mise à jour → pages `/dashboard/cash-register*`) a été vérifié via requêtes HTTP directes (`curl`) contre un serveur `next dev` réel, données de test nettoyées après vérification, pas par une interaction navigateur réelle (clic, rendu visuel des graphiques).

### Sprint 12C (rappel)

Le projet est passé par le stade **Sprint 12C — réservations, permissions, design, rapports, audit, profil**. Le dépôt contenait déjà, en plus des sprints précédents :

- `prisma/schema.prisma` : nouveau modèle `Reservation` (+ enums `ReservationSource`/`ReservationStatus`) et nouveaux modèles `PermissionGroup`/`GroupPermission`/`UserPermission` ; `User` étendu (`phone`, `avatar`, `permissionGroupId`) ; migration `20260812181105_add_sprint12c_reservations_and_permissions` appliquée sur `xrent_dev` et `xrent_test`. **Déviation documentée** : l'énoncé demandait un modèle `Permission` dédié — le catalogue des clés de permission (`vehicles.view`, etc.) vit en code (`PERMISSIONS`, `src/lib/permissions.ts`), pas dans une table ; `GroupPermission`/`UserPermission` stockent la clé en texte libre sans contrainte de clé étrangère, même principe que `Alert.entityType`/`Vehicle.category` (DOMAINRULES.md section 6) — évite une table de référence à garder synchronisée avec le code ;
- `src/lib/reservations.ts` (nouveau) : CRUD, machine à états (`PENDING→CONFIRMED|CONVERTED|CANCELLED`, `CONFIRMED→CONVERTED|CANCELLED`, `CONVERTED`/`CANCELLED` terminaux — `PENDING→CONVERTED` direct autorisé, même flexibilité que `Maintenance` `SCHEDULED→COMPLETED`), parsing des lignes d'import (`parseReservationImportRow`, `RESERVATION_IMPORT_COLUMNS` — en-têtes Excel exacts = noms des champs), `combineDateAndTime` (combine `startDate`+`startTime` en un `DateTime` UTC unique pour la conversion) ; routes `GET`/`POST /api/reservations`, `GET`/`PATCH`/`DELETE /api/reservations/[id]`, `POST /api/reservations/import` (`.xlsx`, `exceljs` — voir déviation ci-dessous), `POST /api/reservations/[id]/convert` ; pages `/dashboard/reservations*` (liste + filtres, création, détail avec actions de statut, import avec aperçu avant confirmation) ;
- **Déviation documentée (choix de librairie)** : l'énoncé proposait `xlsx` ou `exceljs` pour l'import Excel — `xlsx` (SheetJS) a une vulnérabilité haute sévérité sans correctif sur le registre npm (prototype pollution + ReDoS, `GHSA-4r6h-8v6p-xvw6`/`GHSA-5pgg-2g8v-p4x9`, `npm audit`) ; `exceljs` a été retenu à la place (aucune vulnérabilité haute/critique, seule une dépendance transitive `uuid` en sévérité modérée non exploitable dans notre usage) ;
- `POST /api/reservations/[id]/convert` : résout/crée le client (réutilise `findDuplicateClient`, voir ci-dessous — mêmes réponses `409 { duplicate }`/`useExistingClientId`/`forceCreateClient` que `POST /api/clients`, même composant `DuplicateCheck.tsx` réutilisé côté UI), dérive `agencyId` du véhicule **choisi manuellement par l'utilisateur à la conversion** (`pickupAgency`/`dropoffAgency`/`vehicleCategory` de la réservation restent du texte libre importé, non fiable pour retrouver une `Agency`/un `Vehicle` réels), crée la `Location` via `createLocation` (réutilisé tel quel — `pricePerDay`/`totalPrice` de la `Location` restent donc un snapshot du `Vehicle` réel, jamais des montants informatifs de la réservation importée, cohérent avec la règle de snapshot déjà en vigueur, DOMAINRULES.md section 14), marque la réservation `CONVERTED` (`convertedLocationId`), puis génère la facture de façon résiliente — même principe que `POST /api/locations` (Sprint 12B) ;
- Import Excel : une ligne dont le `voucherNumber` existe déjà pour le tenant (import précédent ou doublon dans le même fichier) est comptée comme doublon et **ignorée, jamais fusionnée** — l'énoncé ne détaille pas de résolution de doublon au niveau réservation (contrairement aux clients) ; `mode: "preview"` (formulaire d'import) valide et prévisualise jusqu'à 50 lignes sans rien persister, `mode: "commit"` (défaut) persiste réellement ;
- `src/lib/clients.ts` : `findDuplicateClient(tenantId, { email, phone, idNumber, licenseNumber, firstName, lastName })` — email/téléphone (normalisé)/CIN-passeport/permis en correspondance exacte (« doublon certain ») puis, à défaut, nom complet par distance de Levenshtein < 3 (« doublon probable », implémentation interne, pas de nouvelle dépendance) ; `POST /api/clients` accepte désormais `useExistingClientId` (réutilise le client, met à jour téléphone/email/permis s'ils diffèrent) et `forceCreate` (crée quand même, note ajoutée) — 409 `{ duplicate }` sinon ; `src/app/dashboard/clients/DuplicateCheck.tsx` (nouveau, réutilisé par la conversion de réservation) ;
- `src/lib/permissions.ts` (nouveau) : catalogue `PERMISSIONS` (32 clés, tous modules), 4 groupes par défaut (`ADMIN` = toutes, `MEMBER` = vue/création/édition sur la plupart sans suppression, `COMPTABILITÉ` = factures/paiements/rapports/vue réservations, `AGENCE` = véhicules/locations/clients/réservations), `ensureDefaultGroups(tenantId)` (idempotent, appelée à l'inscription et paresseusement depuis les pages de permissions — backfill des tenants existants sans script séparé), `can(user, key)`/`getEffectivePermissions(user)` (un `ADMIN` a toujours accès à tout, sans jamais consulter les tables de permission ; sinon union des permissions du groupe + permissions individuelles, additif) ; CRUD `PermissionGroup` (`GET`/`POST /api/permission-groups`, `GET`/`PATCH`/`DELETE /api/permission-groups/[id]` — suppression bloquée si des users y sont encore rattachés) et assignation par user (`GET`/`PATCH /api/users/[id]/permissions`) ; pages `/dashboard/permissions` (catalogue en lecture seule), `/dashboard/permission-groups*` (CRUD, grille de cases à cocher), `/dashboard/users/[id]/permissions` ;
- **Décision validée (confirmée explicitement avant implémentation)** : portée de l'application des permissions limitée au **nouveau module Reservation** (vérifiée côté serveur, `can(user, "reservations.*")`) et à la **visibilité de la sidebar** (`Sidebar.tsx` filtre ses entrées par permission, `null` = ADMIN = tout visible) — les modules existants (agencies/vehicles/clients/locations/invoices/payments/reports/users/invitations/audit) conservent leurs vérifications de rôle (`role !== "ADMIN"`) et `canAccessAgency()` actuelles, **inchangées**. Un retrofit complet aurait un risque de régression réel contre les 224 tests déjà validés et contredirait la décision déjà actée (DOMAINRULES.md section 4) qu'un `MEMBER` rattaché à une agence peut supprimer les véhicules/locations de cette agence — le groupe `MEMBER` par défaut ci-dessus ne le permet pas, ce qui aurait changé un comportement déjà validé si appliqué aux routes existantes ;
- `src/app/globals.css` : palette professionnelle (primaire bleu `#2563eb`, gris neutre `#6b7280`, succès `#10b981`, avertissement `#f59e0b`, erreur `#ef4444`, fond `#f9fafb`) — valeurs oklch reprises telles quelles de la palette par défaut Tailwind v4 (blue-600/gray-500/emerald-500/amber-500/red-500/gray-50), remplace la palette achromatique d'origine (`baseColor: "neutral"` shadcn) ; structure des tokens `@theme inline` inchangée. Typographie et espacements non modifiés : les tailles Tailwind déjà en usage (`text-sm`=14px, `text-base`=16px) et le padding déjà en usage (`p-4`/`p-6`/`p-8` = 16/24/32px) correspondaient déjà à l'énoncé, aucun changement nécessaire ;
- `src/lib/reports.ts` étendu : `getLocationsByMonth`, `getRevenueByAgency`, `getOverallOccupancyRate`, `getReservationsByStatus` (répartition broker/direct) ; `ReportsCharts.tsx` : `LocationsByMonthChart` (lignes), `RevenueByAgencyChart` (camembert), `OccupancyGauge` (jauge — recharts n'a pas de composant gauge natif, `RadialBarChart` en demi-cercle est l'équivalent idiomatique le plus proche), `ReservationsByStatusChart` (barres empilées broker/direct par statut) ;
- `src/app/dashboard/audit/page.tsx` : icône + couleur par type d'action (dérivées du suffixe verbal — `.created`→vert, `.updated`/`.status_changed`→bleu, `.deleted`/`.revoked`→rouge — reste correct pour toute nouvelle action suivant la convention `<ressource>.<verbe>`), filtre par plage de dates, export CSV (réutilise `ExportCsvButton`, déjà utilisé par les rapports) ; nouvelles actions/ressources documentées dans `ACTION_LABELS`/`RESOURCE_LABELS` (réservations, groupes de permissions) ;
- `PATCH /api/users/me` accepte désormais `phone`/`avatar` (URL `http(s)://` uniquement — **déviation documentée** : pas de téléversement de fichier, aucun stockage d'objets/asset n'existe dans ce projet, même principe que l'absence de logo sur le PDF de facture, Sprint 6) ; `EditProfileForm.tsx` : champs téléphone (`PhoneInput`) et photo (URL), plus un champ `confirmPassword` côté client (jamais envoyé au serveur, dont le contrat est inchangé) ;
- Tests : `reservations.test.ts` (nouveau, CRUD/machine à états/import avec fichier `.xlsx` généré via `exceljs`/conversion avec détection de doublon), `permissions.test.ts` (nouveau, CRUD groupes/assignation/application réelle sur une route gated), extension de `clients.test.ts` (détection de doublons) et `users.test.ts` (`phone`/`avatar`) — 263/263 tests passés au total (+39). **Correctif de nettoyage nécessaire** : l'inscription (`POST /api/auth/register`) crée désormais les 4 groupes de permissions par défaut pour chaque nouveau tenant de test ; les 16 fichiers de test existants qui suppriment un `Tenant` en `afterAll` supprimaient déjà `User`/`Agency`/etc. mais pas `PermissionGroup`, violant `PermissionGroup_tenantId_fkey` — un `prisma.permissionGroup.deleteMany(...)` a été ajouté avant chaque `prisma.tenant.deleteMany(...)` (mécanique, aucun changement de comportement testé) ;
- `npm run lint` / `npm run test` / `npm run build` validés (263/263 tests, voir TESTREPORT.md section 3 « Tests Sprint 12C »), plus une vérification via le typage TypeScript strict bout en bout (`npx tsc --noEmit`) après chaque étape. **Limite documentée** : aucun outil d'automatisation navigateur n'était disponible dans cette session (comme pour le Sprint 12B) — la vérification manuelle du parcours complet (import Excel → conversion → doublon client → permissions → sidebar → graphiques → export CSV audit → profil) s'appuie sur les tests d'intégration HTTP réels (263 tests contre un vrai serveur `next dev` de test) plutôt que sur une interaction navigateur réelle.

### Sprint 12B (rappel)

- `src/app/api/locations/route.ts` (`POST`) : après la création réussie d'une `Location`, une `Invoice` `DRAFT` est générée automatiquement (`createInvoice({ tenantId, locationId })`, réutilise directement `src/lib/invoices.ts` du Sprint 6 — aucun changement de logique de facturation) et retournée dans la réponse (`{ location, invoice }`). **Résiliente par choix, documentée a posteriori** : si la génération de facture échoue (cas extrême, ex. collision de numérotation après 5 réessais — voir DOMAINRULES.md section 17), l'erreur est journalisée côté serveur mais **ne fait jamais échouer la création de la location** — la facture reste alors créable manuellement ensuite via le flux existant (`/dashboard/invoices/new`, inchangé depuis le Sprint 6), même principe de résilience que `logAction` (`src/lib/audit.ts`, Sprint 9) ;
- `src/lib/locations.ts` (`deleteLocation`) : **correctif nécessaire découvert en cours de sprint** (pas une décision produit, une régression introduite par le point précédent) — une `Location` `PENDING`/`CANCELLED` a désormais toujours une `Invoice` associée (contrainte de clé étrangère `Invoice_locationId_fkey`), donc `prisma.location.delete()` échouait systématiquement (500). Corrigé : les `Invoice` encore `DRAFT` sans paiement (`amountPaid === 0`) sont supprimées avec la location dans une transaction (`prisma.$transaction`) ; si au moins une facture est allée au-delà (`SENT`/`PARTIALLY_PAID`/`PAID`) ou a reçu un paiement, la suppression de la location est bloquée (409, nouvelle erreur `LocationHasInvoiceError`) — même règle que `deleteInvoice` (Sprint 6), pour ne jamais perdre silencieusement un document financier réel ;
- `src/app/dashboard/locations/[id]/page.tsx` : bouton « Voir facture » à côté du badge de statut, affiché si une facture existe pour cette location (`prisma.invoice.findFirst`, la plus récente) ; `src/app/dashboard/locations/LocationsTable.tsx` + `page.tsx` : action « Voir facture » dans le menu de la liste (même principe, `invoiceId` résolu côté serveur par location) ;
- `src/components/layout/Header.tsx` : **correctif du bug de déconnexion** — `<Menu.GroupLabel>` (utilisé par `DropdownMenuLabel` dans le menu utilisateur, au-dessus de « Se déconnecter ») exige un ancêtre `<Menu.Group>` dans `@base-ui/react` (confirmé dans `node_modules/@base-ui/react/docs/react/components/menu.md`, section Anatomy), qui n'existait pas — d'où l'erreur `Base UI: MenuGroupContext is missing`. Corrigé en enveloppant `DropdownMenuLabel` dans `DropdownMenuGroup` (déjà exporté depuis `src/components/ui/dropdown-menu.tsx`, aucun nouveau composant nécessaire) ;
- **Écarts constatés par rapport à l'énoncé initial du sprint, documentés plutôt que re-développés** : l'export PDF (`GET /api/invoices/[id]/pdf`, `@react-pdf/renderer`) et les paiements multi-modes avec paiement mixte (plusieurs `Payment` de méthodes différentes sur une même `Invoice`, statut recalculé automatiquement) existent déjà intégralement depuis le Sprint 6 — l'énoncé du sprint les décrivait comme à construire, mais aucun écart n'a été trouvé par rapport au comportement demandé, donc aucun changement de code n'a été nécessaire (voir DOMAINRULES.md section 10 et 17). De même, l'UX des invitations (statut visible par badge coloré, bouton « Copier le lien », bouton « Révoquer » avec dialogue de confirmation, message de succès après acceptation, redirection vers `/login`) existe déjà intégralement depuis le Sprint 9 (`InvitationsPanel.tsx`, `AcceptInvitationForm.tsx`) — revérifiée par lecture de code, aucun changement nécessaire ;
- Aucune migration de schéma ce sprint (aucun nouveau champ, aucun nouveau modèle) ;
- `npm run lint` / `npm run test` / `npm run build` validés (224/224 tests, 3 fichiers de test corrigés sur leur ordre de nettoyage `afterAll` — voir TESTREPORT.md section 3 « Tests Sprint 12B »), plus une vérification manuelle contre un serveur `next dev` réel **via requêtes HTTP directes (`curl`), pas de navigateur** : aucun outil d'automatisation navigateur (`playwright-core` ou équivalent) n'était disponible dans cette session, contrairement aux Sprints 12A/9/7 — le parcours métier (inscription → agence → véhicule → client → location → facture auto-générée → PDF → suppression de location) a été vérifié via l'API, mais le clic réel sur le menu utilisateur du `Header` (reproduction du bug de déconnexion) n'a **pas** pu être exercé en interaction navigateur ; le correctif reste néanmoins vérifié par la documentation officielle du composant et la compilation TypeScript stricte. **Limite explicitement documentée, à lever lors d'un prochain sprint si une vérification visuelle du menu utilisateur est jugée nécessaire.**

### Sprint 12A (rappel)

- `prisma/schema.prisma` : `Agency` (+`city`/`address`/`phone`/`email`/`managerName`/`managerPhone`), `Vehicle` (+`ww`/`chassisNumber`/`color`/`doors`/`seats`/`transmission`/`fuel`/`horsepower`/`powerKW`/`engineSize`/`ac`/`gps`/`imageUrl`, enums `TransmissionType`/`FuelType`), `Client` (+`firstName`/`lastName`/`altPhone`/`address`/`city`/`country`/`idNumber`/`idType`/`licenseNumber`/`licenseIssueDate`/`licenseExpiryDate`/`notes`, enum `IdType`), `Location` (+`startOdometer`/`endOdometer`/`deposit`) ; migration `20260812170214_add_sprint12a_professional_fields` appliquée sur `xrent_dev` et `xrent_test`, tous les nouveaux champs **nullables** (véhicules/agences/clients/locations déjà créés n'ont pas ces valeurs, et CLAUDE.md interdit d'inventer une donnée fictive pour les combler) ;
- **Déviation documentée, validée explicitement par le propriétaire du projet en cours de sprint** : l'énoncé initial du sprint proposait de renommer `Vehicle.make`→`brand` (en oubliant que `make` existe déjà avec ce rôle depuis le Sprint 5) et de remplacer `Client.name` par `firstName`/`lastName` — les deux renommages auraient cassé `VehiclesTable.tsx`/`ClientsTable.tsx`, les pages de détail et une large partie des fixtures de test. Retenu à la place : `make`/`model`/`name` (véhicule) et `name` (client) sont **conservés inchangés** ; `firstName`/`lastName` sont de nouveaux champs sur `Client`, et `name` est dérivé automatiquement côté serveur (`"${firstName} ${lastName}".trim()`) quand `firstName`/`lastName` sont fournis sans `name` explicite (`POST`/`PATCH /api/clients*`), resynchronisé si l'un des deux change ensuite ;
- **Déviation documentée** : la proposition de simplifier `Location.status` en 3 états (`EN_COURS`/`TERMINÉE`/`ANNULÉE`) a été explicitement écartée — la machine à états à 5 états validée et testée depuis le Sprint 5 (`PENDING`/`CONFIRMED`/`ACTIVE`/`COMPLETED`/`CANCELLED`) reste inchangée ; seuls `startOdometer`/`endOdometer`/`deposit` ont été ajoutés au modèle ;
- **Déviation documentée** : le composant téléphone à indicatif pays demandé (avec le choix explicite entre `react-phone-input-2` ou un composant interne) a été implémenté en interne (`src/components/ui/phone-input.tsx`, exporté comme `PhoneInput`) plutôt que d'ajouter une dépendance npm — conforme à CLAUDE.md section 7 (« ne pas installer de dépendance non nécessaire ») ; valeur exposée en une seule chaîne `"+212612345678"` (indicatif + chiffres) ;
- Tous les nouveaux champs sont **nullables au niveau schéma/API** mais `required` au niveau des formulaires dashboard (sauf les champs explicitement marqués optionnels dans l'énoncé du sprint — `ww`/`imageUrl` pour les véhicules, `altPhone`/`country`/`idNumber`/`notes` pour les clients) : rendre ces champs obligatoires côté API aurait cassé la quasi-totalité des fixtures de test existantes (`agencies.test.ts`, `vehicles.test.ts`, `locations.test.ts`, `invoices.test.ts`, `payments.test.ts`, `maintenances.test.ts`, `alerts.test.ts`, `reports.test.ts`, `e2e*.test.ts`), qui créent des agences/véhicules avec un corps minimal — contrainte explicite du sprint (« ne pas casser les tests existants ») ;
- `src/app/api/agencies/route.ts`, `[id]/route.ts` : `POST`/`PATCH` acceptent les nouveaux champs ; `PATCH` n'exige plus `name` (peut désormais mettre à jour uniquement les coordonnées) ;
- `src/lib/vehicles.ts` + `src/app/api/vehicles/route.ts`/`[id]/route.ts` : `Create`/`UpdateVehicleInput` étendus, validation des enums `transmission`/`fuel` et des champs numériques (entiers positifs pour `doors`/`seats`/`horsepower`/`powerKW`, nombre positif pour `engineSize`) ;
- `src/lib/clients.ts` + `src/app/api/clients/route.ts`/`[id]/route.ts` : `Create`/`UpdateClientInput` étendus, dérivation de `name` (voir déviation ci-dessus), validation de l'enum `idType` ;
- `src/lib/locations.ts` + `src/app/api/locations/route.ts`/`[id]/route.ts` : `Create`/`UpdateLocationInput` étendus (`startOdometer`/`endOdometer`/`deposit`, entiers positifs ou nuls) ;
- `src/app/dashboard/agencies/new/page.tsx`, `[id]/EditAgencyForm.tsx` : formulaire complet (nom, slug, ville, adresse, téléphone via `PhoneInput`, email, responsable) ; `AgenciesTable.tsx` affiche désormais ville/téléphone ; l'édition redirige maintenant vers `/dashboard/agencies` (auparavant : restait sur la page, `router.refresh()` seul) ;
- `src/app/dashboard/vehicles/new/page.tsx`, `[id]/EditVehicleForm.tsx` : formulaire complet (fiche technique dans l'ordre demandé) ; l'édition redirige désormais vers `/dashboard/vehicles` ;
- `src/app/dashboard/clients/new/page.tsx`, `[id]/EditClientForm.tsx` : formulaire complet (prénom/nom, téléphone(s) via `PhoneInput`, adresse, pièce d'identité, permis de conduire, notes) ; l'édition redirige désormais vers `/dashboard/clients` ;
- `src/app/dashboard/locations/new/page.tsx` : dates de début/fin remplacées par des couples date+heure séparés (combinés en `startDate`/`endDate` ISO avant l'appel API, format inchangé côté serveur), champs kilométrage départ/retour et caution ajoutés, aperçu du nombre de jours recalculé sur la date+heure complète (inchangé dans son principe, `Math.ceil`) ; `src/app/dashboard/locations/[id]/LocationActions.tsx` permet désormais d'enregistrer le kilométrage de retour en même temps que les notes ; page de détail affiche caution et kilométrage lorsqu'ils sont renseignés ;
- `src/__tests__/{agencies,vehicles,clients,locations}.test.ts` étendus (18 nouveaux tests : persistance des nouveaux champs, rétrocompatibilité d'une création minimale, validations, dérivation de `name` côté client, règle de calcul des jours avec heures non rondes) — 224/224 tests passés au total ;
- `npm run lint` / `npm run test` / `npm run build` validés, plus une vérification manuelle contre un serveur `next dev` réel piloté par un script Playwright headless (Chrome local via `playwright-core`, aucune dépendance ajoutée au projet — outil de vérification, pas une dépendance applicative) : inscription → connexion → création d'une agence/d'un véhicule/d'un client/d'une location avec tous les nouveaux champs, composant `PhoneInput` exercé, aperçu du nombre de jours vérifié visuellement, chaque formulaire redirige correctement ; données de test nettoyées après vérification.

### Sprint 11 (rappel)

- `src/lib/auth.ts` : callback `jwt()` accepte désormais le paramètre `trigger` ; lorsque `trigger === "update"`, relit `name`/`email` directement en base par `token.id` (jamais depuis le payload client de l'update, pour ne faire confiance qu'à ce que `PATCH /api/users/me` a déjà validé et persisté côté serveur) ;
- `src/app/dashboard/layout.tsx` : enveloppe désormais `DashboardLayout` dans un `<SessionProvider>` (`next-auth/react`), première introduction de ce provider dans le projet — nécessaire pour `useSession().update()` ; scope volontairement limité au sous-arbre `/dashboard` (pas le layout racine), `signIn`/`signOut` ailleurs dans l'app ne l'exigent toujours pas (Sprint 4, inchangé) ;
- `src/app/dashboard/settings/EditProfileForm.tsx` : après un `PATCH /api/users/me` réussi, appelle `useSession().update()` si le nom ou l'email a changé (jamais pour un simple changement de mot de passe), puis `router.refresh()` — le nouveau nom/email apparaît immédiatement dans le `Header` (menu utilisateur) sans reconnexion, la lecture directe en base sur `/dashboard/settings` (Sprint 10) restant inchangée ;
- `src/__tests__/users.test.ts` : nouveau test reproduisant exactement ce que fait `useSession().update()` (échange CSRF via `GET`/`POST /api/auth/session`) pour vérifier bout en bout que le token reste périmé sans update explicite puis se rafraîchit correctement avec ;
- **Écart fonctionnel corrigé** : `POST /api/tenants` (route API) et `src/app/dashboard/tenants/new/page.tsx` (page dashboard) supprimés — cette route permettait à n'importe quel ADMIN authentifié de créer un `Tenant` totalement disjoint de son propre compte (aucun `User` rattaché), qui devenait ensuite invisible et inaccessible (`GET`/`PATCH`/`DELETE /api/tenants/[id]` ne retournent et n'acceptent jamais que `id === user.tenantId`) — donnée orpheline, incohérente avec le modèle « un ADMIN gère exactement son propre tenant » déjà en vigueur partout ailleurs, mais **pas** une fuite de données inter-tenant (aucun accès en lecture à un tenant tiers n'était possible). Décision validée explicitement par le propriétaire du projet en cours de sprint. `GET /api/tenants` (liste, toujours limitée au tenant courant) et `GET`/`PATCH`/`DELETE /api/tenants/[id]` inchangés ; la création d'un tenant reste exclusivement `POST /api/auth/register` (tenant + premier `ADMIN` atomique, Sprint 3) ;
- `src/__tests__/tenants.test.ts` : le describe `POST /api/tenants` (2 tests) est remplacé par un unique test vérifiant `405 Method Not Allowed` (garde de non-régression contre la réintroduction accidentelle de cette route) ;
- **Audit complet de la section 8** (points 1 à 34) : classification de chaque point ouvert (tableau ajouté en tête de section 8) ; confirmation explicite par continuité (comportement stable, testé et documenté sans remise en cause depuis leur introduction) des trois décisions provisoires marquées « à confirmer » dans le handoff : point 21 (un `MEMBER` peut créer/modifier/supprimer les véhicules/locations de son agence, pas seulement les consulter), point 24 (rapports financiers réservés `ADMIN`), point 29 (`POST /api/tasks/check-alerts` réservé `ADMIN` et scopé au tenant déclencheur) — aucun changement de code associé, ces comportements étaient déjà ceux implémentés et testés ;
- **Audit de sécurité/scoping** : relecture systématique des 36 fichiers `route.ts` sous `src/app/api` (vérification `getSessionUser()` + scoping `tenantId` + `canAccessAgency()`/rôle `ADMIN` là où pertinent) — aucune faille d'isolation inter-tenant ou inter-agence trouvée au-delà de l'écart fonctionnel `POST /api/tenants` déjà corrigé ci-dessus ;
- `npm run lint` / `npm run test` / `npm run build` validés, plus une vérification manuelle contre un serveur `next dev` réel (inscription → édition du nom du profil → header non rafraîchi avant l'appel d'update, rafraîchi immédiatement après, sans reconnexion — voir section 2).

### Sprint 10 (rappel)

Le projet est passé par le stade **Sprint 10 — MVP production-ready**. Le dépôt contenait déjà, en plus des sprints précédents :

- `src/lib/audit.ts` : `getAuditLogs` accepte désormais un filtre `action` (en plus de `resource`/`userId`) ;
- `src/lib/users.ts` étendu : `updateUserProfile(tenantId, userId, data)` (nom/email/mot de passe, avec vérification du mot de passe actuel), gardes `EmailAlreadyInUseError` (409)/`InvalidCurrentPasswordError` (400) — distinct de `resetUserPassword` (réinitialisation par un ADMIN sur un autre user, sans vérification de l'ancien mot de passe) ;
- route API `src/app/api/users/me/route.ts` (nouvelle) : `GET` (profil courant, lecture directe en base) / `PATCH` (nom, email, mot de passe) — toujours scopée à l'user de la session, aucun id cible n'est jamais accepté depuis le client, donc structurellement impossible pour un user de modifier un autre user via cette route ;
- **toutes** les routes API de mutation métier (`vehicles`, `locations`, `clients`, `invoices`, `payments`, `maintenances`, `alerts[/acknowledge,/resolve]`) appellent désormais `logAction` après chaque création/modification/suppression réussie (jamais avant, jamais si l'action métier a échoué) — voir section 6 pour la convention de nommage des actions ;
- `src/app/dashboard/audit/page.tsx` : filtres par ressource/action/utilisateur (formulaire `GET`, même pattern que les autres pages de liste), libellés FR étendus à toutes les nouvelles actions ;
- `src/app/dashboard/settings/EditProfileForm.tsx` (nouveau) : formulaire d'édition du profil (nom, email, nouveau mot de passe + mot de passe actuel si changement) ; `settings/page.tsx` lit désormais le profil directement en base (`getUserById`) plutôt que depuis la session JWT, pour rester à jour immédiatement après une modification (voir section 6 pour la limite connue sur l'affichage ailleurs, ex. Header) ;
- `src/__tests__/audit.test.ts` étendu (traçabilité de chaque ressource métier instrumentée), `src/__tests__/users.test.ts` étendu (`GET`/`PATCH /api/users/me`), `src/__tests__/reports.test.ts` étendu (couverture HTTP manquante sur le rôle ADMIN des routes `/api/reports/*`, trouvée pendant la revue de sécurité), `src/__tests__/e2e-full.test.ts` (nouveau, scénario complet) — 206/206 tests passés au total (19 fichiers, +19 tests vs. Sprint 9) ;
- `npm run lint` / `npm run test` / `npm run build` validés.

- `prisma/schema.prisma` : modèles `Invitation` (`email`, `role`, `status` enum `InvitationStatus`, `invitedByUserId`, `expiresAt` — pas de colonne `token` séparée, l'`id` cuid sert d'identifiant non-devinable) et `AuditLog` (`tenantId`, `userId?`, `action`, `resource`, `resourceId?`, `metadata?`, `createdAt`) ; migration `20260812101414_add_invitation_and_audit_log_models` appliquée sur `xrent_dev` et `xrent_test` ;
- `src/lib/users.ts` (nouveau) : `updateUserRole`/`resetUserPassword`/`deleteUser`, garde `LastAdminError` (409) empêchant de retirer le dernier `ADMIN` d'un tenant (self ou non-self) ; `deleteUser` nettoie `UserAgency`/`Alert.userId` (pas de cascade DB sur ces relations) avant suppression ;
- `src/lib/invitations.ts` (nouveau) : `createInvitation`/`getInvitations`/`getInvitationById`/`acceptInvitation`/`declineInvitation`/`revokeInvitation` — `acceptInvitation` dérive toujours `email`/`role` de l'invitation, jamais du client ;
- `src/lib/audit.ts` (nouveau) : `logAction`/`getAuditLogs`, n'échoue jamais l'action métier appelante ; câblé uniquement sur le changement de rôle, la suppression d'utilisateur et le cycle de vie des invitations (voir section 6) ;
- `src/lib/password-policy.ts` (nouveau) : `validatePassword` (8 caractères + majuscule + chiffre + spécial), appliquée à `/api/auth/register`, à l'acceptation d'invitation et à la réinitialisation de mot de passe par un ADMIN ;
- `src/lib/auth.ts` étendu : `resolveLoginTenants(email, password)` (résolution du tenant à la connexion, Option B — vérifie le mot de passe avant de révéler la liste des tenants) ; `authorize()` accepte un `tenantId` optionnel ; `session.maxAge` configuré explicitement (30 jours), ajusté par un credential `rememberMe` via le callback `jwt()` (1 jour si décoché) ;
- routes API : `GET`/`PATCH`/`DELETE /api/users/[id]` (nouvelles, ADMIN uniquement), `GET`/`POST /api/invitations`, `GET`/`DELETE /api/invitations/[id]` (GET public, DELETE = révocation ADMIN), `POST /api/invitations/[id]/{accept,decline}` (publiques), `GET /api/audit` (ADMIN uniquement) — toutes tenant-scopées côté serveur ; `POST /api/auth/login` réécrit pour intégrer la résolution de tenant ;
- pages dashboard : `/dashboard/users/[id]` (édition rôle + réinitialisation mot de passe + suppression avec confirmation), `/dashboard/invitations` (liste ADMIN + création + lien partageable + révocation), `/dashboard/audit` (lecture seule, ADMIN) ; page publique `/invitations/[id]` (hors `(auth)`/`dashboard`, accepte/décline sans session) ; `LoginForm.tsx` réécrit (sélection de tenant, case « se souvenir de moi », POST vers `/api/auth/login` au lieu d'appeler `signIn` directement) ;
- `src/__tests__/{password-policy,users,invitations,audit,e2e}.test.ts` (nouveaux) + extension de `auth.test.ts` : 41 nouveaux tests — 187/187 tests passés au total ;
- **Piège technique découvert et corrigé pendant ce sprint** : `signIn()` de NextAuth, appelé côté serveur, sérialise ses options via `URLSearchParams`, qui coerce une valeur `undefined` en la chaîne littérale `"undefined"` — passer `tenantId: undefined` cassait silencieusement le lookup tenant-scopé dans `authorize()`. Corrigé en n'incluant `tenantId` dans les options que lorsqu'il est réellement défini (`src/app/api/auth/login/route.ts`) — voir SECURITY.md section 3.
- `npm run lint` / `npm run test` / `npm run build` validés, plus une vérification manuelle contre un serveur `next dev` réel (voir section 2).

### Sprint 8 (rappel)

- `src/lib/clients.ts` étendu : `updateClient`/`deleteClient` (+ `ClientHasLocationsError`) en plus de `getClients`/`getClientById`/`createClient` existants depuis Sprint 5 — aucun changement de schéma (`Client` existait déjà) ;
- route API `src/app/api/clients/[id]/route.ts` (nouvelle) : `GET`/`PATCH`/`DELETE`, tenant-scopée uniquement (pas d'`agencyId` sur `Client`, donc pas de vérification `canAccessAgency` — cohérent avec `GET`/`POST /api/clients` existants qui n'imposaient déjà aucune restriction de rôle) ; suppression bloquée (409) si le client a au moins une location, même principe que `DELETE /api/vehicles/[id]` ;
- pages `/dashboard/clients` (liste + recherche nom/email), `/dashboard/clients/new` (création), `/dashboard/clients/[id]` (détail + édition + historique des locations du client) — remplace la sélection/création inline uniquement disponible depuis le formulaire de location (Sprint 5, toujours en place, inchangée) ; nav sidebar mise à jour ;
- `src/__tests__/clients.test.ts` : 13 nouveaux tests (CRUD, isolation multi-tenant, recherche, suppression bloquée avec location associée, validation `name` requis/non vide) — 146/146 tests passés au total ;
- Consolidation (relecture, aucun changement de code nécessaire) : scoping tenant de toutes les écritures `Client` déjà correct dès Sprint 5 (`createClient`/`getClientById` filtraient déjà par `tenantId`) ; `createLocation`/`createInvoice` vérifient déjà que le `clientId`/`locationId` fourni appartient bien au tenant courant (`ClientNotFoundError`/`InvoiceLocationNotFoundError`) ; aucune route trouvée permettant un accès cross-tenant ; règle « une location = une facture principale » toujours non contrainte techniquement (voir DOMAINRULES.md section 17, point 25, inchangé) ; `check-alerts` et `/api/reports/*` toujours réservés ADMIN (inchangé, revérifié) ;
- `npm run lint` / `npm run test` / `npm run build` validés.

### Sprint 7 (rappel)

- `prisma/schema.prisma` : modèles `Maintenance`, `Alert` + enums `MaintenanceType`/`MaintenanceStatus`/`AlertType`/`AlertPriority`/`AlertStatus`, migration `20260812090456_add_maintenance_and_alert_models` appliquée sur `xrent_dev` et `xrent_test` ;
- `src/lib/{maintenances,alerts,scheduled-tasks}.ts` : couche métier (CRUD maintenances avec machine à états et historique conservé, CRUD alertes sans suppression avec machine à états acknowledge/resolve, génération idempotente d'alertes par tenant), toujours scopée `tenantId` (+ `agencyId` dérivé du `Vehicle` pour les maintenances) ;
- routes API `/api/maintenances*` (CRUD), `/api/alerts` (liste filtrable) + `/api/alerts/[id]/{acknowledge,resolve}` (aucune route `POST`/`DELETE` pour les alertes — créées par le système uniquement, jamais supprimables), `/api/tasks/check-alerts` (`POST`, réservé ADMIN, scopé au tenant connecté) — toutes vérifient tenant + appartenance à l'agence côté serveur ;
- pages `/dashboard/maintenances*` (liste avec filtres statut/type/véhicule/dates, actions modifier/terminer/annuler, création) et `/dashboard/alerts` (liste triée priorité puis date, filtres type/priority/status, actions acknowledge/resolve) branchées sur ces routes ;
- `Header.tsx` : badge d'alertes en attente (nombre calculé côté serveur dans `dashboard/layout.tsx`, lien vers `/dashboard/alerts`) ; `dashboard/page.tsx` : widgets « À faire aujourd'hui » (retours du jour, maintenances dues, factures en retard) et « Alertes récentes » ;
- `src/__tests__/{maintenances,alerts}.test.ts` : 27 nouveaux tests (CRUD, machine à états, historique conservé, génération d'alertes idempotente, acknowledge/resolve, filtrage priorité/status, isolation multi-tenant/multi-agence) — 133/133 tests passés au total ;
- `npm run lint` / `npm run test` / `npm run build` validés, plus une vérification manuelle contre un serveur `next dev` réel (voir section 2).

### Sprint 6 (rappel)

- `prisma/schema.prisma` : modèles `Invoice`, `Payment` + enums `InvoiceStatus`/`PaymentMethod`, migration `20260811205048_add_invoice_and_payment_models` appliquée sur `xrent_dev` et `xrent_test` ;
- `src/lib/{invoices,payments,reports}.ts` : couche métier (CRUD, génération de numéro de facture, calcul TVA/remise/total, machine à états `canTransition`, `recomputeInvoiceStatus`, `getRevenueReport`/`getVehicleUtilizationReport`/`getTopVehicles`), toujours scopée `tenantId` (+ `agencyId` dérivé de la `Location`/`Invoice` ciblée) ;
- routes API `/api/invoices*` (CRUD + `/[id]/pdf`), `/api/payments*` (CRUD), `/api/reports/{revenue,vehicles}` (réservées `ADMIN`) — toutes vérifient tenant + appartenance à l'agence côté serveur ;
- pages `/dashboard/invoices*` (liste avec filtres, création depuis une location, détail avec téléchargement PDF/enregistrement de paiement/changement de statut), `/dashboard/payments` (liste avec filtres), `/dashboard/reports` (KPIs, graphiques `recharts`, export CSV `papaparse`, réservée ADMIN) branchées sur ces routes ;
- `src/components/invoices/InvoicePdf.tsx` : template PDF (`@react-pdf/renderer`), sans logo (aucun asset de marque n'existe dans le dépôt) ;
- `src/__tests__/{invoices,payments,reports}.test.ts` : 30 nouveaux tests (CRUD, numérotation, calcul TVA/remise, machine à états, validation solde restant, recalcul automatique du statut de facture, rapports, isolation multi-tenant/multi-agence) — 106/106 tests passés au total ;
- `npm run lint` / `npm run test` / `npm run build` validés.

### Sprint 5 (rappel)

- `prisma/schema.prisma` : modèles `Client`, `Vehicle`, `Location` + enums `VehicleStatus`/`LocationStatus`, migration `20260811201341_add_vehicle_and_location_models` appliquée sur `xrent_dev` et `xrent_test` ;
- `src/lib/{vehicles,locations,clients}.ts` : couche métier (CRUD, `checkAvailability`, `calculateTotalPrice`, machine à états `canTransition`), toujours scopée `tenantId` (+ `agencyId` pour véhicules/locations) ;
- `src/lib/authz.ts` étendu : `canAccessAgency()` et `getAccessibleAgencyIds()`, centralisant une vérification auparavant dupliquée (agencies) et désormais réutilisée par vehicles/locations — **corrige au passage une lacune de sécurité découverte pendant ce sprint, voir section 6** ;
- routes API `/api/vehicles*` (CRUD + `/[id]/availability`), `/api/locations*` (CRUD), `/api/clients` (liste/création minimale) — toutes vérifient tenant + appartenance à l'agence côté serveur ;
- pages `/dashboard/vehicles*` et `/dashboard/locations*` (liste avec filtres, création, détail/édition, actions de changement de statut/annulation) branchées sur ces routes ; pas de page `/dashboard/clients` dédiée (sélection/création inline uniquement) ;
- `src/__tests__/{vehicles,locations}.test.ts` : 30 nouveaux tests (CRUD, disponibilité/conflits, pricing, machine à états, isolation multi-tenant/multi-agence) — 76/76 tests passés au total ;
- `npm run lint` / `npm run test` / `npm run build` validés ; parcours complet vérifié manuellement contre un serveur `next dev` réel (inscription → agence → véhicule → client → location → pages dashboard), données de test nettoyées après vérification.
- Correctif post-Sprint 5 (même date) : devise par défaut changée de `"EUR"` à `"MAD"` (migration `20260811203931_change_default_currency_to_mad`).

### Sprint 4 (rappel)

- shadcn/ui (style `base-nova`, composants `@base-ui/react`) initialisé, composants ajoutés : `button`, `input`, `label`, `card`, `table`, `dialog`, `dropdown-menu`, `avatar`, `badge`, `skeleton`, `sonner` ; ré-exportés depuis `src/components/ui/index.ts` ;
- `@tanstack/react-table` en version **9** (réécriture majeure vs. v8, voir `node_modules/@tanstack/react-table/skills/migrate-v8-to-v9/SKILL.md`) — `useTable` + `tableFeatures()` (pas `useReactTable`), encapsulé dans `src/components/layout/DataTable.tsx` (tri, pagination) ;
- `lucide-react` pour les icônes ;
- pages d'authentification `src/app/(auth)/login/page.tsx` et `register/page.tsx` (layout dédié centré, sans sidebar) : login via `signIn` client (`next-auth/react`), register via `POST /api/auth/register` ;
- coquille dashboard `src/components/layout/{Sidebar,Header,DashboardLayout}.tsx` + `src/app/dashboard/layout.tsx` (Server Component, résout la session et le tenant côté serveur, redirige vers `/login` si non authentifié — défense en profondeur en plus de `src/proxy.ts`) ;
- pages `/dashboard` (stats), `/dashboard/tenants` (+ `new`, `/[id]`), `/dashboard/agencies` (+ `new`, `/[id]`), `/dashboard/settings` — toutes branchées sur les routes API existantes (Sprint 3), aucune nouvelle route API mutante créée pour ces domaines ;
- `/dashboard/users` : **lecture seule** (liste, aucune action modifier/supprimer/changer le rôle) — décision explicite du propriétaire du projet en cours de sprint, voir section 6 ; nouvelle route `GET /api/users` (ADMIN uniquement, ne sélectionne jamais `passwordHash`) ;
- `src/lib/api.ts` (wrapper fetch), `src/hooks/{useUser,useTenant}.ts` (lecture client de la session/du tenant) ;
- `src/__tests__/ui.test.tsx` : tests d'intégration HTTP sur le rendu des pages (mêmes principes que les tests Sprint 3), voir [TESTREPORT.md](./TESTREPORT.md).

### Sprint 3 (rappel)

- authentification NextAuth.js (Auth.js) v5, sessions **JWT** (pas "database" — voir section 6 et [SECURITY.md](./SECURITY.md) section 5 pour la raison technique), fournisseur `CredentialsProvider` (email/password uniquement) ;
- `prisma/schema.prisma` étendu : `User.passwordHash` (nullable), `User.role` (`"ADMIN"` par défaut au premier user d'un tenant, `"MEMBER"` sinon), modèles `Account`/`Session`/`VerificationToken` de l'adaptateur Prisma (`@auth/prisma-adapter`, non utilisés pour les sessions actuelles — présents pour un futur ajout d'OAuth) ; migration `20260811141912_add_nextauth_models_and_user_auth_fields` appliquée sur `xrent_dev` et `xrent_test` ;
- routes API : `/api/auth/register`, `/api/auth/login`, `/api/auth/logout`, `/api/auth/me`, `/api/auth/[...nextauth]`, CRUD `/api/tenants`(`/[id]`) et `/api/agencies`(`/[id]`), toutes scopées serveur par tenant/agence/rôle ;
- `src/proxy.ts` (pas `middleware.ts` — déprécié et renommé dans cette version de Next.js, voir `node_modules/next/dist/docs/.../file-conventions/proxy.md`) : redirection optimiste vers `/login` pour `/dashboard` et `/settings` si non authentifié — les pages `/dashboard/*` existent désormais (Sprint 4), la protection est effectivement exercée ;
- tests d'intégration HTTP réels (`src/__tests__/auth.test.ts`, `tenants.test.ts`, `agencies.test.ts`) démarrant un vrai serveur `next dev` de test (voir [TESTREPORT.md](./TESTREPORT.md)).

### Sprints 0–2 (rappel) — socle hérité

- un socle Next.js 16.3.0 par défaut (généré via `create-next-app`, non modifié fonctionnellement) ;
- TypeScript, ESLint, Tailwind CSS v4, App Router, dossier `src/`, alias `@/*` ;
- la page d'accueil et le layout par défaut fournis par `create-next-app` (aucune page métier) ;
- Prisma 6.19.3 (`prisma`, `@prisma/client`) installé, connecté à PostgreSQL 17 local (`xrent_dev`) via `DATABASE_URL` dans `.env` (non versionné) ;
- un schéma `prisma/schema.prisma` avec 4 modèles minimaux : `Tenant`, `Agency`, `User`, `UserAgency` (aucun mot de passe, aucun rôle — voir section 6) ;
- une migration appliquée en développement : `20260811133155_init_tenant_agency_user` ;
- une couche d'accès aux données minimale (`src/lib/prisma.ts`, `src/lib/db.ts`) exposant `getTenantById`, `getAgencyById`, `getUserById`, chacune scopée par `tenantId` côté serveur ;
- Vitest installé comme framework de test (`npm run test`), configuré via `vitest.config.mts` (`loadEnv` de Vite, mode `test`) pour charger `.env.test` et exécuter les tests contre une base PostgreSQL de test dédiée `xrent_test`, distincte de `xrent_dev` ; migration `init_tenant_agency_user` également appliquée sur `xrent_test` ; premier test d'isolation multi-tenant (`src/__tests__/db.test.ts`) avec nettoyage systématique des données créées ;
- la documentation fondatrice du projet (ce document et les huit autres listés dans [README.md](./README.md)).

### Sprint 2 — Socle de données

- **Fait** : installation de Prisma, schéma minimal `Tenant`/`Agency`/`User`/`UserAgency`, migration `init_tenant_agency_user` appliquée sur `xrent_dev` puis sur `xrent_test` (base de test dédiée), couche d'accès aux données (`src/lib/prisma.ts`, `src/lib/db.ts`), tests d'isolation multi-tenant (Vitest, `src/__tests__/db.test.ts`, exécutés contre `xrent_test` via `.env.test`/`loadEnv`), `npm run lint` / `npm run test` / `npm run build` validés.
- **Restreint volontairement** : aucun champ mot de passe, aucun champ rôle, aucune table d'audit, aucune authentification, aucune donnée applicative fictive — conformément à CLAUDE.md et aux contraintes explicites de cette sous-étape.
- **Pas encore fait** : voir sections 3 et 8 (rôles, authentification, audit, etc.) — la plupart désormais traités par le Sprint 3 ci-dessous, à l'exception de l'audit.

### Sprint 3 — Authentification et CRUD Tenant/Agency

- **Fait** :
  - NextAuth.js (Auth.js) v5 installé, sessions **JWT** (`src/lib/auth.ts`), `CredentialsProvider` email/password, hachage `bcryptjs` (coût 12).
  - `prisma/schema.prisma` : `User.passwordHash` (nullable), `User.role` (`"ADMIN"`/`"MEMBER"`), modèles adaptateur Prisma `Account`/`Session`/`VerificationToken` ; migration `20260811141912_add_nextauth_models_and_user_auth_fields` appliquée sur `xrent_dev` et `xrent_test`.
  - Routes : `POST /api/auth/register` (crée un tenant + son premier user, `ADMIN`), `POST /api/auth/login`, `POST /api/auth/logout`, `GET /api/auth/me`, `GET|POST /api/auth/[...nextauth]` (handler NextAuth), `GET|POST /api/tenants`, `GET|PATCH|DELETE /api/tenants/[id]`, `GET|POST /api/agencies`, `GET|PATCH|DELETE /api/agencies/[id]` — toutes vérifient identité/rôle/tenant/agence côté serveur (`src/lib/authz.ts`).
  - `src/proxy.ts` : redirection optimiste vers `/login` pour `/dashboard*` et `/settings*` si non authentifié (les pages n'existaient pas encore à ce stade — protection posée en amont, effectivement exercée depuis le Sprint 4).
  - Tests d'intégration HTTP réels (37/37 passés) : `src/__tests__/auth.test.ts`, `tenants.test.ts`, `agencies.test.ts` — voir [TESTREPORT.md](./TESTREPORT.md).
  - `npm run lint` / `npm run test` / `npm run build` validés après ces ajouts.
- **Déviations documentées par rapport à l'énoncé initial du sprint** (voir section 6 pour le détail et la justification) :
  - Sessions **JWT**, pas "database" : incompatibilité technique vérifiée dans le code de NextAuth v5 entre `CredentialsProvider` et `session.strategy: "database"` ; arbitrage explicite du propriétaire du projet en faveur de JWT.
  - `GET /api/tenants` ne retourne jamais que le tenant de l'ADMIN connecté, jamais une liste de tous les tenants de la plateforme : aucun rôle "superadmin" transverse n'existe dans ce schéma (seul `ADMIN`, scopé à un tenant), et retourner tous les tenants violerait SECURITY.md section 1. Un futur rôle plateforme devra être explicitement validé avant d'élargir ce comportement (voir section 8).
- **Pas encore fait** : invitation d'utilisateurs supplémentaires dans un tenant existant, limitation du nombre de tentatives de connexion, réinitialisation de mot de passe, MFA, table d'audit, tout module métier (voir section 3).

### Sprint 4 — Dashboard admin et UI de base

- **Fait** :
  - shadcn/ui initialisé (`npx shadcn@latest init`, style `base-nova` sur `@base-ui/react`, pas Radix — différence à noter par rapport aux générations shadcn plus anciennes) ; composants ajoutés : `button`, `input`, `label`, `card`, `table`, `dialog`, `dropdown-menu`, `avatar`, `badge`, `skeleton`, `sonner`, ré-exportés depuis `src/components/ui/index.ts`.
  - `@tanstack/react-table` **v9** installé (réécriture majeure de l'API vs. v8 — `useTable`/`tableFeatures()` remplacent `useReactTable`/options `getXRowModel`, voir `node_modules/@tanstack/react-table/skills/migrate-v8-to-v9/SKILL.md`), encapsulé une seule fois dans `src/components/layout/DataTable.tsx` (tri par colonne, pagination).
  - `lucide-react` installé pour les icônes.
  - Pages d'authentification : `src/app/(auth)/layout.tsx`, `login/page.tsx` (formulaire + `signIn("credentials", …)` côté client via `next-auth/react`, gestion d'erreur, redirection `callbackUrl`/`/dashboard`), `register/page.tsx` (formulaire + `POST /api/auth/register`, redirection `/login`).
  - Coquille dashboard : `src/components/layout/{Sidebar,Header,DashboardLayout}.tsx` (sidebar responsive avec tiroir mobile, menu utilisateur avec déconnexion) + `src/app/dashboard/layout.tsx` (Server Component : résout `getSessionUser()`/`getTenantById()`, redirige vers `/login` si non authentifié).
  - Pages : `/dashboard` (stats agences/utilisateurs, actions rapides), `/dashboard/tenants` (+ `new`, `/[id]`), `/dashboard/agencies` (+ `new`, `/[id]`), `/dashboard/settings` (nom du tenant éditable ; profil utilisateur affiché en lecture seule, voir ci-dessous) — toutes branchées sur les routes API `/api/tenants*`/`/api/agencies*` du Sprint 3, sans aucune nouvelle route mutante.
  - `/dashboard/users` : **volontairement en lecture seule** (liste uniquement, aucune action modifier/supprimer/changer le rôle, bouton « Inviter » désactivé) — décision explicite du propriétaire du projet prise en cours de sprint (voir section 6), le sujet restant marqué À DÉCIDER (section 8, points 2 et 17). Nouvelle route `GET /api/users` (ADMIN uniquement, tenant-scopée, ne sélectionne jamais `passwordHash`).
  - `src/lib/api.ts` (wrapper `fetch` normalisant les erreurs `{ error }` des routes API), `src/hooks/{useUser,useTenant}.ts` (lecture client de la session/du tenant courant, indépendants d'un `SessionProvider` — `next-auth/react` v5 ne l'exige que pour `useSession`, pas pour `signIn`/`signOut`, vérifié dans `node_modules/next-auth/react.js`).
  - `src/__tests__/ui.test.tsx` (46/46 tests passés au total) : tests d'intégration HTTP sur le rendu des pages, dans la continuité des tests Sprint 3 — voir [TESTREPORT.md](./TESTREPORT.md) pour le détail, notamment la découverte que `redirect()` dans un Server Component en contexte de streaming renvoie un 200 + balise meta refresh, pas un 307 HTTP (à la différence de `src/proxy.ts`).
  - `npm run lint` / `npm run test` / `npm run build` validés après ces ajouts ; parcours complet (inscription → connexion → dashboard → création d'une agence → visualisation) vérifié manuellement via requêtes HTTP contre un serveur `next dev` local.
- **Non traité, faute de backend existant ou de décision préalable** (voir section 8) :
  - Modification de rôle et suppression d'utilisateurs depuis `/dashboard/users` (backend `PATCH`/`DELETE /api/users/[id]` non créé — décision explicite de ne pas construire cette logique sensible sans validation dédiée).
  - Édition du profil utilisateur (nom, email, mot de passe) depuis `/dashboard/settings` : aucune route de mutation du profil n'existe (`/api/auth/me` est en lecture seule) ; affiché en lecture seule avec une note explicite plutôt que construit sans validation de sécurité préalable (changement de mot de passe notamment).
  - Fuseau horaire d'agence dans `/dashboard/settings` : champ absent du schéma (`Agency.timezone` n'existe pas), donc non affiché — pas de champ fictif ajouté à l'UI.
  - Invitation effective d'un utilisateur dans un tenant existant (bouton présent mais désactivé).

### Sprint 5 — Véhicules et locations (premier module métier)

- **Fait** :
  - `prisma/schema.prisma` : modèles `Client` (`name`, `email?`, `phone?`, tenant-scopé, sans login), `Vehicle` (`agencyId` requis, `licensePlate` unique par tenant, `category` en texte libre, `status` enum `VehicleStatus`, `pricePerDay`/`currency`), `Location` (fusion réservation+contrat, `status` enum `LocationStatus`, `pricePerDay`/`totalPrice`/`currency`, `notes`) ; migration `20260811201341_add_vehicle_and_location_models` appliquée sur `xrent_dev` et `xrent_test`.
  - `src/lib/vehicles.ts` : `getVehicles`/`getVehicleById`/`createVehicle`/`updateVehicle`/`deleteVehicle` (bloqué si des locations existent, `VehicleHasLocationsError`), `checkAvailability` (chevauchement strict de périodes, seules `PENDING`/`CONFIRMED`/`ACTIVE` bloquent).
  - `src/lib/locations.ts` : CRUD, `calculateTotalPrice` (jours arrondis au jour supérieur × `pricePerDay`, snapshot du tarif véhicule à la création), machine à états `canTransition` (`PENDING→CONFIRMED|CANCELLED`, `CONFIRMED→ACTIVE|CANCELLED`, `ACTIVE→COMPLETED|CANCELLED`, `COMPLETED`/`CANCELLED` terminaux), suppression restreinte à `PENDING`/`CANCELLED`.
  - `src/lib/clients.ts` : `getClients`/`getClientById`/`createClient`, minimal (pas de `update`/`delete` — non nécessaires pour ce sprint).
  - `src/lib/authz.ts` étendu : `canAccessAgency(user, agencyId)` et `getAccessibleAgencyIds(user)`, centralisant et **renforçant** une vérification déjà présente mais dupliquée pour `agencies` (voir « Déviations » ci-dessous pour la faille corrigée).
  - Routes API : `GET|POST /api/vehicles`, `GET|PATCH|DELETE /api/vehicles/[id]`, `GET /api/vehicles/[id]/availability`, `GET|POST /api/locations`, `GET|PATCH|DELETE /api/locations/[id]`, `GET|POST /api/clients` — toutes vérifient tenant + appartenance à l'agence côté serveur ; `agencyId` d'une location toujours dérivé du véhicule côté serveur, jamais accepté depuis le client.
  - Pages dashboard : `/dashboard/vehicles` (liste + filtres statut/catégorie/agence + `VehiclesTable`), `/new`, `/[id]` (édition + tableau des locations associées) ; `/dashboard/locations` (liste + filtres statut/véhicule/client/dates + `LocationsTable` avec action « Annuler »), `/new` (sélection véhicule disponible, vérification de disponibilité en direct, calcul du prix estimé, sélection/création de client inline), `/[id]` (détail + `LocationActions` : transitions de statut affichées selon `canTransition`, édition des notes). Navigation ajoutée dans `Sidebar.tsx`.
  - `src/lib/format.ts` (`formatMoney`) : utilitaire partagé d'affichage des montants (entiers + devise).
  - `src/__tests__/vehicles.test.ts` et `locations.test.ts` (30 tests) : CRUD, immatriculation unique, disponibilité/conflits, calcul de prix, machine à états, isolation multi-tenant **et** multi-agence — 76/76 tests passés au total (voir [TESTREPORT.md](./TESTREPORT.md)).
  - `npm run lint` / `npm run test` / `npm run build` validés ; parcours complet vérifié manuellement contre un serveur `next dev` réel (inscription → agence → véhicule → client → location → 6 pages dashboard, toutes en 200), données de test nettoyées après vérification.
- **Décisions prises pendant ce sprint** (validées explicitement par le propriétaire du projet avant implémentation, voir section 6) :
  - `Location` fusionne réservation et contrat en une seule entité (au lieu de deux modèles séparés envisagés dans DOMAINRULES.md section 7/8).
  - Modèle `Client` distinct de `User` (le locataire externe n'est pas un compte staff avec identifiants de connexion).
  - `Vehicle.agencyId` obligatoire (pas de véhicule multi-agence ni sans agence).
  - Colonne `currency` (défaut `"MAD"`, changé depuis `"EUR"` peu après le Sprint 5 — voir section 6) ajoutée à côté de chaque montant, en cohérence avec la règle déjà validée en Sprint 1 (ARCHITECTURE.md), malgré l'absence de décision multi-devises définitive.
- **Faille de sécurité détectée et corrigée pendant ce sprint** (avant tout déploiement, révélée par un test d'isolation) : `canAccessAgency` ne vérifiait initialement que le rôle (`ADMIN` → accès accordé) sans jamais vérifier que l'agence ciblée appartenait bien au tenant de l'utilisateur. Un `ADMIN` du tenant A aurait pu, en fournissant un `agencyId` du tenant B, créer un véhicule ou une location rattachés à l'agence d'un autre tenant. Corrigé en ajoutant une vérification `agency.tenantId === user.tenantId` en tête de la fonction (`src/lib/authz.ts`) avant toute logique de rôle. Les routes `agencies/[id]` existantes n'étaient pas exploitables (elles vérifiaient déjà le tenant via `getAgencyById` avant d'appeler `canAccessAgency`), mais en bénéficient désormais aussi de façon défensive. Voir [TESTREPORT.md](./TESTREPORT.md) pour le test qui a révélé le problème.
- **Décision de granularité de rôle (provisoire, à confirmer explicitement)** : un `MEMBER` rattaché à une agence via `UserAgency` peut créer/modifier/supprimer les véhicules et locations de cette agence (pas seulement les consulter) — contrairement à `Tenant`/`Agency` où l'écriture reste réservée à `ADMIN`. Voir [DOMAINRULES.md](./DOMAINRULES.md) section 4 et section 8 point 21 ci-dessous.
- **Non traité, hors périmètre explicite du sprint** :
  - Page `/dashboard/clients` dédiée (recherche, historique, modification, fusion de doublons) — uniquement sélection/création inline depuis le formulaire de location.
  - Modèle `Category` dédié pour les véhicules — `category` reste un champ texte libre sur `Vehicle`.
  - Paiements, cautions, incidents de location, contenu détaillé de contrat (conditions générales, franchise) — toujours hors périmètre.
  - Gestion de la maintenance et de l'historique kilométrique des véhicules.

### Sprint 7 — Maintenance des véhicules et alertes/notifications

- **Fait** :
  - `prisma/schema.prisma` : modèle `Maintenance` (`vehicleId` requis, `agencyId`/`currency` dérivés du véhicule, `status` enum `MaintenanceStatus`, `type` enum `MaintenanceType`, `cost` entier optionnel), modèle `Alert` (`tenantId` requis, `agencyId`/`userId` optionnels, `type`/`priority`/`status` enums, `entityType`/`entityId` texte libre) ; migration `20260812090456_add_maintenance_and_alert_models` appliquée sur `xrent_dev` et `xrent_test`.
  - `src/lib/maintenances.ts` : CRUD, machine à états `canTransition` (`SCHEDULED→IN_PROGRESS|COMPLETED|CANCELLED`, `IN_PROGRESS→COMPLETED|CANCELLED`, `COMPLETED`/`CANCELLED` terminaux), édition/suppression bloquées une fois `COMPLETED`/`CANCELLED` (historique conservé, `MaintenanceNotEditableError`/`MaintenanceNotDeletableError`), `getDueMaintenances(tenantId, daysAhead)`, `createMaintenanceFromSchedule(tenantId, vehicleId, type, scheduledDate)` (signature ajustée avec `tenantId` en premier paramètre — déviation documentée, voir ci-dessous).
  - `src/lib/alerts.ts` : CRUD sans suppression (aucune fonction `deleteAlert`), machine à états `canTransition` (`PENDING→ACKNOWLEDGED|RESOLVED`, `ACKNOWLEDGED→RESOLVED`, `RESOLVED` terminal), `acknowledgeAlert`/`resolveAlert` (idempotent si déjà `ACKNOWLEDGED`), `getPendingAlerts(tenantId, userId?)` (alertes non résolues, tri par priorité).
  - `src/lib/scheduled-tasks.ts` : `checkDueMaintenances`/`checkReturnsToday`/`checkOverdueInvoices`, chacune scopée à un tenant, génération idempotente d'alertes (pas de doublon tant qu'une alerte non résolue existe déjà pour la même ressource `entityType`/`entityId`).
  - Routes API : `GET|POST /api/maintenances`, `GET|PATCH|DELETE /api/maintenances/[id]`, `GET /api/alerts`, `PATCH /api/alerts/[id]/acknowledge`, `PATCH /api/alerts/[id]/resolve`, `POST /api/tasks/check-alerts` (réservé `ADMIN`) — toutes vérifient tenant + appartenance à l'agence côté serveur ; aucune route `POST`/`DELETE` pour `Alert` (créée par le système uniquement, jamais supprimable).
  - Pages dashboard : `/dashboard/maintenances` (liste + filtres statut/type/véhicule/dates + `MaintenancesTable` avec actions modifier/marquer terminée/annuler via dialogues), `/new` (formulaire de planification) ; `/dashboard/alerts` (liste triée priorité puis date + filtres type/priority/status + `AlertsList` avec actions acknowledge/resolve). Navigation ajoutée dans `Sidebar.tsx`.
  - `Header.tsx` : badge d'alertes en attente (icône cloche, lien vers `/dashboard/alerts`), compté côté serveur dans `dashboard/layout.tsx` (`getPendingAlerts` + filtrage par agence accessible, même principe que vehicles/locations) et transmis via `DashboardLayout`.
  - `dashboard/page.tsx` : widget « À faire aujourd'hui » (retours du jour, maintenances dues, factures en retard — requêtes de lecture directes, sans effet de bord, distinctes de `checkX()` qui créent des alertes) et widget « Alertes récentes » (5 dernières, badges priorité/statut).
  - `src/__tests__/maintenances.test.ts` et `alerts.test.ts` (27 tests) : CRUD, machine à états, historique conservé, génération d'alertes idempotente (`POST /api/tasks/check-alerts`), acknowledge/resolve, filtrage priorité/status, isolation multi-tenant **et** multi-agence — 133/133 tests passés au total (voir [TESTREPORT.md](./TESTREPORT.md)).
  - `npm run lint` / `npm run test` / `npm run build` validés ; parcours complet vérifié manuellement contre un serveur `next dev` réel (inscription → agence → véhicule → maintenance planifiée aujourd'hui → déclenchement des vérifications → alerte visible dans le badge/le widget/la liste → acquittement → résolution → badge revenu à zéro ; second déclenchement sans doublon d'alerte), données de test nettoyées après vérification.
- **Déviations documentées par rapport à l'énoncé initial du sprint** (voir section 6 pour le détail) :
  - `createMaintenanceFromSchedule` prend `tenantId` en premier paramètre (l'énoncé initial l'omettait) : toute requête doit rester scopée tenant côté serveur, conformément à CLAUDE.md section 5 — aucune exception, même pour une fonction interne.
  - `createAlert(...)` prend un objet `CreateAlertInput` plutôt que des paramètres positionnels (`type, priority, tenantId, agencyId?, userId?, message`) — cohérent avec le pattern déjà en place pour toutes les autres fonctions `create*` du projet (`CreateVehicleInput`, `CreateLocationInput`, etc.).
  - `acknowledgeAlert`/`resolveAlert` prennent `tenantId` en premier paramètre (l'énoncé les omettait) — même raison que `createMaintenanceFromSchedule` ci-dessus.
  - Machine à états `Maintenance` : `SCHEDULED → COMPLETED` directement est autorisé (pas seulement via `IN_PROGRESS`), pour ne pas forcer une étape « en cours » à une petite agence qui traite l'entretien le jour même — seules « Terminer »/« Annuler » sont exposées dans l'UI.
- **Décision non soumise à validation préalable, prise en cours d'implémentation et documentée a posteriori** : `POST /api/tasks/check-alerts` est réservé au rôle `ADMIN` et reste scopé au tenant de l'ADMIN déclencheur (pas de scan multi-tenant global, aucun rôle « superadmin » transverse n'existant — voir section 8 point 16) — même justification pragmatique que l'accès aux rapports financiers (Sprint 6). **À confirmer explicitement**, voir section 8 point 29.
- **Non traité, hors périmètre explicite du sprint** :
  - Notifications par email — aucun prestataire SMTP configuré, décision explicite du propriétaire du projet en début de sprint (alertes in-app uniquement pour ce sprint).
  - Assignation manuelle d'une alerte à un utilisateur précis (`Alert.userId` existe dans le schéma mais n'est jamais renseigné par le code actuel — toutes les alertes générées par le système ont `userId` null, donc diffusées à tout le tenant/l'agence).
  - Récurrence automatique de maintenance (planification du prochain entretien après complétion d'un précédent) — `createMaintenanceFromSchedule` existe comme brique réutilisable mais n'est appelée par aucun moteur de récurrence.
  - Vrai cron périodique multi-tenant pour `checkDueMaintenances`/`checkReturnsToday`/`checkOverdueInvoices` — `POST /api/tasks/check-alerts` doit aujourd'hui être déclenché manuellement (ou via un cron externe appelant la route avec une session ADMIN), un mécanisme d'authentification de service dédié restant à concevoir.
  - Page de détail dédiée `/dashboard/maintenances/[id]` — actions (modifier/terminer/annuler) exposées directement depuis la liste via des dialogues, non demandée explicitement par l'énoncé du sprint.

Le dépôt est un dépôt git (branche `main`) avec un commit initial : `e673184` — "chore: initialize XRent Manager project". Le remote `origin` est configuré vers le dépôt GitHub privé `https://github.com/hdsc-123/xrent-manager.git`, et `main` est synchronisée avec `origin/main`. Le tag `v0.1.0` a été créé et envoyé, correspondant au socle initial.

Les principes d'architecture validés lors du Sprint 1 (voir section 6) sont désormais **largement implémentés** : socle de données, authentification (dont résolution du tenant à la connexion depuis le Sprint 9), autorisation serveur (tenant/agence/rôle), montants financiers en entiers + devise, gestion des utilisateurs/invitations, audit (périmètre limité), et les modules métier véhicules/locations/clients/factures/paiements/maintenance/alertes en place ; les cautions et incidents restent non implémentés (voir section 3).

## 2. Ce qui est terminé

- Initialisation du projet Next.js (TypeScript, ESLint, Tailwind CSS, App Router, `src/`, alias `@/*`).
- Validation de `npm run lint` (aucune erreur).
- Validation de `npm run build` (build de production réussi, 2 routes statiques : `/` et `/_not-found`).
- Documentation fondatrice (Sprint 0) : CLAUDE.md, HANDOFF.md, PROJECT_MAP.md, ARCHITECTURE.md, DOMAINRULES.md, SECURITY.md, TESTREPORT.md, INCIDENTS.md, README.md.
- Initialisation Git et commit initial (`e673184` — "chore: initialize XRent Manager project") sur la branche `main`.
- Connexion au dépôt GitHub privé `hdsc-123/xrent-manager` (remote `origin`) et synchronisation de `main` avec `origin/main`.
- Création et envoi (push) du tag `v0.1.0`, correspondant au socle initial.
- Validation par le propriétaire du projet des principes d'architecture fondamentaux (Sprint 1) : base de données/ORM cibles, modèle d'isolation multi-tenant/multi-agence, représentation des montants et des dates, séparation des environnements, audit, exports/imports, reset sécurisé, priorités de tests — voir section 6 et le détail dans [ARCHITECTURE.md](./ARCHITECTURE.md), [DOMAINRULES.md](./DOMAINRULES.md), [SECURITY.md](./SECURITY.md).
- Installation de Prisma 6.19.3 et `@prisma/client`, connexion à PostgreSQL 17 en local (`xrent_dev`).
- Schéma minimal `Tenant`/`Agency`/`User`/`UserAgency` et migration `20260811133155_init_tenant_agency_user` appliquée en développement (`xrent_dev`).
- Couche d'accès aux données minimale (`src/lib/prisma.ts` singleton, `src/lib/db.ts`), chaque fonction de lecture scopée obligatoirement par `tenantId`.
- Installation de Vitest comme framework de test (`npm run test`) et premier test d'isolation multi-tenant (`src/__tests__/db.test.ts`), validé (5/5 tests passés, aucune donnée résiduelle après nettoyage).
- Base de données de test dédiée `xrent_test` créée, distincte de `xrent_dev` ; `vitest.config.mts` configuré avec `loadEnv` (Vite, mode `test`) pour charger `DATABASE_URL` depuis `.env.test` ; migration `init_tenant_agency_user` appliquée sur `xrent_test` (`prisma migrate deploy`) ; les tests s'exécutent désormais contre cette base dédiée, plus contre `xrent_dev`.
- Validation de `npm run lint`, `npm run test` et `npm run build` après ces ajouts.
- **Sprint 3** : authentification NextAuth.js v5 (sessions JWT), champs `passwordHash`/`role` sur `User`, migration `add_nextauth_models_and_user_auth_fields` appliquée sur `xrent_dev` et `xrent_test`, routes `/api/auth/*`, CRUD `/api/tenants*` et `/api/agencies*` scopés serveur, `src/proxy.ts`, tests d'intégration HTTP (37/37 passés) — voir la section Sprint 3 ci-dessus et [TESTREPORT.md](./TESTREPORT.md).
- **Sprint 4** : shadcn/ui + TanStack Table v9 + lucide-react, pages `/login`/`/register`/`/dashboard` (+ `tenants`, `agencies`, `users` en lecture seule, `settings`), coquille dashboard (Sidebar/Header/DashboardLayout), `GET /api/users`, `src/lib/api.ts`, `src/hooks/{useUser,useTenant}.ts`, tests d'intégration HTTP sur le rendu des pages (46/46 passés au total) — voir la section Sprint 4 ci-dessus et [TESTREPORT.md](./TESTREPORT.md).
- **Sprint 5** : modèles `Client`/`Vehicle`/`Location`, CRUD API + dashboard complets (véhicules, locations), disponibilité/conflits de réservation, machine à états, calcul de prix (`totalPrice`), `src/lib/{vehicles,locations,clients,format}.ts`, extension de `src/lib/authz.ts` (`canAccessAgency`/`getAccessibleAgencyIds`, avec correction d'une faille de vérification tenant), 76/76 tests passés au total — voir la section Sprint 5 ci-dessus et [TESTREPORT.md](./TESTREPORT.md).
- **Sprint 6** : modèles `Invoice`/`Payment`, CRUD API + dashboard complets (factures, paiements), numérotation de facture par tenant, calcul TVA/remise/total, machine à états, export PDF, paiements manuels (pas d'intégration Stripe/PayPal), rapports (revenu/utilisation véhicule/top véhicules, export CSV, réservés ADMIN), `src/lib/{invoices,payments,reports}.ts`, `src/components/invoices/InvoicePdf.tsx`, 106/106 tests passés au total — voir la section Sprint 6 ci-dessus et [TESTREPORT.md](./TESTREPORT.md).
- **Sprint 7** : modèles `Maintenance`/`Alert`, CRUD API + dashboard complets (maintenances), liste + acknowledge/resolve (alertes), génération automatique et idempotente d'alertes (`src/lib/scheduled-tasks.ts`, `POST /api/tasks/check-alerts` réservé ADMIN), badge d'alertes dans le header, widgets « À faire aujourd'hui »/« Alertes récentes » sur le dashboard, alertes in-app uniquement (pas d'email), `src/lib/{maintenances,alerts,scheduled-tasks}.ts`, 133/133 tests passés au total — voir la section Sprint 7 ci-dessus et [TESTREPORT.md](./TESTREPORT.md).
- **Sprint 8** : module `/dashboard/clients*` dédié (liste + recherche, création, détail/édition, suppression bloquée si location associée), `GET`/`PATCH`/`DELETE /api/clients/[id]` nouveaux (`GET`/`POST /api/clients` existaient déjà depuis Sprint 5), `src/lib/clients.ts` étendu (`updateClient`/`deleteClient`), consolidation des règles métier/sécurité existantes (aucun écart trouvé, aucun changement de code nécessaire au-delà du module clients), 146/146 tests passés au total — voir la section Sprint 8 ci-dessus et [TESTREPORT.md](./TESTREPORT.md).
- **Sprint 9** : `PATCH`/`DELETE /api/users/[id]` (rôle, réinitialisation mot de passe, suppression, garde « dernier ADMIN »), flux d'invitation complet (`Invitation`, `/dashboard/invitations`, page publique `/invitations/[id]`, pas d'email), résolution du tenant à la connexion (Option B), politique de mot de passe, session `maxAge` + « se souvenir de moi », journal d'audit (`AuditLog`, `/dashboard/audit`, périmètre limité), pas de SUPERADMIN (décision explicite), 187/187 tests passés au total — voir la section Sprint 9 ci-dessus et [TESTREPORT.md](./TESTREPORT.md).
- **Sprint 10** : journal d'audit exhaustif sur tout le CRUD métier (vehicles/locations/clients/invoices/payments/maintenances/alerts), filtres d'audit (ressource/action/utilisateur), édition du profil utilisateur (`GET`/`PATCH /api/users/me`, avec vérification de l'ancien mot de passe), revue de sécurité consolidée (aucune faille, un gap de couverture de test comblé sur `/api/reports/*`), scénario E2E complet (`e2e-full.test.ts`), documentation finale consolidée, 206/206 tests passés au total — voir la section Sprint 10 ci-dessus et [TESTREPORT.md](./TESTREPORT.md).
- **Sprint 11** : rafraîchissement immédiat de la session JWT (nom/email) après édition du profil (`SessionProvider` + `useSession().update()` côté client, trigger `"update"` relisant la base côté serveur dans le callback `jwt()`), levant la limite documentée depuis le Sprint 10 ; audit complet des points ouverts de HANDOFF.md section 8 (tableau de suivi ajouté, 3 décisions provisoires confirmées par continuité — points 21/24/29) ; audit de sécurité/scoping tenant des 36 fichiers `route.ts` (aucune faille inter-tenant trouvée) ; écart fonctionnel corrigé (`POST /api/tenants` créait des tenants orphelins jamais rattachés à aucun user — route et page `/dashboard/tenants/new` retirées) ; 206/206 tests passés au total — voir la section Sprint 11 ci-dessus et [TESTREPORT.md](./TESTREPORT.md).
- **Sprint 12A** : formulaires professionnels complets pour agences/véhicules/clients/locations, champs métier ajoutés à `Agency`/`Vehicle`/`Client`/`Location` (migration additive, tous nullables), composant `PhoneInput` interne, redirection vers la liste correspondante après création/édition, aperçu du nombre de jours en temps réel sur le formulaire de location (règle inchangée), 224/224 tests passés au total — voir la section Sprint 12A ci-dessus et [TESTREPORT.md](./TESTREPORT.md).
- **Sprint 12B** : génération automatique d'une facture `DRAFT` à la création d'une location (résiliente, réutilise `src/lib/invoices.ts` du Sprint 6 sans le modifier), boutons « Voir facture » sur la liste et le détail des locations, correctif de `deleteLocation` (blocage 500 devenu systématique sur les locations `PENDING`/`CANCELLED` du fait de leur nouvelle facture associée — nettoyage transactionnel si `DRAFT` sans paiement, sinon 409), correctif du bug de déconnexion Base UI (`DropdownMenuLabel` enveloppé dans `DropdownMenuGroup`, `Header.tsx`) ; export PDF, paiements multi-modes/mixtes et UX des invitations vérifiés déjà conformes depuis les Sprints 6/9 (aucun changement nécessaire) ; 224/224 tests passés au total (aucun nouveau test, 3 fichiers de test corrigés sur leur ordre de nettoyage) — voir la section Sprint 12B ci-dessus et [TESTREPORT.md](./TESTREPORT.md).
- **Sprints 12C à 14C** : réservations (import Excel, machine à états, conversion en contrat), permissions granulaires (`PermissionGroup`), caisse (`CashRegister`/`CashEntry`), paiement intégré simple/partiel/mixte, refonte du flux réservation → contrat, redesign complet du design system (palette/cartes/boutons/navigation mobile), socle véhicules/réservations et rafraîchissement immédiat du rôle, numérotation séquentielle de contrat + PDF contrat/facture/lots, transferts entre agences et bons de déplacement interne + revue de l'onglet Maintenance + six nouvelles alertes métier — voir les sous-sections « (rappel) » de la section 1 ci-dessus pour le détail complet de chacun, non dupliqué ici.
- **Sprint 14D** : correction de `CLAUDE.md` (ne prétendait plus à tort être au Sprint 0) ; reset de données métier/test d'un tenant depuis le SaaS (`GET`/`POST /api/data-reset`, réservé ADMIN, confirmation par nom de tenant, verrou de double-exécution, garde de production, journalisation) ; harmonisation de l'alignement des tableaux (`DataTable.tsx`, champ `meta.align` centralisé) ; 362/362 tests passés au total — voir la section 1 ci-dessus et [TESTREPORT.md](./TESTREPORT.md).
- **Sprint 14E** : correctif du bug racine causant le wrap anarchique des en-têtes de tableaux (`columnSizingFeature` de TanStack Table v9 fusionnait un `size: 150` par défaut dans toute colonne, y compris sans `size` explicite déclaré — `defaultColumn: { size: undefined }` ajouté à `useTable()` dans `DataTable.tsx`, `align-bottom` remplacé par `align-middle`, wrap désormais coupé aux espaces uniquement) ; propagé sans modification page par page aux 17 tables du SaaS utilisant ce composant partagé ; deux tables hors composant (Audit, État des véhicules) harmonisées séparément ; correctif purement visuel, aucune nouvelle route/migration/test, 362/362 tests toujours passés, vérifié par inspection DOM et capture d'écran (Chromium/Playwright) — voir la section 1 ci-dessus et DOMAINRULES.md section 32.

## 3. Ce qui n'est pas commencé

- Cautions, incidents de location, contenu détaillé de contrat/facture (conditions générales, franchise, kilométrage inclus, mentions légales, TVA intracommunautaire, avoirs/factures rectificatives).
- Intégration d'un prestataire de paiement (Stripe/PayPal ou autre) — paiements enregistrés manuellement uniquement (décision explicite Sprint 6, voir DOMAINRULES.md section 10) ; pas de remboursement/paiement négatif modélisé.
- Modèle `Category` dédié pour les véhicules (reste un champ texte libre sur `Vehicle`) ; historique de fusion de doublons ou documents d'identité/permis pour `Client` (module clients de base uniquement depuis Sprint 8, voir section 6).
- Notifications par email (alertes in-app uniquement, décision explicite Sprint 7, reconduite pour les invitations Sprint 9 — voir section 6) ; assignation manuelle d'une alerte à un utilisateur précis ; récurrence automatique de maintenance ; vrai cron périodique multi-tenant (voir section 8, points 30-32).
- Kilométrage et état technique détaillé des véhicules au-delà du suivi d'entretien (`Maintenance`, Sprint 7).
- Limitation du nombre de tentatives de connexion (protection brute force), MFA (voir [SECURITY.md](./SECURITY.md) section 3).
- Tout rôle transverse à plusieurs tenants ("superadmin" plateforme) — décision explicite reconduite au Sprint 9, seul `ADMIN`, scopé à un tenant, existe.
- Fuseau horaire par agence (aucun champ `timezone` sur `Agency`).
- Tout environnement de staging ou de production réel.
- Tout script de seed de développement.

## 4. Prochaine action recommandée

Le MVP reste **production-ready au sens fonctionnel** (Sprint 10) et vient d'être consolidé (Sprint 11) : rafraîchissement de session après édition de profil, audit complet des points ouverts, audit de sécurité/scoping tenant de toutes les routes API, un écart fonctionnel corrigé (tenants orphelins). Ce qui reste dépend désormais exclusivement de décisions produit/infra encore ouvertes (section 8, tableau de suivi) — plus aucune décision provisoire non confirmée ni dette technique connue à ce jour : le choix d'un prestataire d'email transactionnel si les notifications par email (alertes et/ou invitations) sont confirmées comme nécessaires (point 30), les modalités de cautions (point 5), la granularité des rôles au-delà d'ADMIN/MEMBER (points 2/15), et le prochain module métier à construire (cautions ? incidents de location ?) avant tout déploiement réel en production (aucun environnement staging/production, aucune stratégie de sauvegarde/hébergement/juridiction tranchée — points 6/7/9/10). Conformément à [CLAUDE.md](./CLAUDE.md), aucun code métier ou dépendance supplémentaire ne doit être ajouté sans validation explicite distincte de celle de ce sprint.

## 5. Commandes déjà validées

| Commande | Statut | Résultat observé |
|---|---|---|
| `npm run lint` | ✅ Validé | Aucune erreur ESLint (Sprint 14E) |
| `npx tsc --noEmit` | ✅ Validé | Aucune erreur TypeScript strict (Sprint 14E) |
| `npm run test` | ✅ Validé | 362/362 tests passés (Vitest, 28 fichiers, aucune régression Sprint 14E) — voir TESTREPORT.md section 1 pour l'historique complet sprint par sprint et section 3 pour le détail narratif de chaque fichier de test |
| `npm run build` | ✅ Validé | Build de production réussi (Turbopack, Next.js 16.3.0), TypeScript strict sans erreur, 71 routes (API + pages `/dashboard/*` + `/login`/`/register` + page publique `/invitations/[id]` + Proxy) — inchangé depuis le Sprint 14D (correctif Sprint 14E purement CSS/JSX, aucune nouvelle route), revalidé ce sprint |

Migrations de développement appliquées via `npx prisma migrate dev` (sur `xrent_dev`) puis répercutées sur `xrent_test` via `npx prisma migrate deploy` — jamais l'inverse, et jamais de commande interactive/destructive en production (aucun environnement de production n'existe à ce jour). Aucune commande de seed n'a été exécutée à ce jour (le reset de données du Sprint 14D est l'inverse d'un seed — il vide, il ne génère jamais de données fictives, conformément à CLAUDE.md règle 7).

## 6. Décisions prises

Décisions produit initiales (Sprint 0) :

- Le projet sera un SaaS multi-tenant et multi-agence, mobile-first, avec dashboard-admin dès le MVP (décision produit initiale, confirmée par le brief de démarrage).
- Aucun montant financier ne sera représenté en `float`.
- Aucune carte bancaire ne sera stockée en clair.
- Toute action sensible sera validée côté serveur.

Principes d'architecture validés (Sprint 1, 2026-08-11) — détail complet dans [ARCHITECTURE.md](./ARCHITECTURE.md), [DOMAINRULES.md](./DOMAINRULES.md) et [SECURITY.md](./SECURITY.md) :

- PostgreSQL comme base de données cible, Prisma comme ORM cible — **implémenté (Sprint 2)** : PostgreSQL 17 local (`xrent_dev`), Prisma 6.19.3.
- Isolation multi-tenant par `tenant_id` dans des tables partagées ; rattachement aux agences par `agency_id` — **implémenté au niveau schéma et couche d'accès aux données (Sprint 2)**, pas encore au niveau d'actions/routes serveur (aucune n'existe).
- Couche d'accès aux données centralisée, avec garde tenant/agence obligatoire — **implémentée (Sprint 2–3)** : `src/lib/db.ts` (`getTenantById`, `getAgencyById`, `getUserById`) réutilisée par les routes `/api/agencies*` ; les routes `/api/tenants*` interrogent Prisma directement avec un filtrage `tenantId`/`id` explicite (pas encore consolidé dans `src/lib/db.ts` — écarts mineurs à corriger si `db.ts` devient le point d'entrée unique imposé pour l'écriture aussi).
- Validation côté serveur systématique de l'identité, du rôle, du tenant, de l'agence et de l'appartenance de la ressource — **implémenté (Sprint 3)** : `src/lib/authz.ts` (`getSessionUser`) + vérifications explicites dans chaque route (`src/app/api/tenants/**`, `src/app/api/agencies/**`), testées par `tenants.test.ts`/`agencies.test.ts`.
- Montants financiers en entiers, exprimés dans la plus petite unité monétaire, avec devise stockée explicitement à côté de chaque montant — **implémenté (Sprint 5)** : `Vehicle.pricePerDay`/`Location.pricePerDay`/`Location.totalPrice` (entiers, centimes) + `currency` (défaut `"MAD"`, provisoire — voir section 8 point 3).
- Dates stockées en UTC, affichées selon le fuseau horaire de l'agence — **partiellement implémenté (Sprint 5)** : `Location.startDate`/`endDate` stockées en UTC (type `DateTime` Prisma/Postgres) ; toujours pas de conversion à l'affichage selon un fuseau d'agence (`Agency.timezone` n'existe toujours pas, voir section 8).
- Environnements développement, test, staging et production strictement séparés — partiellement respecté : développement (`xrent_dev`) et test (`xrent_test`) sont désormais deux bases distinctes ; staging et production restent non définis (voir section 8).
- Table d'audit dédiée comme mécanisme technique de traçabilité — non implémentée, toujours hors périmètre.
- Exports et imports validés côté serveur et scopés par tenant — non implémenté (aucun export/import n'existe).
- Reset de données techniquement impossible en environnement de production — non implémenté (aucun mécanisme de reset n'existe).
- Tests introduits dès le premier module métier, avec priorité aux tests d'isolation multi-tenant — **appliqué dès la couche d'accès aux données (Sprint 2) puis étendu aux routes API (Sprint 3)** : `src/__tests__/db.test.ts`, `tenants.test.ts`, `agencies.test.ts` couvrent l'isolation `Tenant`/`Agency`/`User` par `tenantId` et l'isolation multi-agence par `UserAgency`.

Décisions techniques Sprint 3 (2026-08-11), validées par le propriétaire du projet :

- Authentification : **NextAuth.js (Auth.js) v5**, fournisseur `CredentialsProvider` uniquement (pas d'OAuth à ce stade).
- Sessions : **JWT**, pas "database" — décision ajustée en cours de sprint après avoir vérifié dans le code de NextAuth v5 (`node_modules/@auth/core/lib/utils/assert.js`) que `CredentialsProvider` et `session.strategy: "database"` sont techniquement incompatibles (la librairie lève `UnsupportedStrategy`). Le propriétaire du projet a tranché en faveur de JWT, la stratégie standard pour ce type d'authentification (voir [SECURITY.md](./SECURITY.md) section 5).
- Rôles : `ADMIN` (par défaut pour le premier user d'un tenant, créé via `/api/auth/register`) et `MEMBER` — stockés en clair sur `User.role` (`String`), pas encore un type énuméré ni une table de permissions.
- `GET /api/tenants` retourne uniquement le tenant de l'ADMIN connecté (jamais tous les tenants de la plateforme) : aucun rôle "superadmin" transverse n'existe dans ce schéma, et l'énoncé initial du sprint ("lister les tenants, admin only") était ambigu sur ce point — interprété au sens le plus restrictif pour ne pas violer SECURITY.md section 1 (voir aussi section 8, point 16).

Décisions techniques Sprint 4 (2026-08-11), validées par le propriétaire du projet :

- UI : **shadcn/ui**, icônes **lucide-react**, table **TanStack Table**, pas de gestion d'état global (Zustand/Redux) — RSC + état local suffisent pour ce périmètre.
- **`/dashboard/users` en lecture seule pour ce sprint** : demandé lors du cadrage initial avec actions modifier/supprimer/changer le rôle, mais aucune route API `PATCH`/`DELETE /api/users/[id]` n'existait et la granularité des permissions reste explicitement À DÉCIDER (section 8, point 2) ; le propriétaire du projet a tranché en faveur d'une liste en lecture seule (nouvelle route `GET /api/users`, ADMIN uniquement) plutôt que de construire une logique de gestion des rôles/suppression sans validation dédiée — voir aussi point 17 ci-dessous.
- Édition du profil utilisateur (nom/email/mot de passe) dans `/dashboard/settings` : non construite, pour la même raison (aucune route de mutation existante, changement de mot de passe = sujet de sécurité nécessitant sa propre validation) — affichage en lecture seule avec message explicite plutôt qu'un formulaire non fonctionnel.

Décisions techniques Sprint 5 (2026-08-11), validées par le propriétaire du projet **avant** implémentation (via questions explicites, conformément à CLAUDE.md section 8) :

- `Location` fusionne réservation et contrat en un seul modèle avec cycle de statuts, plutôt que deux entités séparées.
- Locataire modélisé par un nouveau modèle `Client` (nom/email/téléphone, sans login), distinct de `User` (comptes staff).
- `Vehicle.agencyId` obligatoire (un véhicule appartient à exactement une agence).
- Colonne `currency` ajoutée à côté de chaque montant (`Vehicle.pricePerDay`, `Location.pricePerDay`/`totalPrice`), valeur par défaut `"EUR"` initialement pour ce sprint, **puis changée en `"MAD"` (Dirham marocain)** peu après sur demande explicite du propriétaire du projet — migration `20260811203931_change_default_currency_to_mad` (`ALTER COLUMN ... SET DEFAULT`, sans effet rétroactif sur les enregistrements déjà créés), appliquée sur `xrent_dev` et `xrent_test` ; labels UI « Prix / jour (€) » corrigés en « Prix / jour (MAD) » (`/dashboard/vehicles/new`, `EditVehicleForm.tsx`) et assertions de test mises à jour (`vehicles.test.ts`, `locations.test.ts`). Devise réellement supportée en production (multi-devises) toujours À DÉCIDER (section 8, point 3/22).
- **Décision non soumise à validation préalable, prise en cours d'implémentation et documentée a posteriori** : granularité de rôle pour véhicules/locations — un `MEMBER` rattaché à une agence peut créer/modifier/supprimer (pas seulement lire) les véhicules et locations de cette agence, contrairement au modèle `Tenant`/`Agency` où l'écriture reste ADMIN uniquement. Choix pragmatique cohérent avec le modèle `UserAgency` déjà validé, mais **à confirmer explicitement** — voir section 8, point 21.
- **Correction de sécurité, pas une décision produit** : `canAccessAgency` vérifie désormais que l'agence appartient au tenant de l'utilisateur avant toute logique de rôle (voir section « Sprint 5 » ci-dessus pour le détail de la faille corrigée).

Décisions techniques Sprint 6 (2026-08-11), validées par le propriétaire du projet **avant** implémentation (brief de sprint explicite, conformément à CLAUDE.md section 8) :

- Bibliothèques : **`@react-pdf/renderer`** (PDF), **`papaparse`** (CSV), **`recharts`** (graphiques) — seules nouvelles dépendances ajoutées à `package.json` pour ce sprint.
- **Aucune intégration Stripe/PayPal** : les paiements sont enregistrés manuellement par le personnel d'agence (voir DOMAINRULES.md section 10).
- `taxRate` en points de base (entier, ex. `2000` = 20 %) et `discountAmount` en montant fixe (entier, plus petite unité monétaire) — cohérent avec la règle « jamais de `float` pour un montant financier » (voir DOMAINRULES.md section 14 et 17).
- Numérotation de facture `INV-{année}-{5 chiffres}`, unique par tenant, générée par comptage (pas de table de séquence dédiée) avec réessai en cas de collision — voir DOMAINRULES.md section 17.
- `Invoice.amountPaid`/`status` ne sont jamais modifiés directement : toujours recalculés depuis la somme réelle des `Payment` (`recomputeInvoiceStatus`) — voir DOMAINRULES.md section 10.
- **Décision non soumise à validation préalable, prise en cours d'implémentation et documentée a posteriori** : les rapports financiers (`/api/reports/*`, `/dashboard/reports`) sont réservés au rôle `ADMIN` — choix pragmatique (données agrégées sur tout le tenant), **à confirmer explicitement**, voir section 8 point 24.
- **Décision non soumise à validation préalable, prise en cours d'implémentation et documentée a posteriori** : `Invoice` peut être créée en plusieurs exemplaires pour une même `Location` (aucune contrainte d'unicité `locationId`) — le schéma ne l'empêche pas, mais aucun cas d'usage (facture partielle, avoir) n'a été explicitement demandé, **à confirmer**, voir section 8 point 25.

Décisions techniques Sprint 7 (2026-08-12), validées par le propriétaire du projet **avant** implémentation (brief de sprint explicite, conformément à CLAUDE.md section 8) :

- Alertes **in-app uniquement** (badge + liste `/dashboard/alerts`) — pas d'envoi par email pour ce sprint, aucun prestataire SMTP configuré (décision explicite, voir section 8 point 30).
- Une alerte n'est **jamais supprimable**, seulement `acknowledge`/`resolve` — aucune route `POST`/`DELETE /api/alerts*`, aucune fonction `deleteAlert`.
- Coûts de maintenance (`Maintenance.cost`) en entier (centimes), `currency` par défaut `"MAD"` — cohérent avec la règle déjà validée (section 14, DOMAINRULES.md).
- Historique de maintenance conservé même après complétion : aucune modification ni suppression possible une fois `COMPLETED`/`CANCELLED` (contrainte explicite de l'énoncé du sprint).
- **Décision non soumise à validation préalable, prise en cours d'implémentation et documentée a posteriori** : `POST /api/tasks/check-alerts` réservé au rôle `ADMIN` et scopé au tenant du déclencheur (pas de cron multi-tenant global, aucun rôle « superadmin » transverse n'existant) — même justification pragmatique que l'accès aux rapports financiers (Sprint 6), **à confirmer explicitement**, voir section 8 point 29.
- **Décision non soumise à validation préalable, prise en cours d'implémentation et documentée a posteriori** : machine à états `Maintenance` autorisant `SCHEDULED → COMPLETED` directement (sans passer par `IN_PROGRESS`) — choix pragmatique pour ne pas imposer une étape « en cours » distincte à une petite agence, **à confirmer**, voir section 8 point 31.

Décisions techniques Sprint 8 (2026-08-12), validées par le propriétaire du projet **avant** implémentation (brief de sprint explicite, conformément à CLAUDE.md section 8) :

- Module `Client` dédié construit sur le modèle existant depuis Sprint 5 (aucun changement de schéma) : page `/dashboard/clients*` + `GET`/`PATCH`/`DELETE /api/clients/[id]` nouveaux, en plus de `GET`/`POST /api/clients` déjà en place.
- **Décision non soumise à validation préalable, prise en cours d'implémentation et documentée a posteriori** : suppression d'un client bloquée (409, `ClientHasLocationsError`) tant qu'il a au moins une location associée — même principe que `DELETE /api/vehicles/[id]` (`VehicleHasLocationsError`), pour ne jamais perdre l'historique de location d'un client déjà utilisé. **À confirmer explicitement**, comme les autres décisions pragmatiques prises en cours de sprint (voir section 8).
- Aucune restriction de rôle ajoutée sur `/api/clients/[id]` (`GET`/`PATCH`/`DELETE`) : cohérent avec `GET`/`POST /api/clients` déjà en place depuis Sprint 5, qui n'imposaient aucune restriction — un `MEMBER` peut donc gérer tous les clients de son tenant, sans notion d'agence (DOMAINRULES.md section 9).
- Relecture de sécurité/consolidation demandée par le sprint : aucun écart trouvé (scoping tenant déjà correct sur toutes les écritures `Client`/`Location`/`Invoice`, `check-alerts`/`/api/reports/*` toujours ADMIN-only, aucune route d'accès cross-tenant identifiée) — voir section « Sprint 8 » ci-dessus, aucune correction de code nécessaire au-delà du module clients lui-même.
- Aucun superadmin, aucune notification email, aucune récurrence de maintenance, aucun avoir/facture rectificative introduits (hors périmètre explicite du sprint, contraintes respectées).

Décisions techniques Sprint 9 (2026-08-12), validées par le propriétaire du projet **avant** implémentation (brief de sprint explicite + 3 questions de cadrage explicites — résolution de tenant, superadmin, audit — conformément à CLAUDE.md section 8) :

- Résolution du tenant à la connexion en cas d'email dupliqué entre tenants : **Option B** — page de sélection explicite du tenant après vérification du mot de passe (pas la résolution arbitraire au premier tenant trouvé, Option A) ; voir HANDOFF.md point 16 (tranché) et SECURITY.md section 3.
- Aucun rôle SUPERADMIN plateforme construit — décision explicite, cohérente avec toutes les décisions précédentes (Sprints 3, 6, 7) qui excluaient déjà un rôle transverse à plusieurs tenants ; voir HANDOFF.md point 16 (tranché, reconduit).
- Journal d'audit construit (`AuditLog`, `src/lib/audit.ts`, `/dashboard/audit`) — **décision non soumise à validation préalable, prise en cours d'implémentation et documentée a posteriori** : le périmètre d'instrumentation est volontairement limité aux actions sensibles de ce sprint (rôle/suppression user, invitations), pas un audit exhaustif de tout le CRUD métier. **À confirmer explicitement** si un audit plus large est nécessaire, voir section 8 point 33.
- **Décision non soumise à validation préalable, prise en cours d'implémentation et documentée a posteriori** : la garde « dernier ADMIN » (`LastAdminError`) bloque toute rétrogradation/suppression laissant un tenant sans `ADMIN`, que l'action porte sur soi-même ou sur un autre utilisateur — extension au-delà de la seule auto-rétrogradation/auto-suppression demandée dans le brief initial (HANDOFF.md point 19). **À confirmer**, voir section 8 point 34.
- **Décision non soumise à validation préalable, prise en cours d'implémentation et documentée a posteriori** : un `ADMIN` peut réinitialiser le mot de passe d'un autre utilisateur de son tenant (`PATCH /api/users/[id]` avec `password`) sans vérification de l'ancien mot de passe — action administrative distincte d'un changement de mot de passe par l'utilisateur lui-même (toujours hors périmètre, voir section 8 point 20). **À confirmer**.
- Invitations sans envoi d'email (reconduit de la décision Sprint 7 sur les alertes) : le lien `/invitations/[id]` est partagé manuellement par l'ADMIN — limite du MVP explicitement documentée, voir section 8 point 30.
- `id` (cuid) de l'`Invitation` sert directement d'identifiant non-devinable dans le lien partageable, pas de colonne `token` séparée — choix de simplicité (CLAUDE.md section 7), cohérent avec l'usage de `cuid()` comme identifiant opaque partout ailleurs dans le schéma.
- **Correction de sécurité, pas une décision produit, découverte pendant ce sprint** : `signIn()` de NextAuth (appelé côté serveur) sérialise ses options via `URLSearchParams`, qui coerce `undefined` en la chaîne littérale `"undefined"` — passer `tenantId: undefined` cassait silencieusement le lookup tenant-scopé dans `authorize()`. Corrigé en n'incluant `tenantId` dans les options que lorsqu'il est réellement défini.

Décisions techniques Sprint 10 (2026-08-12), validées par le propriétaire du projet **avant** implémentation (brief de sprint explicite tranchant 4 points, conformément à CLAUDE.md section 8) :

- Audit exhaustif sur tout le CRUD métier : **tranché** (précise/clôt le point 33 ci-dessous, ouvert depuis Sprint 9) — `logAction` est désormais appelé après chaque création/modification/suppression réussie sur `Vehicle`, `Location`, `Client`, `Invoice`, `Payment`, `Maintenance`, `Alert` (acknowledge/resolve, la seule ressource sans `POST`/`DELETE`). Convention de nommage retenue : `<ressource-en-minuscules>.<verbe>` (`vehicle.created`/`updated`/`deleted`, etc.), avec une action `.status_changed` distincte de `.updated` quand la mutation change explicitement `status` (`Location`, `Invoice`, `Maintenance` — les trois modèles à machine à états instrumentés ; `Client`/`Vehicle`/`Payment` n'ont pas de machine à états et n'utilisent donc que `.updated`). `metadata` inclut systématiquement le corps de la requête (`changes`) et, pour les transitions de statut, le statut de départ (`from`) — jamais de donnée sensible (mot de passe, `passwordHash`).
- Garde « dernier ADMIN » étendue aux actions d'un tiers : **confirmée** (clôt le point 34 ci-dessous) — relecture du code (`assertNotLastAdmin` dans `src/lib/users.ts`) : la garde ne dépend que du nombre d'ADMIN restants dans le tenant après l'action, jamais de l'identité de l'acteur, donc elle s'applique structurellement de façon identique en self-action et en action d'un tiers. Aucun scénario de test distinct n'est possible pour la "action d'un tiers" : si retirer le rôle ADMIN à la cible laisserait le tenant sans ADMIN, c'est que la cible est l'unique ADMIN du tenant — et seul un ADMIN peut appeler cette route (403 sinon), donc l'acteur ne peut être que la cible elle-même dans ce cas précis.
- Édition du profil utilisateur par lui-même : **tranchée et implémentée** (clôt le point 20 ci-dessous) — `GET`/`PATCH /api/users/me` (nom, email, mot de passe avec vérification de l'ancien mot de passe), distincte de la réinitialisation *par un ADMIN sur un autre user* (Sprint 9, sans vérification de l'ancien mot de passe). Aucune invalidation des sessions JWT existantes n'a été ajoutée (hors périmètre : un changement de mot de passe ne révoque pas les sessions déjà émises sur d'autres appareils — cohérent avec le risque déjà documenté « Sessions JWT non révocables côté serveur », section 7).
- Invitations sans email : **reconduit tel quel** (aucun changement ce sprint) — reste en lien manuel pour le MVP, documenté comme limite explicite (voir section 3).
- **Décision non soumise à validation préalable, prise en cours d'implémentation et documentée a posteriori** : le rafraîchissement de la session JWT (`session.user.name`/`email`) après une édition de profil n'a pas été implémenté (pas de `SessionProvider`/`update()` ajouté) — voir la limite documentée section 3. Le choix a été de ne pas introduire cette dépendance architecturale sans qu'elle soit explicitement demandée par le sprint, conformément à CLAUDE.md section 7 (« ne pas installer de dépendance non nécessaire », « ne pas modifier ce qui n'est pas strictement nécessaire »). **À confirmer** si jugé prioritaire.
- Préparation déploiement : `.env.example` complété (`AUTH_SECRET` manquant, ajouté aux côtés de `DATABASE_URL`) — voir section 2. Docker (`Dockerfile`/`docker-compose.yml`, optionnels selon l'énoncé du sprint) explicitement déclinés pour ce sprint sur confirmation du propriétaire du projet, cohérent avec le fait que l'hébergeur cible reste **À DÉCIDER** (point 6) : construire une configuration Docker sans décision d'hébergement validée risquerait de figer un choix non tranché.
- Revue de sécurité consolidée : aucune faille trouvée (toutes les routes API vérifiées lisent `getSessionUser()`, scopent par `tenantId`, et les routes `ADMIN`-only le sont bien — `tenants`, `agencies` en écriture, `users`, `invitations` en écriture, `audit`, `reports/*`, `tasks/check-alerts`). Un gap de **couverture de test** (pas de faille de sécurité en elle-même) a été trouvé et comblé : `src/__tests__/reports.test.ts` ne testait que les fonctions `src/lib/reports.ts` directement, jamais les routes HTTP `GET /api/reports/*` — aucun test ne vérifiait qu'un `MEMBER` recevait bien 403. Six tests HTTP ajoutés (401/403/200 pour les deux routes).

Décisions techniques Sprint 11 (2026-08-12), sprint de consolidation demandé explicitement par le propriétaire du projet (audit des points restants du handoff + rafraîchissement de session + passe finale de tests/documentation, conformément à CLAUDE.md section 8) :

- Rafraîchissement de la session JWT après édition de profil : **tranché et implémenté**, clôt le point 35 (ouvert depuis Sprint 10). Option retenue parmi celles listées par le brief de sprint : `SessionProvider` (`next-auth/react`) + `useSession().update()`, plutôt qu'un simple `router.refresh()` seul — un `router.refresh()` sans `update()` ne suffit pas ici, puisque `dashboard/layout.tsx` lit `getSessionUser()` → `auth()` → le **token JWT déjà émis** (figé à la connexion), pas la base ; sans regénérer ce token via le mécanisme `trigger: "update"` de NextAuth, aucun nombre de `router.refresh()` n'aurait rafraîchi le nom/email affiché dans le `Header`. Le `SessionProvider` est scopé au sous-arbre `/dashboard` (pas le layout racine), pour rester strictement nécessaire (CLAUDE.md section 7) — `signIn`/`signOut` ailleurs dans l'app ne l'exigent toujours pas (Sprint 4). Par prudence de sécurité, le nom/email injecté dans le nouveau token n'est **jamais** pris du payload client de l'`update()` mais relu directement en base par `token.id` côté serveur (`src/lib/auth.ts`, callback `jwt()`) — seul `PATCH /api/users/me`, déjà authentifié et validé, fait foi.
- Points 21/24/29 (décisions provisoires « à confirmer » depuis les Sprints 5/6/7) : **confirmés par continuité**, pas de nouvelle validation explicite sollicitée ce sprint — ces trois comportements sont en production stable, testés, et documentés sans qu'aucune remise en cause n'ait été formulée en 4 à 6 sprints ultérieurs. Aucun changement de code associé.
- **Écart fonctionnel corrigé, validé explicitement par le propriétaire du projet en cours de sprint** (clôt le nouveau point 36) : `POST /api/tenants` créait un `Tenant` orphelin (jamais rattaché à aucun `User`, ni ré-accessible ensuite via `GET`/`PATCH`/`DELETE /api/tenants/[id]`, tous scopés à `id === user.tenantId`). Trouvé pendant l'audit de sécurité/scoping systématique de ce sprint. Ce n'est **pas** une faille d'isolation inter-tenant (aucune lecture croisée n'était possible), mais une incohérence avec le modèle produit déjà en vigueur partout ailleurs (« un ADMIN gère exactement son propre tenant », affiché texto sur `/dashboard/tenants`). Option retenue (parmi celles proposées) : retirer entièrement la route et la page plutôt que documenter seulement — la création de tenant reste l'exclusivité de `POST /api/auth/register` (tenant + premier ADMIN, atomique, Sprint 3), seul chemin cohérent avec le modèle actuel.
- Audit de sécurité/scoping (demandé par le sprint, section 2 du brief) : relecture systématique des 36 fichiers `route.ts`, confirmant que chaque route authentifiée appelle `getSessionUser()`, scope ses lectures/écritures par `user.tenantId`, et vérifie `canAccessAgency()`/le rôle `ADMIN` là où pertinent (agences, véhicules, locations, factures, paiements, maintenances, alertes, rapports, tâches planifiées, utilisateurs, invitations, audit) — aucune faille trouvée au-delà de l'écart `POST /api/tenants` déjà corrigé ci-dessus.

**Note (Sprint 14D)** : les décisions techniques des Sprints 12A à 14C n'ont pas été dupliquées dans cette section au moment de leur sprint respectif — elles restent entièrement documentées, décision par décision, dans DOMAINRULES.md (sections 21 à 30, chacune marquée « Décision validée (Sprint X) ») ainsi que dans les sous-sections « (rappel) » de la section 1 ci-dessus ; rien n'a été perdu, seule cette section 6 n'avait pas été tenue à jour en parallèle.

Décisions techniques Sprint 14D (2026-08-13), validées par le propriétaire du projet **avant** implémentation (brief de sprint explicite en 10 points, conformément à CLAUDE.md section 8) :

- Réalignement documentaire : **tranché et implémenté**. Seul `CLAUDE.md` affirmait un état d'avancement faux (« Sprint 0 », « aucun module métier n'existe encore ») — corrigé. Vérification explicite du reste du cadrage : HANDOFF.md (section 1), PROJECT_MAP.md, DOMAINRULES.md et SECURITY.md étaient **déjà** à jour jusqu'au Sprint 14C ; aucune correction de fond n'y était nécessaire au-delà des ajouts propres à ce sprint (section 1 ci-dessus, DOMAINRULES.md section 31, SECURITY.md section 17).
- Interprétation du « super admin » demandé par l'énoncé pour le reset : **tranchée, cohérente avec une décision déjà reconduite plusieurs fois** (points 16 et 21 ci-dessous) — aucun rôle transverse à plusieurs tenants n'existe dans ce schéma, décision explicite reconduite depuis le Sprint 9 et jamais remise en cause depuis. Plutôt que de construire un nouveau rôle `SUPERADMIN` (ce qui aurait été une décision de schéma/RBAC non validée préalablement, interdite par CLAUDE.md section 8 sans confirmation explicite), le rôle `ADMIN` existant — déjà le plus privilégié, déjà scopé à un tenant — a été retenu, avec le reset **strictement scopé au tenant de l'ADMIN déclencheur**. Cohérent avec le traitement de tous les autres cas similaires déjà rencontrés (`/api/reports/*` Sprint 6, `/api/tasks/check-alerts` Sprint 7, `/api/tenants` Sprint 9/11).
- Périmètre du reset (à vider / à préserver) : **tranché et implémenté** conformément à la section 3 de l'énoncé du sprint, sans écart — voir DOMAINRULES.md section 31 pour le détail modèle par modèle.
- **Décision non soumise à validation préalable, prise en cours d'implémentation et documentée a posteriori** : garde d'environnement de production (`process.env.NODE_ENV === "production"`) ajoutée au reset — ce n'était pas une demande explicite du brief de ce sprint, mais l'implémentation d'une décision de sécurité **déjà validée dès le Sprint 1** (SECURITY.md section 17 : « tout reset de données doit être techniquement impossible en environnement de production »), dont seul le mécanisme technique précis restait ouvert. Ignorer cette décision préexistante pour construire un reset utilisable en production aurait contredit un principe de sécurité déjà tranché sans nouvelle validation explicite — la garde a donc été ajoutée par prudence, **à confirmer explicitement** si le propriétaire du projet souhaite au contraire un reset utilisable même en production (aucun environnement de production ne existe à ce jour, donc sans impact pratique immédiat).
- Mécanisme de confirmation (saisie du nom du tenant) et verrou anti-double-exécution : **décisions d'ingénierie non soumises à validation préalable**, l'énoncé du sprint laissait le mécanisme exact au choix de l'implémentation (« par exemple via saisie d'un mot de confirmation ou double validation »). Verrou en mémoire de process explicitement documenté comme suffisant seulement tant qu'un seul process sert l'application — **à reconfirmer si un déploiement multi-instance est un jour décidé** (aucun hébergeur n'est tranché à ce jour, point 6).
- Harmonisation UI des tableaux : **tranchée et implémentée** au niveau du composant partagé `DataTable.tsx` plutôt que page par page, conformément à l'esprit de l'énoncé (« corriger cela partout »/« harmoniser »). Portée volontairement limitée aux incohérences concrètement vérifiables dans le code existant (alignement de la colonne actions, colonnes monétaires/numériques non alignées à droite) plutôt qu'une refonte visuelle plus large non spécifiée par l'énoncé — voir DOMAINRULES.md section 27 (addendum).

Décisions techniques encore ouvertes : voir section 8.

## 7. Risques identifiés

- ~~**Résolution du tenant à la connexion ambiguë**~~ **Résolu (Sprint 9)** : `resolveLoginTenants` vérifie le mot de passe contre tous les users partageant l'email avant de révéler la liste des tenants ; `/api/auth/login` renvoie une sélection explicite si plusieurs tenants correspondent (Option B) — voir [SECURITY.md](./SECURITY.md) section 3 et section 8 point 16 (tranché).
- **Sessions JWT non révocables côté serveur** : la déconnexion efface le cookie mais un jeton déjà émis reste valide jusqu'à son expiration (pas de table de sessions consultée à chaque requête). `session.maxAge` est désormais configuré explicitement (30 jours, 1 jour si « se souvenir de moi » décoché, Sprint 9), ce qui borne ce risque sans le supprimer — à réévaluer si un besoin de révocation immédiate apparaît (ex. compromission de compte) — voir [SECURITY.md](./SECURITY.md) section 5.
- ~~**Audit non exhaustif (Sprint 9)**~~ **Résolu (Sprint 10)** : `AuditLog` est désormais câblé sur tout le CRUD métier (véhicules, locations, clients, factures, paiements, maintenances, alertes), en plus des actions Sprint 9 (rôle/suppression user, invitations) — voir section 6 et section 8 point 33 (tranché).
- ~~**Garde « dernier ADMIN » étendue au-delà du self (Sprint 9)**~~ **Confirmée (Sprint 10)** : la garde ne dépend structurellement que du nombre d'ADMIN restants, jamais de l'identité de l'acteur — voir section 6 et section 8 point 34 (tranché).
- ~~**Session JWT non rafraîchie après édition de profil (Sprint 10)**~~ **Résolu (Sprint 11)** : `useSession().update()` + trigger `"update"` du callback `jwt()` rafraîchissent désormais `name`/`email` immédiatement après un `PATCH /api/users/me` réussi, sans reconnexion — voir section 6 et section 8 point 35 (tranché).
- **Invitations sans email (Sprint 9)** : le lien d'invitation doit être partagé manuellement par l'ADMIN (copier/coller) — pas de garantie que l'invité le reçoive par un canal fiable ; risque UX plus que sécurité, mais à traiter si le volume d'invitations augmente (voir section 8, point 30).
- **Aucune limitation des tentatives de connexion** : `/api/auth/login` n'implémente aucun rate-limiting — risque de brute force, à traiter avant mise en production (voir [SECURITY.md](./SECURITY.md) section 3).
- **Aucune stratégie de gestion des cautions définie** : à trancher avant tout développement du module cautions (voir DOMAINRULES.md section 11). Le module paiements existe désormais mais reste manuel uniquement (aucun prestataire de paiement intégré, décision explicite Sprint 6).
- **Devise `"MAD"` codée en dur par défaut (Sprint 5, changée depuis `"EUR"`)** : `Vehicle`/`Location` ont un champ `currency` mais toute création actuelle utilise `"MAD"` par défaut faute de décision multi-devises tranchée (voir section 8, point 3) — à revoir avant d'onboarder un tenant hors zone MAD (le champ existe justement pour permettre EUR, USD, etc. sans nouvelle migration). `Invoice`/`Payment` héritent de la même devise par dérivation (jamais fournie par le client) ; `getRevenueReport` (Sprint 6) suppose une devise unique par tenant pour son agrégation.
- ~~**Granularité de rôle véhicules/locations non validée formellement**~~ **Confirmée par continuité (Sprint 11)** : comportement stable et testé depuis le Sprint 5, reconduit explicitement — voir section 6 et section 8 point 21 (tranché).
- **Numérotation de facture non garantie sous forte concurrence** (Sprint 6) : `generateInvoiceNumber` compte les factures existantes plutôt que d'utiliser une séquence PostgreSQL dédiée ; en cas de collision, `createInvoice` réessaie (jusqu'à 5 fois) mais ne garantit pas une absence totale d'échec sous charge concurrente élevée sur un même tenant — acceptable pour ce sprint (volume de facturation manuel, un utilisateur à la fois en pratique), à réévaluer si le volume augmente (voir DOMAINRULES.md section 17).
- ~~**Accès aux rapports financiers restreint à ADMIN sans validation préalable**~~ **Confirmé par continuité (Sprint 11)** : comportement stable et testé depuis le Sprint 6, reconduit explicitement — voir section 6 et section 8 point 24 (tranché).
- **Génération d'alertes non automatisée** (Sprint 7) : `POST /api/tasks/check-alerts` doit être déclenché manuellement par un ADMIN (ou par un cron externe appelant la route avec une session ADMIN valide) — aucun scheduler interne (ex. `node-cron`) n'a été ajouté (pas de dépendance supplémentaire non validée, CLAUDE.md section 7), donc les alertes ne sont générées qu'au moment où la route est appelée, pas en continu. À traiter avant mise en production si des alertes en quasi temps réel sont attendues.
- ~~**Accès à `check-alerts` réservé ADMIN et scopé au tenant déclencheur sans validation préalable**~~ **Confirmé par continuité (Sprint 11)** : comportement stable et testé depuis le Sprint 7, reconduit explicitement — voir section 6 et section 8 point 29 (tranché). Reste vrai (non-résolu) : sans rôle « superadmin » transverse, un futur cron multi-tenant réel nécessiterait un mécanisme d'authentification de service dédié, non conçu à ce stade.
- **Nouveau risque identifié et corrigé (Sprint 11)** : `POST /api/tenants` permettait à tout ADMIN authentifié de créer des `Tenant` orphelins (données non rattachées à aucun user, jamais nettoyables via l'API) — voir section 6 et section 8 point 36 (tranché, route et page retirées).

## 8. Points à valider avec le propriétaire du projet

Les points suivants restent explicitement **À DÉCIDER**. Détail dans [DOMAINRULES.md](./DOMAINRULES.md), [ARCHITECTURE.md](./ARCHITECTURE.md) et [SECURITY.md](./SECURITY.md) :

### Tableau de suivi (Sprint 11)

Classification de chaque point ci-dessous. Statut : **FAIT** (tranché et implémenté), **EN COURS** (progresse sprint après sprint, jamais figé en une fois), **REPORTÉ** (décision produit/infra hors périmètre des sprints de développement en cours, ne bloque pas la suite). Priorité : impact estimé si le point reste ouvert. Sprint cible : `—` si déjà fait, `11` si tranché ce sprint, `12+` si un futur sprint de développement peut raisonnablement l'adresser, `hors scope` si la décision précède un déploiement réel (hébergement, juridique) et ne dépend d'aucun sprint de développement.

| Point | Description courte | Statut | Priorité | Sprint cible |
|---|---|---|---|---|
| 1 | MFA (fournisseur d'auth tranché, MFA reste ouvert) | REPORTÉ | BASSE | hors scope MVP |
| 2 | Granularité des rôles au-delà d'ADMIN/MEMBER | REPORTÉ | MOYENNE | 12+ |
| 3 | Devise initiale et multi-devises | REPORTÉ | BASSE | hors scope MVP |
| 4 | Règles d'arrondi financier | REPORTÉ | MOYENNE | 12+ |
| 5 | Modalités des cautions | REPORTÉ | HAUTE | 12+ |
| 6 | Hébergeur et gestionnaire de secrets prod | REPORTÉ | HAUTE | hors scope (avant déploiement) |
| 7 | Stratégie de sauvegarde | REPORTÉ | HAUTE | hors scope (avant déploiement) |
| 8 | Outil de test de charge | REPORTÉ | BASSE | hors scope MVP |
| 9 | Périmètre exact du MVP | EN COURS | MOYENNE | 11 (affiné ce sprint) |
| 10 | Juridiction cible et RGPD | REPORTÉ | HAUTE | hors scope (avant déploiement) |
| 11 | Emplacement des dossiers serveur | FAIT (de facto : `src/lib` + `src/app/api`) | BASSE | — |
| 12 | Formats et périmètre des exports/imports | REPORTÉ | BASSE | 12+ |
| 13 | Durée de conservation des journaux d'audit | REPORTÉ | MOYENNE | 12+ |
| 14 | Fuseau horaire par agence | REPORTÉ | BASSE | 12+ |
| 15 | Granularité des rôles tenant vs agence | REPORTÉ | MOYENNE | 12+ |
| 16 | Résolution tenant à la connexion / pas de superadmin | FAIT (Sprint 9) | — | — |
| 17 | Flux d'invitation | FAIT (Sprint 9) | — | — |
| 18 | Durée de session / politique mot de passe | FAIT (Sprint 9) | — | — |
| 19 | `PATCH`/`DELETE /api/users/[id]` | FAIT (Sprint 9) | — | — |
| 20 | Profil utilisateur (`GET`/`PATCH /api/users/me`) + rafraîchissement de session | FAIT (Sprint 10 + Sprint 11) | — | — |
| 21 | `MEMBER` peut créer/modifier/supprimer véhicules/locations de son agence | FAIT — confirmé par continuité (Sprint 11) | HAUTE | — |
| 22 | Devise(s) réellement supportée(s) en prod (`MAD` codé en dur) | REPORTÉ | BASSE | 12+ |
| 23 | Module `Client` : fusion de doublons, documents d'identité | REPORTÉ | MOYENNE | 12+ |
| 24 | Rapports financiers réservés `ADMIN` | FAIT — confirmé par continuité (Sprint 11) | HAUTE | — |
| 25 | Une `Location` peut-elle avoir plusieurs `Invoice` ? | REPORTÉ | BASSE | 12+ |
| 26 | Remise en pourcentage + arrondi financier | REPORTÉ | MOYENNE | 12+ |
| 27 | Contenu légal détaillé de facture | REPORTÉ | HAUTE | hors scope (juridique, avant déploiement) |
| 28 | Alertes in-app uniquement vs email | REPORTÉ (in-app confirmé pour l'instant) | BASSE | 12+ |
| 29 | `check-alerts` réservé `ADMIN`, scopé au tenant déclencheur | FAIT — confirmé par continuité (Sprint 11) | HAUTE | — |
| 30 | Prestataire SMTP transactionnel | REPORTÉ | BASSE | 12+ |
| 31 | Transition `Maintenance` `SCHEDULED → COMPLETED` directe | FAIT (Sprint 7, comportement stable) | — | — |
| 32 | Récurrence automatique de maintenance | REPORTÉ | MOYENNE | 12+ |
| 33 | Périmètre de l'audit (exhaustif) | FAIT (Sprint 10) | — | — |
| 34 | Garde « dernier ADMIN » en action tiers | FAIT (Sprint 10) | — | — |
| 35 | Rafraîchissement de la session JWT après édition de profil | FAIT (Sprint 11) | — | — |
| 36 | `POST /api/tenants` créait des tenants orphelins (écart fonctionnel) | FAIT — route et page retirées (Sprint 11) | — | — |
| 37 | Reset de données : mécanisme de garde de production (`NODE_ENV`) et verrou en mémoire de process | FAIT (Sprint 14D) — à confirmer si multi-instance | BASSE | si déploiement multi-instance décidé |

Détail narratif de chaque point :

1. ~~Fournisseur d'authentification et stratégie MFA.~~ Fournisseur tranché (NextAuth.js v5, Sprint 3) ; **MFA reste À DÉCIDER**.
2. Rôles et permissions détaillés au-delà de `ADMIN`/`MEMBER` (granularité par module/action).
3. Devise initiale et support multi-devises.
4. Règles d'arrondi financier.
5. ~~Prestataire de paiement.~~ Tranché (Sprint 6) : aucun, paiements enregistrés manuellement. Modalités des cautions restent **À DÉCIDER**.
6. Hébergeur précis et gestionnaire de secrets en production.
7. Stratégie détaillée de sauvegarde.
8. Outil de test de charge.
9. Périmètre exact du MVP (quels modules métier sont inclus dans la première version livrable).
10. Juridiction cible et périmètre RGPD applicable.
11. Nom et emplacement exacts des dossiers serveur (structure de code métier, distincte de `src/lib` utilisé pour la couche d'accès aux données technique).
12. Formats et périmètre détaillés des exports/imports.
13. Droits d'accès aux journaux d'audit : **tranché (Sprint 9)** pour les actions instrumentées (réservé ADMIN, tenant-scopé). Durée de conservation reste **À DÉCIDER** (aucune purge automatique). Voir aussi point 33 (périmètre d'instrumentation).
14. Modélisation précise du fuseau horaire par agence (champ `timezone` non ajouté à `Agency` à ce stade).
15. Granularité des rôles (tenant vs par agence), à trancher avant d'enrichir `UserAgency`.
16. ~~Résolution du tenant à la connexion en cas d'email dupliqué entre tenants, et existence éventuelle d'un rôle "superadmin" plateforme.~~ **Tranché (Sprint 9)** : Option B (page de sélection explicite du tenant, mot de passe vérifié avant de révéler la liste) ; pas de rôle SUPERADMIN construit (décision explicite, reconduite).
17. ~~Flux d'invitation d'un utilisateur supplémentaire dans un tenant existant.~~ **Tranché et implémenté (Sprint 9)** : `Invitation`, `/dashboard/invitations`, page publique `/invitations/[id]`, pas d'email (lien partagé manuellement) — voir section 6.
18. ~~Durée d'expiration de session et politique de complexité de mot de passe.~~ **Tranché et implémenté (Sprint 9)** : `session.maxAge` = 30 jours (1 jour si « se souvenir de moi » décoché) ; `validatePassword` (8 caractères + majuscule + chiffre + spécial).
19. ~~Conception des routes `PATCH`/`DELETE /api/users/[id]`.~~ **Tranché et implémenté (Sprint 9)** : changement de rôle, réinitialisation de mot de passe, suppression, avec garde « dernier ADMIN » — voir section 6. Granularité des rôles au-delà de `ADMIN`/`MEMBER` reste **À DÉCIDER** (point 2).
20. ~~Route de mutation du **profil utilisateur courant, par l'utilisateur lui-même** (nom, email, changement de mot de passe avec vérification de l'ancien).~~ **Tranché et implémenté (Sprint 10)** : `GET`/`PATCH /api/users/me` — voir section 6. ~~Reste À DÉCIDER : invalidation/rafraîchissement des sessions JWT existantes après un changement de mot de passe ou d'email.~~ **Tranché et implémenté (Sprint 11)** : voir point 35.
21. ~~Confirmer (ou ajuster) la décision provisoire selon laquelle un `MEMBER` rattaché à une agence peut créer/modifier/supprimer les véhicules et locations de cette agence (pas seulement les consulter).~~ **Confirmé par continuité (Sprint 11)** : comportement stable et testé sans changement ni contestation depuis son introduction (Sprint 5), reconduit explicitement — voir section 6 et section 7. Un futur module métier réutilisant ce pattern devra néanmoins re-confirmer le point 2 (granularité générale des rôles) avant extension.
22. **Nouveau (Sprint 5, mis à jour)** : devise(s) réellement supportée(s) en production (le champ `currency` existe, mais `"MAD"` est actuellement codé en dur comme valeur par défaut à la création, changé depuis `"EUR"`) — précise le point 3 ci-dessus.
23. ~~Un module `Client` à part entière (page dédiée, recherche, historique de locations) est-il nécessaire ?~~ Tranché (Sprint 8) : page `/dashboard/clients*` dédiée construite (liste + recherche, création, détail/édition, historique des locations). **Reste À DÉCIDER** : fusion de doublons, documents d'identité/permis (données personnelles sensibles, voir [SECURITY.md](./SECURITY.md) section 11) — hors périmètre du Sprint 8.
24. ~~Confirmer (ou ajuster) la décision provisoire selon laquelle les rapports financiers (`/api/reports/*`, `/dashboard/reports`) sont réservés au rôle `ADMIN`, y compris pour un `MEMBER` rattaché à toutes les agences pertinentes.~~ **Confirmé par continuité (Sprint 11)** : comportement stable et testé depuis le Sprint 6, reconduit explicitement — voir section 6 et section 7.
25. **Nouveau (Sprint 6)** : une `Location` peut-elle avoir plusieurs `Invoice` (facture partielle, avoir, facture rectificative), ou une seule facture par location est-elle la règle ? Le schéma ne l'empêche pas techniquement — voir DOMAINRULES.md section 17.
26. **Nouveau (Sprint 6)** : règles de remise en pourcentage (aujourd'hui `discountAmount` est un montant fixe, pas un pourcentage) et règles d'arrondi financier au-delà du calcul de jours de location — précise le point 4 ci-dessus.
27. **Nouveau (Sprint 6)** : contenu détaillé de facture (mentions légales, conditions générales, TVA intracommunautaire) — dépend en partie de la juridiction cible (point 10) ; gestion des remboursements/paiements négatifs, non modélisée à ce stade.
28. **Nouveau (Sprint 7)** : les alertes resteront-elles in-app uniquement, ou une intégration email (avec choix d'un prestataire SMTP) est-elle nécessaire pour un futur sprint ? Décision explicite de démarrage de sprint : in-app uniquement pour l'instant, **à reconfirmer** si le besoin d'email devient prioritaire.
29. ~~Confirmer (ou ajuster) la décision provisoire selon laquelle `POST /api/tasks/check-alerts` est réservé `ADMIN` et scopé au tenant déclencheur (pas de scan multi-tenant global).~~ **Confirmé par continuité (Sprint 11)** : comportement stable et testé depuis le Sprint 7, reconduit explicitement — voir section 6 et section 7 ; si un vrai cron périodique multi-tenant est nécessaire, un mécanisme d'authentification de service dédié devra être conçu (aucun rôle « superadmin » transverse n'existe, point 16).
30. **Nouveau (Sprint 7)** : si les notifications par email sont un jour nécessaires, quel prestataire SMTP (et quelle politique de contenu/fréquence) ? Aucune dépendance ni configuration n'existe à ce stade (précise le point 28).
31. **Nouveau (Sprint 7)** : confirmer (ou ajuster) la décision provisoire selon laquelle `SCHEDULED → COMPLETED` est autorisé directement pour une `Maintenance` (sans passer par `IN_PROGRESS`) — voir section 6. À trancher si un workflow de maintenance plus fin (ex. suivi du temps passé « en cours ») s'avère nécessaire.
32. **Nouveau (Sprint 7)** : récurrence automatique de maintenance (planification du prochain entretien après complétion d'un précédent, ex. tous les X km ou tous les Y mois) — `createMaintenanceFromSchedule` existe comme brique réutilisable mais aucun moteur de récurrence ne l'appelle ; périmètre et règles de récurrence à définir.
33. ~~`AuditLog` est-il destiné à rester limité aux actions instrumentées Sprint 9, ou un audit exhaustif de tout le CRUD métier est-il nécessaire ?~~ **Tranché (Sprint 10)** : audit exhaustif — voir section 6.
34. ~~Confirmer (ou ajuster) la décision provisoire selon laquelle la garde « dernier ADMIN » s'applique aussi aux actions d'un tiers.~~ **Confirmé (Sprint 10)** : la garde est structurellement identique en self-action et en action d'un tiers (ne dépend que du nombre d'ADMIN restants) — voir section 6.
35. ~~Rafraîchissement de la session JWT (`name`/`email`) après édition de profil.~~ **Tranché et implémenté (Sprint 11)** : `SessionProvider` + `useSession().update()` côté client (`EditProfileForm.tsx`), trigger `"update"` du callback `jwt()` relisant `name`/`email` en base par `token.id` côté serveur (`src/lib/auth.ts`) — voir section 1 (Sprint 11) et section 6.
36. **Nouveau (Sprint 11)** : `POST /api/tenants` permettait à tout ADMIN authentifié de créer un `Tenant` orphelin (aucun `User` rattaché), invisible et inaccessible ensuite (`GET`/`PATCH`/`DELETE /api/tenants/[id]` scopés à `user.tenantId`) — écart fonctionnel découvert pendant l'audit de sécurité de ce sprint, **pas** une fuite inter-tenant. **Tranché et corrigé (Sprint 11)**, validé explicitement par le propriétaire du projet : route et page `/dashboard/tenants/new` retirées, la création de tenant reste exclusive à `POST /api/auth/register` — voir section 1 et section 6.
37. **Nouveau (Sprint 14D)** : le reset de données (`/api/data-reset`) complète la décision de sécurité Sprint 1 « reset impossible en production » (SECURITY.md section 17) avec un mécanisme concret (`process.env.NODE_ENV === "production"`) et ajoute un verrou anti-double-exécution en mémoire de process — **tranché et implémenté (Sprint 14D)**, décisions d'ingénierie non soumises à validation préalable (voir section 6). **Reste à confirmer explicitement** : (a) si un reset utilisable même en production est un jour souhaité (contredirait la décision Sprint 1, actuellement sans impact pratique puisqu'aucun environnement de production n'existe) ; (b) le verrou en mémoire de process cesserait d'être suffisant si un déploiement multi-instance était un jour décidé (aucun hébergeur n'est tranché à ce jour, point 6) — un mécanisme de verrou distribué (ex. verrou consultatif PostgreSQL, Redis) devrait alors être conçu.

Framework de test : **tranché** (Vitest, voir section 2) — les outils e2e et de test de charge restent À DÉCIDER.
