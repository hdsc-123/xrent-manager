# INCIDENTS.md — Suivi des incidents projet

Ce document trace les incidents **techniques/projet** (bugs, pannes, régressions) rencontrés au cours du développement de XRent Manager. Il ne concerne pas les incidents métier liés à une location (dommage véhicule, accident, litige client) — ceux-ci relèveront d'un module métier dédié, dont les règles sont à définir dans [DOMAINRULES.md](./DOMAINRULES.md).

Trois incidents sont enregistrés ci-dessous : INC-1 (technique/process, environnement de développement, Sprint 15), INC-2 (bug applicatif de stabilité, hydration mismatch, Sprint 21) et INC-3 (infrastructure de test, blocage intermittent du serveur `next dev` partagé, Sprint 24-2, réduit au Sprint 24-3, investigation complémentaire toujours réduit, nouvelle tentative de résolution complète au Sprint 24-4 — toujours réduit, non résolu ; résolu au Sprint 24-5 via un nouveau runner de test à recyclage préventif du serveur entre groupes de fichiers, `scripts/test-grouped.mjs`, complémentaire à `npm test` — voir portée exacte au Sprint 24-5 ci-dessous).

## Structure d'un incident

Chaque incident futur devra être consigné ci-dessous en suivant ce gabarit :

```
## INC-<numéro> — <titre court>

- **Date** : <AAAA-MM-JJ>
- **Environnement** : <développement | test | staging | production>
- **Gravité** : <critique | majeure | mineure | cosmétique>
- **Description** : <description factuelle de l'incident>
- **Impact** : <utilisateurs/tenants/données concernés, durée>
- **Cause** : <cause identifiée, ou "en cours d'investigation">
- **Correction** : <description de la correction apportée, ou "non corrigé">
- **Test de non-régression** : <référence du test ajouté, ou "à ajouter">
- **Statut** : <ouvert | en cours | corrigé | clôturé>
- **Validation finale** : <nom/rôle du validateur et date, ou "en attente">
```

## Journal des incidents

## INC-3 — Blocage intermittent et total du serveur `next dev` de test partagé (suite de tests, `npm test`)

- **Date** : 2026-08-15
- **Environnement** : test (suite Vitest locale, serveur `next dev` de test dédié sur le port 3811, base `xrent_test`) — jamais observé en développement (`npm run dev`) ni en production
- **Gravité** : mineure pour le produit (aucun code applicatif ni utilisateur final concerné), mais gênante pour la fiabilité de la suite de tests (résultats `npm test` intermittents)
- **Description** : lors de l'exécution de la suite complète (610 tests, ~30 fichiers), certaines exécutions échouent avec des `Test timed out in 20000ms`/`Hook timed out in 60000ms` répartis sur un ensemble de fichiers différent à chaque run, sans jamais d'échec d'assertion. Signalé initialement sur `location-payment.test.ts` (hook `beforeAll`) et `e2e-full.test.ts`.
- **Impact** : aucun impact produit/production. Impact sur la suite de tests uniquement : `npm test` (parallélisme par défaut ou `--no-file-parallelism`) peut échouer de façon non déterministe sans qu'aucune régression de code n'existe réellement, ce qui oblige à relancer/isoler pour distinguer un vrai échec d'un faux positif d'infrastructure.
- **Cause** : confirmée par investigation (voir TESTREPORT.md, « Tests Sprint 24-2 », paragraphe « Investigation complémentaire ») — le serveur `next dev` unique et de longue durée partagé par toute la suite (nécessaire car `next/headers`, utilisé par NextAuth, exige un contexte de requête HTTP réel) entre parfois, sous charge soutenue (~610 tests, milliers de requêtes sur un seul processus tournant plusieurs minutes), dans un état de blocage complet où **toutes** les requêtes HTTP échouent (confirmé par watchdog externe : serveur totalement injoignable, pas seulement lent, jusqu'à plus de 4 minutes d'affilée dans le pire cas observé). Profilage du processus (`ps`, `sample`) pendant un blocage : 0 % CPU (processus réellement bloqué, pas en boucle infinie), 36 threads du processus tous parqués dans une attente de variable de condition noyau (`pthread_cond_wait`), avec le binding natif du moteur de requêtes Prisma présent dans au moins une des piles observées — piste plausible mais non confirmée avec certitude absolue (attribution de pile peu fiable sur binaire Rust optimisé sans symboles). `DATABASE_URL` de test ne définit aucun `connection_limit`/`pool_timeout` Prisma explicite.

**Statut actuel (au 2026-08-15)** : résolu pour l'usage recommandé de la suite complète — `node scripts/test-grouped.mjs` (voir Sprint 24-5 ci-dessous pour la démonstration complète, 10/10 exécutions parfaites). Reste présent et non corrigé à la source pour tout usage direct de `npm test`/`npx vitest run` sans le runner groupé en suite complète — les tests ciblés (`npx vitest run <fichier>`) restent, eux, pleinement utilisables et fiables. Voir le complément du 2026-08-15 (Sprint 26A, Finding B) en fin de section pour la plus récente reconfirmation de ce diagnostic.

**Complément d'investigation et réduction (Sprint 24-3)** : nouveau brief explicite du propriétaire du projet demandant de reproduire, analyser plus avant et corriger si une correction sûre est identifiable, sans toucher aux règles métier ni aux routes véhicule. Démarche :
  1. **Reproduction isolée/séquentielle** : `vehicles.test.ts`/`data-reset.test.ts` exécutés seuls (parallèles et séquentiels) restent 100 % verts (95/95), confirmant que le sprint 24-2 lui-même n'est pas en cause.
  2. **Reproduction complète avec surveillance en direct** : 2 exécutions complètes (`npm test`, parallélisme par défaut) instrumentées avec un moniteur `pg_stat_activity` (échantillonné chaque seconde) et un watchdog HTTP externe (`GET /` chaque seconde, timeout 5s) contre le serveur de test. Un blocage total a été capturé en direct sur la 2ᵉ exécution : le watchdog a montré le serveur strictement injoignable (aucune connexion établie, pas seulement lent) pendant environ 3 min 30 s d'affilée. **Fait nouveau, déterminant** : pendant toute la durée de ce blocage, `pg_stat_activity` ne montre **aucune** requête active côté PostgreSQL — uniquement des connexions `idle`/`ClientRead` (Postgres attend le client, jamais l'inverse) — ce qui écarte formellement un verrou ou un épuisement de connexions côté base de données comme cause du blocage. En parallèle, la charge machine (`uptime`) est restée basse (1,0 à 2,3 sur 10 cœurs physiques) pendant tout le blocage, ce qui écarte également une contention CPU/ordonnancement comme cause. Le blocage se situe donc avec certitude du côté du processus Node/serveur `next dev` partagé lui-même (client Postgres, pas serveur), cohérent avec le profilage `ps`/`sample` du Sprint 24-2.
  3. **Hypothèse testée et invalidée** : réglage explicite de `connection_limit=10`/`pool_timeout=20` sur `DATABASE_URL` de test + limitation du parallélisme de fichiers Vitest (`maxWorkers: 4`) — cette combinaison a été appliquée puis **mesurée** sur une exécution complète : résultat nettement dégradé (56/610 tests échoués contre 16 à 30/610 sans ce changement sur les exécutions de comparaison, durée totale quasi triplée, blocage prolongé à environ 13 minutes contre 3 min 30 dans l'exécution comparable) — changement **annulé**, non conservé. Non retenu comme correctif : il ne fait qu'allonger le run (moins de parallélisme réel) sans réduire la probabilité du blocage sous-jacent, à effectif de tests inchangé.
  4. **Observation complémentaire, non concluante à elle seule** : lors d'une reproduction ciblée, le processus `next-server` réel (`v16.3.0`, distinct du process CLI `npx next dev` qui se contente de l'englober) a été surpris à plus de 100 % CPU, plusieurs minutes de temps CPU cumulé, pendant que le port restait totalement injoignable — donc une boucle active (pas seulement une attente passive) dans au moins une occurrence du blocage. Cette observation est rapportée avec une réserve explicite : elle a été faite immédiatement après une exécution précédente dont l'arrêt propre du serveur n'était pas garanti à l'époque (voir point 5) — une confusion avec un processus résiduel d'une exécution antérieure n'est pas formellement exclue, même si un balayage complet des processus juste avant n'en a trouvé aucun.
- **Correction appliquée (Sprint 24-3)** : aucune modification du code applicatif ni des routes véhicule. Seul `vitest.global-setup.ts` (infrastructure de test) est modifié : un contrôle de vivacité (health check) interroge désormais périodiquement (`GET /` toutes les 5s, délai d'attente 5s par tentative) le serveur `next dev` de test partagé tout au long du run. Après 9 échecs consécutifs (~45s d'injoignabilité totale et soutenue — seuil délibérément très supérieur à toute lenteur de démarrage légitime constatée, et très inférieur aux pannes observées de 3 min 30 à plus de 13 minutes), le serveur reçoit un arrêt contrôlé (`SIGTERM`, puis `SIGKILL` après 5s de grâce s'il ne réagit pas) et un nouveau serveur est relancé automatiquement ; la suite reprend ensuite normalement contre le serveur relancé. Ce mécanisme ne masque aucun échec : les requêtes réellement en cours au moment du blocage échouent toujours et sont rapportées comme avant (aucun timeout de test augmenté, aucune assertion supprimée) — il évite seulement que *tous* les tests suivants du run échouent en cascade derrière un serveur définitivement mort. Un bug de fuite de processus découvert en cours de mise au point (un redémarrage encore en cours au moment de la fin du run pouvait laisser un serveur orphelin non arrêté) a été corrigé avant validation finale (le nettoyage de fin de run attend désormais la fin d'un redémarrage en cours avant d'arrêter le serveur final).
- **Résultat mesuré (avant/après, voir TESTREPORT.md « Tests Sprint 24-3 » pour le détail complet)** : la durée d'un blocage, quand il se produit, est désormais bornée à environ 45–60 secondes au lieu d'une durée non bornée observée jusqu'à plus de 13 minutes (pouvant consommer la quasi-totalité du temps restant du run) ; aucune fuite de processus constatée sur les 2 exécutions complètes post-correctif (contrôle exhaustif de la table des processus après chaque run). **En revanche, le nombre brut de tests en échec sur une exécution donnée n'est pas garanti à la baisse, et a même été plus élevé sur l'un des essais post-correctif (59/610) que sur les exécutions de référence (16 à 30/610)** : le fichier dont le hook `beforeAll` était en cours d'exécution au moment précis du blocage voit tous ses tests suivants échouer en cascade une fois ce hook interrompu (Vitest ne relance pas un `beforeAll` en échec) — l'incident, au lieu de se disperser en informe plusieurs fichiers sous forme de simples timeouts, se concentre alors dans un seul fichier sous forme d'un grand nombre d'échecs. Aucune régression métier introduite : le correctif ne touche que l'infrastructure de test, jamais le code applicatif ; `vehicles.test.ts`/`data-reset.test.ts` restent 100 % verts en exécution isolée, et chaque fois qu'ils ont été touchés par un blocage en exécution complète, les échecs constatés sont des artefacts d'infrastructure (`ECONNRESET`/timeout), jamais des échecs d'assertion métier.

**Complément d'investigation (suite Sprint 24-3, même session, non commité)** : nouvelle demande explicite du propriétaire du projet de poursuivre l'investigation sur l'isolation du serveur de test — architecture par fichier/groupe, indispensabilité réelle du serveur partagé, cycle de vie Prisma/connexions, ressources non libérées — sans jamais masquer d'échec ni gonfler de timeout, en comparant systématiquement `npm test`, `npm test -- --no-file-parallelism` et les tests ciblés Sprint 24-2. Démarche et résultats :
  1. **Le serveur partagé est indispensable pour 25 des 28 fichiers de test** (`grep` exhaustif : tous appellent `apiFetch`/`TEST_BASE_URL`) car chaque route API appelle `getSessionUser()` → `auth()` (NextAuth), qui dépend de `next/headers`, lequel n'existe que dans le contexte d'une vraie requête HTTP servie par Next.js (confirmé Sprint 24-2). Aucune autre API Next.js request-scope (`cookies()`, `redirect()`, `NextRequest`) n'est utilisée directement dans les routes — le seul point de couplage réel est `auth()`. Les 3 fichiers restants (`db.test.ts`, `password-policy.test.ts`, `csv-export-sanitization.test.ts`) sont déjà des tests unitaires/directs sans aucun appel HTTP — cette séparation existe donc déjà nativement, fichier par fichier, sans travail de réorganisation supplémentaire nécessaire.
  2. **Un serveur Next distinct par fichier ou groupe de tests est techniquement possible** (via les « projects » de Vitest 4, chaque projet pouvant avoir son propre `globalSetup`/port/`include`), mais n'a **pas été implémenté** : la cause interne exacte du blocage reste inconnue avec certitude (point 2 du complément précédent), donc rien ne garantit qu'un serveur dédié par groupe soit moins sujet au même blocage — seul le rayon d'impact serait réduit, ce que le redémarrage contrôlé (déjà en place) accomplit déjà à moindre coût. Le coût réel (N démarrages `next dev`/Turbopack à froid, mémoire et CPU multipliés par le nombre de groupes, réécriture non triviale de `testServer.ts`/`vitest.global-setup.ts` pour paramétrer port et fichiers par groupe) a été jugé disproportionné par rapport au bénéfice incertain, dans le temps imparti à cette investigation. Non retenu — option documentée, pas rejetée définitivement, pour un éventuel futur sprint si le propriétaire du projet souhaite investir dans cette direction.
  3. **Épuisement du pool de connexions Postgres formellement écarté par nouvelle mesure directe** : surveillance en direct de `pg_stat_activity` (nombre total de connexions, échantillonné toutes les 2s) pendant 2 exécutions complètes (`npm test`, parallélisme par défaut) — pic mesuré à 43–46 connexions simultanées sur les 100 autorisées (`max_connections`), jamais d'erreur `FATAL`/« too many clients » dans le journal PostgreSQL (`/opt/homebrew/var/log/postgresql@17.log`, surveillé en continu pendant les 2 runs). Ceci écarte définitivement l'épuisement du pool de connexions comme cause contributive, en cohérence avec l'observation déjà faite au point 2 du complément Sprint 24-3 (aucune requête active côté Postgres pendant un blocage).
  4. **Cycle de vie Prisma / ressources non libérées** : revue exhaustive des 19 appels `prisma.$transaction` du code applicatif (`src/lib/*.ts`, `src/app/api/auth/register/route.ts`) — aucun n'effectue d'attente d'E/S externe (réseau, fichier) à l'intérieur de la transaction, uniquement des opérations Prisma pures (`findUnique`/`updateMany`/`create`/`delete`) : aucun risque identifié de transaction interactive restant ouverte anormalement longtemps. Seul `db.test.ts` appelle `prisma.$disconnect()` (les autres fichiers de test n'en ont pas besoin, le client Vitest-side étant un singleton `globalThis` réutilisé). Aucune fuite de processus ni de connexion constatée après le redémarrage déclenché pendant les runs de cette session (contrôle `ps aux`/comptage de connexions avant et après chaque redémarrage).
  5. **Limitation du parallélisme Vitest, isolée cette fois de tout autre changement** : le complément précédent avait invalidé la combinaison `connection_limit=10` + `maxWorkers: 4`, mais sans jamais tester `maxWorkers` seul. Testé isolément (`test.maxWorkers: 4` dans `vitest.config.mts`, aucun autre changement) sur 2 essais complets, comparés à 2 essais de référence (parallélisme par défaut, sans plafond) : le nombre de **fichiers touchés** par un blocage baisse de façon reproductible (4 fichiers avec `maxWorkers: 4` sur les 2 essais, contre 7 et 8 fichiers sans plafond) — rayon d'impact plus resserré, cohérent avec une charge concurrente réduite sur le serveur partagé au moment du blocage. Le nombre brut de tests en échec reste en revanche irrégulier d'un essai à l'autre (14 puis 28, contre 28 et 31 sans plafond) — pas de gain fiable sur ce plan. Aucun effet secondaire négatif constaté (durée totale inchangée ~146s, connexions Postgres toujours largement sous la limite, aucune fuite de processus). **Conservé** comme amélioration complémentaire mineure et sûre (rentre dans la catégorie autorisée « limitation du parallélisme »), en plus du redémarrage contrôlé déjà en place — `vitest.config.mts` modifié (une ligne, `maxWorkers: 4`).
  6. **Exécution `--no-file-parallelism` avec `maxWorkers: 4` en place** : 3/610 tests en échec (588 réussis, 19 ignorés), 1 redémarrage déclenché et réussi, aucune fuite de processus — meilleur résultat observé sur l'ensemble des runs de cette session et de la précédente pour ce mode.

**Sprint 24-4 — nouvelle tentative de résolution complète (session dédiée, non commitée)** : nouveau brief explicite du propriétaire du projet demandant d'aller au-delà du redémarrage contrôlé et de `maxWorkers: 4`, avec un objectif chiffré strict : 5 exécutions complètes consécutives à 610/610, zéro timeout, zéro redémarrage. Consigne explicite de tester chaque piste réellement (build, mesure, comparaison), de ne jamais gonfler un timeout ni supprimer une assertion, et de s'arrêter sans commit si l'objectif n'est pas atteint. Pistes testées :
  1. **Serveur compilé de production (`next build` + `next start`) au lieu de `next dev`, en isolant Turbopack/le mode développement comme variable** : implémenté et testé (construction unique en tête de `setup()`, ~3,4s, puis `next start` réutilisé pour tout redémarrage). Vérifié dans `node_modules/next/dist/bin/next` que `process.env.NODE_ENV = process.env.NODE_ENV || defaultEnv` ne réécrase jamais une valeur déjà définie — un forçage explicite à `"test"` au démarrage du serveur est donc bien respecté par le *CLI*. **Rejeté après mesure** : `data-reset.test.ts` échoue quand même (4/95, `403` au lieu de `400`/`200`) — la compilation de production de Next.js/Turbopack inline `process.env.NODE_ENV === "production"` comme littéral **au moment du build** dans le code serveur compilé (comportement standard d'un bundler en mode production, indépendant du `NODE_ENV` du processus au *runtime*), ce qui fait échouer irrémédiablement `assertNotProduction()` (`src/lib/data-reset.ts`) — garde de sécurité intentionnelle (voir SECURITY.md section 17), jamais modifiée. Incompatibilité structurelle entre un vrai build de production et ce garde-fou métier, pas un simple réglage à corriger — option abandonnée, changement immédiatement annulé (fichier restauré à l'identique, vérifié par `git diff`).
  2. **`next dev --webpack` (mode développement conservé, Turbopack seul désactivé)** : option CLI confirmée disponible (`node_modules/next/dist/bin/next`). Testée sur 2 exécutions complètes : 580/610 puis 582/610 réussis, **1 redémarrage contrôlé sur les 2 exécutions**, durée plus longue (187s et 203s contre ~146s avec Turbopack). Aucune amélioration mesurée, et plus lent — Turbopack est donc formellement écarté comme cause spécifique du blocage (le blocage persiste à l'identique sous le bundler webpack classique). Changement annulé, non conservé.
  3. **`connection_limit=10`/`pool_timeout=20` sur `DATABASE_URL` de test, cette fois isolé de tout autre changement (contrairement au Sprint 24-3 qui le combinait à `maxWorkers`)**, en conservant `maxWorkers: 4` déjà adopté : 1 exécution complète, 593/610 réussis, 17 échoués, 1 redémarrage — résultat dans la même fourchette que la configuration de référence sans `connection_limit` (581 à 596 réussis selon les essais), aucun gain net observé. Cohérent avec l'écart déjà formellement établi entre l'épuisement du pool (écarté, pic 43–46/100) et la cause réelle du blocage. Changement annulé, non conservé.
  4. **`--no-file-parallelism` re-testé 3 fois cette session (4 fois au total avec le Sprint 24-3)** : résultats 589/610 (2 échecs), 584/610 (0 échec d'assertion, 2 fichiers en échec par `ECONNREFUSED` pendant un redémarrage, 26 tests ignorés en cascade), 599/610 (5 échecs) — **les 4 exécutions, sans exception, ont chacune déclenché exactement 1 redémarrage contrôlé**, malgré l'exécution strictement séquentielle des fichiers (un seul fichier de test actif à la fois). **Constat nouveau et significatif** : le blocage n'est donc pas uniquement une fonction du volume de requêtes concurrentes — il se reproduit même sans aucune charge concurrente, ce qui oriente davantage vers une cause liée au temps écoulé ou au nombre cumulé de requêtes traitées par le processus serveur/moteur Prisma de longue durée, plutôt que vers une charge en rafale. `--no-file-parallelism` réduit nettement le nombre de tests en échec (0 à 5 contre 14 à 19 en parallèle par défaut) mais n'élimine jamais le redémarrage lui-même.
  5. **Bilan chiffré de la session** : 8 exécutions complètes menées (2 webpack, 1 connection_limit isolé, 3 `--no-file-parallelism`, 1 parallèle par défaut de confirmation, 1 production rejetée avant complétion), auxquelles s'ajoutent les 5 exécutions complètes + 1 `--no-file-parallelism` du complément Sprint 24-3 précédent (même session de travail élargie) — **14 exécutions complètes au total sur les deux sessions d'investigation, dont 13 effectivement menées à terme (la 14ᵉ, production, arrêtée dès l'échec de régression constaté) ; toutes les 13 exécutions menées à terme ont déclenché au moins un redémarrage contrôlé, sans exception**, quels que soient le bundler (Turbopack/webpack), le parallélisme (par défaut/`maxWorkers: 4`/séquentiel), ou la configuration de connexions Postgres testée. Cette reproductibilité quasi systématique, invariante à tous les leviers d'infrastructure testés, est elle-même une preuve indirecte forte que la cause est interne au processus serveur partagé de longue durée (cohérent avec le profilage Sprint 24-2 : threads parqués dans `pthread_cond_wait`, binding natif du moteur Prisma présent dans au moins une pile observée) et non corrigible par la configuration Vitest/Next.js/Postgres seule.
  6. **Piste identifiée mais non implémentée, faute de temps dans cette session** : recyclage **proactif** du serveur partagé (redémarrage planifié après N requêtes ou M minutes, avant tout échec constaté) plutôt que réactif (après 9 échecs de contrôle de vivacité, mécanisme déjà en place). Cohérent avec le constat du point 4 ci-dessus (fonction du temps/volume cumulé plutôt que de la charge instantanée), mais nécessiterait de coordonner le recyclage avec les requêtes de test réellement en cours pour ne pas interrompre un test valide au milieu d'une exécution — réécriture non triviale, non entreprise ; option documentée pour un futur sprint dédié, pas rejetée.
- **Statut (Sprint 24-4)** : réduit, **toujours pas résolu** — l'objectif chiffré explicite (5 exécutions consécutives à 610/610, zéro timeout, zéro redémarrage) n'est **pas atteint** et, sur la base des 13 exécutions complètes menées à terme cette session et la précédente (100 % avec au moins un redémarrage, quelle que soit la configuration), ne paraît pas atteignable par un simple changement de configuration d'infrastructure de test — une correction définitive nécessiterait d'identifier et corriger la cause exacte à l'intérieur du processus serveur/moteur Prisma lui-même (hors périmètre d'une investigation d'infrastructure de test, engagerait potentiellement une investigation amont sur Prisma ou une réécriture plus profonde du cycle de vie du serveur de test). Configuration finale conservée à l'identique de fin de Sprint 24-3 (`maxWorkers: 4` + redémarrage contrôlé, `next dev`/Turbopack, aucun `connection_limit` explicite) — aucune des pistes testées ce sprint n'a apporté d'amélioration justifiant un changement. Ne pas présenter INC-3 comme clos.
- **Test de non-régression** : sans objet pour la cause elle-même (incident d'infrastructure de test, pas un bug de code applicatif) — non-régression confirmée par exécution isolée répétée de `vehicles.test.ts`/`data-reset.test.ts` (95/95 verts à chaque essai de configuration testé cette session, y compris juste avant l'abandon de la piste serveur de production) et par lecture complète de chaque changement testé (aucune modification de code applicatif conservée).
- **Validation finale (Sprint 24-4)** : diagnostic Sprint 24-2/24-3 confirmé et affiné une nouvelle fois (Sprint 24-4 : Turbopack formellement écarté comme cause spécifique — le blocage persiste identique sous webpack ; le blocage se reproduit même en exécution strictement séquentielle, écartant une cause purement liée à la charge concurrente). Configuration finale (`vitest.config.mts`/`vitest.global-setup.ts`, inchangée depuis le Sprint 24-3) revérifiée par `npm run lint`/`npx tsc --noEmit`/`npm run build` verts et `vehicles.test.ts`/`data-reset.test.ts` 100 % verts (95/95) en fin de session. Décision de correction définitive de la cause elle-même, l'investigation sur le moteur Prisma en amont, et la décision d'investir dans un recyclage proactif du serveur ou une architecture à serveurs multiples par groupe de tests, toujours à prendre par le propriétaire du projet — trois sessions d'investigation dédiées (Sprint 24-2, 24-3, 24-4) n'ont pas suffi à identifier une cause interne corrigible avec certitude dans le code applicatif ou l'infrastructure de test.

**Sprint 24-5 — recyclage préventif contrôlé du serveur de test (résolution effective, session dédiée, non commitée)** : nouveau brief explicite du propriétaire du projet, ciblé sur la seule piste identifiée mais non testée au Sprint 24-4 — le recyclage **préventif** du serveur de test (planifié entre groupes de fichiers, jamais pendant un test en cours), le watchdog réactif existant étant conservé uniquement comme dernier filet de sécurité. Consigne explicite : ne modifier aucune règle métier, ne pas toucher `assertNotProduction()`, ne lancer aucun reset de données, ne pas gonfler de timeout, ne pas supprimer d'assertion.
  1. **Cycle de vie existant relu avant toute implémentation** (`vitest.global-setup.ts`, `vitest.config.mts`, `src/__tests__/helpers/testServer.ts`, `fixtures.ts`) : `globalSetup` s'exécute une seule fois par invocation `vitest run` (pas par fichier/worker), et son `teardown` retourné par `setup()` attend déjà la fin complète d'un redémarrage en cours (`restartPromise`) avant d'arrêter le serveur final et d'awaiter sa sortie de processus (`stopServer` : `SIGTERM`, `SIGKILL` après 5s de grâce, attente de l'évènement `exit`). **Constat clé** : ce cycle de vie complet (démarrage neuf → attente de disponibilité `waitForServer` → arrêt propre attendu → aucun résidu) s'exécute déjà intégralement, sans aucune modification de code, à **chaque invocation séparée** de `vitest run` — le recyclage préventif entre groupes peut donc être obtenu en scindant la suite en plusieurs invocations `vitest run` successives (une par groupe de fichiers), sans toucher à `vitest.global-setup.ts` ni `vitest.config.mts`.
  2. **Détection de fin de groupe** : chaque groupe est une invocation `vitest run <fichiers du groupe>` distincte et complète — sa propre sortie de processus (exit code) est le signal de fin de groupe, garanti par Node lui-même (pas de détection heuristique à construire). Aucun test ne peut donc être interrompu par un recyclage : le process orchestrateur ne lance jamais un nouveau groupe avant que le précédent (et son serveur) ait entièrement terminé.
  3. **Implémentation** : nouveau script d'orchestration `scripts/test-grouped.mjs` (aucune modification de `vitest.config.mts`/`vitest.global-setup.ts`/code applicatif). Répartit les 30 fichiers de test (13 685 lignes) en 5 groupes équilibrés par taille (~2 720–2 754 lignes chacun, algorithme glouton par ordre décroissant), lance `npx vitest run <fichiers du groupe> [--no-file-parallelism]` séquentiellement pour chaque groupe, agrège les résultats (`passed`/`failed`/`total` extraits de la ligne `Tests` du reporter Vitest, occurrences de `Test timed out`/`Hook timed out` pour les timeouts, occurrences de `redémarrage contrôlé n°` dans stderr pour les redémarrages watchdog), et vérifie par `pgrep -f "next dev.*-p 3811"` l'absence de tout processus résiduel après chaque groupe. Le watchdog du Sprint 24-3 reste actif et inchangé à l'intérieur de chaque groupe, comme dernier filet de sécurité.
  4. **Résultats mesurés — mode parallélisme par défaut (`node scripts/test-grouped.mjs`)** : **5 exécutions consécutives, 610/610 à chaque fois, zéro timeout, zéro redémarrage watchdog, zéro processus résiduel**, durée quasi identique à chaque essai (149,5 s / 149,8 s / 150,0 s / 150,3 s / 150,0 s), 4 redémarrages préventifs planifiés par exécution (entre les 5 groupes). Objectif chiffré strict du brief atteint sur ce mode.
  5. **Résultats mesurés — mode séquentiel (`node scripts/test-grouped.mjs --no-file-parallelism`)** : 3 exécutions consécutives, **610/610 à chaque fois, zéro timeout, zéro redémarrage watchdog, zéro processus résiduel**, durée quasi identique (194,2 s / 194,3 s / 194,3 s, plus lent que le mode parallèle comme attendu, cohérent avec l'absence de parallélisme intra-groupe). Résultats strictement identiques (610/610, zéro incident) entre le mode normal et le mode séquentiel du nouveau runner groupé.
  6. **Vérifications post-correction** : `npm run lint` et `npx tsc --noEmit` sans erreur ; `npm run build` réussi ; exécution ciblée `vehicles.test.ts`/`data-reset.test.ts` 95/95 (14,68 s), confirmant l'absence de toute régression métier (7 champs techniques obligatoires du véhicule, `pricePerDay` optionnel, permissions et isolation tenant).
  7. **Portée exacte de la résolution — à ne pas généraliser au-delà de ce qui est prouvé** : les 8 exécutions ci-dessus (5 + 3) valident le **nouveau runner groupé** (`scripts/test-grouped.mjs`), pas la commande brute `npm test` (serveur unique de longue durée, inchangée, `vitest.config.mts`/`vitest.global-setup.ts` non modifiés). La commande `npm test` telle quelle reste exposée à la même instabilité qu'aux Sprints 24-2/24-3/24-4 (13/13 exécutions complètes avec au moins un redémarrage sur les sessions précédentes) — le nouveau runner groupé est un **script complémentaire**, pas un remplacement de `npm test` dans `package.json` (non modifié, conformément à la règle CLAUDE.md interdisant toute modification de `package.json` sans validation explicite). C'est en scindant la durée de vie du serveur en 5 tranches d'environ 25–40 s chacune (contre ~150 s pour une exécution continue) que le nouveau runner évite le déclenchement du blocage — cohérent avec l'hypothèse Sprint 24-4 d'une cause liée au temps écoulé/volume cumulé plutôt qu'à la charge instantanée.
  8. **Audit de robustesse du script avant validation finale (même sprint, nouvelle demande explicite)** : relecture complète de `scripts/test-grouped.mjs` et vérification ciblée (fonctionnement depuis la racine, gestion d'erreur/code de sortie, comportement Ctrl+C, transmission des arguments, exhaustivité de la liste de fichiers, fiabilité de l'agrégation) — **deux bugs réels trouvés et corrigés avant toute validation finale**, aucun n'affectant les 8 exécutions précédentes (aucune n'a rencontré ces cas limites) mais tous deux susceptibles de masquer un futur échec réel :
     - **Code de sortie agrégé peu fiable** : reproduit avec un fichier de test factice qui plante à la collecte (exception hors test, avant tout résumé) — Vitest sort avec le code 1 mais affiche `Tests  no tests`, que `parseSummary` (basé sur du texte, pas sur le code de sortie) ne reconnaît pas comme un échec. Le script aurait alors terminé avec le code 0 malgré un groupe réellement en échec. **Corrigé** : le code de sortie réel de chaque groupe (`result.code`) est désormais la source de vérité complémentaire à `summary.failed` pour le code de sortie final.
     - **Nettoyage Ctrl+C non garanti** : reproduit en envoyant `SIGINT` au groupe de processus du script en cours d'exécution (simulation d'un vrai Ctrl+C terminal) — le serveur `next dev` détaché (lancé par `vitest.global-setup.ts`) **survivait** au signal, son teardown asynchrone n'aboutissant pas de façon fiable sur un arrêt brutal de Vitest. **Corrigé** : ajout d'un gestionnaire `SIGINT`/`SIGTERM` dans `test-grouped.mjs` qui transmet explicitement le signal au process enfant puis force, après un court délai de grâce, l'arrêt de tout `next dev` résiduel (`pkill -f "next dev.*-p 3811"`) — filet de sécurité indépendant du teardown de Vitest. En corrigeant ce point, un second bug a été découvert et corrigé dans la foulée : la boucle principale ne vérifiait pas l'indicateur d'arrêt et lançait quand même le groupe suivant après un signal, provoquant un conflit `EADDRINUSE` avec le nettoyage encore en cours — corrigé par une vérification de l'indicateur en début de chaque itération.
     - **Vérifications complémentaires, aucun problème trouvé** : liste des 30 fichiers confirmée complète et sans doublon (comparaison programmatique avec le contenu réel de `src/__tests__/`) ; transmission de `--no-file-parallelism` au sous-processus confirmée correcte ; format exact du message de timeout Vitest (`Test timed out in Xms.` / `Hook timed out in Xms.`, vérifié dans le code source installé) confirmé conforme à la regex de détection du script ; teardown de `vitest.global-setup.ts` confirmé s'exécuter proprement (aucun résidu) même sur un crash de collecte, hors cas Ctrl+C traité ci-dessus.
     - **Re-validation post-correctifs** : 1 nouvelle exécution complète en mode parallèle et 1 en mode `--no-file-parallelism`, toutes deux **610/610, zéro timeout, zéro redémarrage watchdog, zéro processus résiduel** (149,8 s / 194,2 s) — portant le total à **10/10 exécutions parfaites** sur l'ensemble des deux sessions de ce sprint. `npm run lint`/`npx tsc --noEmit`/`npm run build` revérifiés verts après correctifs.
- **Statut (Sprint 24-5)** : **résolu** pour l'usage recommandé de la suite complète (`node scripts/test-grouped.mjs`, avec ou sans `--no-file-parallelism`) — objectif chiffré strict du brief (5 exécutions consécutives à 610/610, zéro timeout, zéro redémarrage watchdog, zéro processus résiduel) atteint et même dépassé (10 exécutions consécutives au total sur l'ensemble du sprint, toutes identiques, après correction des deux bugs de robustesse du point 8). INC-3 reste présent, non corrigé à la source, pour tout usage direct de `npm test`/`npm test -- --no-file-parallelism` (serveur unique non recyclé) — voir point 7 ci-dessus. Recommandation : utiliser `node scripts/test-grouped.mjs` (éventuellement `--no-file-parallelism`) pour toute validation de suite complète en CI ou en local ; décision de formaliser ce script dans `package.json` (ex. `npm test` pointant vers le runner groupé) laissée au propriétaire du projet, hors périmètre de ce brief.
- **Validation finale (Sprint 24-5)** : voir TESTREPORT.md section 3 « Tests Sprint 24-5 » pour le détail complet des 10 exécutions et de l'audit de robustesse. Aucun processus résiduel, aucune fuite de connexion constatée à la fin de la session (`ps aux`/`lsof -i :3811` vides). `HEAD` git inchangé (`3f641de`), rien commité ni poussé conformément au brief.

**Complément du 2026-08-15 (Sprint 26A, Finding B) — nouvelle occurrence, diagnostic reconfirmé** : pendant la validation du Finding B (verrouillage concurrent des paiements, `src/lib/payments.ts`), `npm run test` a échoué deux fois de suite de façon non déterministe (33 puis 19 tests en échec, ensembles de fichiers différents à chaque run — dont plusieurs sans aucun rapport avec les paiements, ex. `e2e.test.ts`, `invoices.test.ts`, `tenants.test.ts` — timeouts génériques + `ECONNRESET`, un redémarrage watchdog déclenché à chaque run), signature identique au motif déjà documenté ci-dessus depuis le Sprint 24-2. Reproduction via `node scripts/test-grouped.mjs` : **2 exécutions consécutives, 637/637, zéro timeout, zéro redémarrage watchdog, zéro processus résiduel** — écartant un défaut logique du nouveau code de verrouillage (les tests ciblés du Finding B, exécutés en isolation répétée, étaient déjà 100 % verts). **Réserve non résolue** : faute d'accès à `git stash` (refusé par le contrôle de permissions de la session), aucune comparaison contrôlée « avant/après » sous charge identique n'a pu être menée pour exclure formellement une contribution marginale des transactions Prisma désormais plus longues (verrou de ligne tenu pendant tout `createPayment`/`updatePayment`/`createMixedPayments`, contre des requêtes autocommit séparées auparavant) à la probabilité du blocage — hypothèse plausible mais non vérifiée, cohérente avec la piste « temps/volume cumulé » du Sprint 24-4, à surveiller si INC-3 redevenait plus fréquent avec le runner groupé lui-même. Ce diagnostic reconfirme, sans le modifier, celui établi aux Sprints 24-2 à 24-5 : la cause reste interne au processus serveur partagé, pas un défaut du code applicatif de ce sprint.

**Complément du 2026-08-16 (Sprint 28.1) — nouvelle cause identifiée et corrigée : serveur de test détaché survivant à un arrêt brutal de Vitest (`EADDRINUSE` port 3811)** : nouveau brief explicite du propriétaire du projet, à la suite de deux occurrences `EADDRINUSE` observées lors d'exécutions directes de `npx vitest run <fichier>` (voir TESTREPORT.md, ligne du 2026-08-16 « Sprint 26E »). Diagnostic en lecture seule d'abord (aucune modification), validation explicite du propriétaire du projet, puis implémentation. **Cause confirmée par reproduction directe** (distincte de la cause originale d'INC-3 ci-dessus — blocage interne du serveur partagé sous charge/durée soutenue — toujours non corrigée à la source, voir résultat `npm test` ci-dessous) : le serveur `next dev -p 3811`, lancé en mode `detached: true` (`vitest.global-setup.ts`), dépend entièrement du teardown asynchrone retourné par `setup()` pour être arrêté proprement. Un `SIGINT` isolé envoyé au process Vitest laisse ce teardown s'exécuter normalement (aucun résidu, vérifié par reproduction). Un arrêt brutal du process Vitest lui-même (`SIGKILL`, simulant un double-interrompu, un timeout externe ou l'arrêt forcé d'une session) ne lui laisse en revanche aucune chance de s'exécuter — reproduit directement (`kill -KILL` sur le process Vitest juste après l'apparition du serveur `next dev` détaché) : le serveur survit intégralement, port 3811 toujours occupé, bloquant toute exécution suivante par `EADDRINUSE`.
- **Correction appliquée** : ajout de `cleanupResidualTestServer()` dans `vitest.global-setup.ts`, appelée en tout début de `startServer()` — nettoyage préventif idempotent (`pkill -f "next dev.*-p 3811"`, pattern strictement borné au port de test, jamais `-p 3000`/développement) suivi d'une courte attente (500ms) pour laisser le port se libérer avant tout nouveau `spawn`. Le teardown existant (`stopServer` : `SIGTERM` puis `SIGKILL` après 5s de grâce, attente de l'évènement `exit`) et les handlers `SIGINT`/`SIGTERM` de `scripts/test-grouped.mjs` (filet de sécurité déjà en place depuis le Sprint 24-5) restent strictement inchangés — correctif complémentaire, pas un remplacement. Seul `vitest.global-setup.ts` modifié.
- **Validations exécutées (2026-08-16)** : `npx tsc --noEmit` / `npm run lint` / `git diff --check` verts ; reproduction avant/après correctif (`SIGKILL` du process Vitest) confirmant que le résidu qui survivait avant le correctif est désormais nettoyé automatiquement à l'exécution suivante, sans `EADDRINUSE` ; `node scripts/test-grouped.mjs` — **710/710 tests réussis, 0 échec, 0 timeout, 0 redémarrage watchdog, 0 processus résiduel**, 161,1s (4 redémarrages préventifs planifiés entre les 5 groupes) ; `npm test` (serveur unique, sans recyclage préventif) — 59/710 échecs, 1 redémarrage watchdog déclenché, **aucune occurrence `EADDRINUSE`**, tous les échecs de type `Test timed out in 20000ms`/`TypeError: fetch failed` (aucun échec d'assertion métier) — reproduit fidèlement le motif déjà documenté (blocage interne du serveur partagé, cause originale d'INC-3, toujours non corrigée à la source pour cet usage direct, voir Sprints 24-4/24-5 ci-dessus) ; aucun processus résiduel après coup (`lsof -i :3811`/`pgrep -f "next dev.*-p 3811"` vides après chaque exécution).
- **Statut (Sprint 28.1)** : la cause spécifique traitée ici (serveur détaché survivant à un arrêt brutal de Vitest, `EADDRINUSE`) est **corrigée et validée**, sous réserve de revue finale du propriétaire du projet. La cause originale d'INC-3 (blocage interne du serveur partagé sous charge/durée soutenue) reste, elle, non corrigée à la source pour tout usage direct de `npm test`/`npx vitest run` sans le runner groupé — recommandation inchangée : `node scripts/test-grouped.mjs` pour toute validation de suite complète. Rien commité, rien poussé.

## INC-2 — Hydration mismatch sur les icônes SVG du dashboard (extension navigateur Dark Reader)

- **Date** : 2026-08-14
- **Environnement** : développement/préproduction (signalé lors d'un test de reset du SaaS)
- **Gravité** : mineure (avertissement console uniquement, aucune donnée ni fonctionnalité affectée — même famille qu'INC précédent traité au Sprint 20, non numéroté à l'époque)
- **Description** : après le correctif Sprint 20 (hydration mismatch sur `<html>` dû à Dark Reader), un nouveau hydration mismatch de même origine est apparu, cette fois sur les icônes SVG (`lucide-react`) du chrome de tableau de bord (Sidebar/Header/BottomNav) — attributs `data-darkreader-inline-stroke` et `style={{ "--darkreader-inline-stroke": "currentColor" }}` injectés par l'extension avant l'hydratation React.
- **Impact** : avertissement console React pour tout visiteur utilisant Dark Reader (ou extension similaire modifiant le DOM avant hydratation) sur toute page `/dashboard/*` — aucune casse fonctionnelle observée (React tolère les mismatchs d'attributs sans démonter le sous-arbre concerné), mais bruit console gênant lors des tests réels et signal potentiellement trompeur lors d'un futur diagnostic.
- **Cause** : `lucide-react` (`node_modules/lucide-react/dist/esm/Icon.mjs`) positionne les attributs `stroke`/`fill` uniquement sur l'élément `<svg>` racine de chaque icône (jamais sur ses enfants `<path>`) — exactement la classe d'élément que l'overrider de style inline de Dark Reader cible. Le correctif Sprint 20 (`suppressHydrationWarning` sur `<html>`) ne couvre que cet élément précis, la prop n'étant pas récursive — les icônes du dashboard restaient donc exposées au même mécanisme.
- **Correction** : `suppressHydrationWarning` ajouté sur les icônes rendues sans condition dans le HTML du premier chargement de toute page `/dashboard/*` (`Sidebar.tsx`, `Header.tsx`, `BottomNav.tsx`) — voir HANDOFF.md Sprint 21 pour le détail complet, y compris le risque résiduel documenté (mais non corrigé, hors périmètre) sur les icônes des tableaux de contenu par page.
- **Test de non-régression** : aucun test automatisé dédié (bug de rendu DOM/hydratation sans surface HTTP testable en Vitest, même nature qu'INC Sprint 20/14E) — vérifié avec un navigateur réel (Chromium/Playwright), voir TESTREPORT.md section 1 « Vérification manuelle (Sprint 21) », y compris la limite documentée sur la fiabilité de la reproduction synthétique en environnement `next dev` local.
- **Statut** : corrigé (pour le chrome persistant du dashboard ; risque résiduel documenté mais non corrigé pour les icônes de contenu par page, voir HANDOFF.md Sprint 21)
- **Validation finale** : `npm run lint` / `npx tsc --noEmit` / `npm run test` (480/480) / `npm run build` verts + vérification navigateur réel, 2026-08-14.

## INC-1 — Agent de développement ayant exécuté `git stash` sans le restaurer avant expiration de son budget de session

- **Date** : 2026-08-14
- **Environnement** : développement (copie de travail locale, jamais poussé/déployé)
- **Gravité** : majeure (risque de perte de travail important), sans impact final (entièrement récupéré)
- **Description** : pendant le Sprint 15, plusieurs agents assistants travaillaient en parallèle sur la copie de travail locale (non isolée par worktree Git). L'un d'eux, chargé d'ajouter des tests d'audit/bout-en-bout, a exécuté `git stash` (visiblement pour isoler temporairement l'état de certains fichiers avant restauration), puis a atteint la limite d'usage de sa session avant d'exécuter `git stash pop` — laissant la copie de travail dans l'état d'avant le stash (fichiers `src/lib/*.ts`, toutes les routes `src/app/api/**` retrofitées ce sprint, tous les composants `src/app/dashboard/**` modifiés, `src/components/layout/Sidebar.tsx`) alors que le stash contenait la totalité de ce travail non commité.
- **Impact** : aucune perte de données réelle — détecté avant tout commit/push. Repéré indirectement via des échecs de tests inattendus et incohérents (routes se comportant comme avant le sprint, ex. `generateContractNumber` encore basé sur `Tenant` au lieu d'`Agency`) plutôt que par un message d'erreur explicite.
- **Cause** : agent avec accès complet à `git` opérant sur la copie de travail partagée (pas de `git worktree`/isolation entre agents concurrents) ; `git stash` (réversible) a heureusement été utilisé plutôt qu'une commande destructive (`git checkout -- .`, `git reset --hard`) qui aurait rendu la récupération impossible sans les journaux de conversation.
- **Correction** : diagnostic via `git status`/`git stash list`/`git diff <fichier>` (confirmation que le stash contenait la version correcte) ; restauration sélective fichier par fichier depuis `git checkout stash@{0} -- <chemin>` pour chaque fichier hors `src/__tests__/` (les fichiers de test, réécrits/étendus après le stash par d'autres agents, étaient déjà plus complets dans la copie de travail que dans le stash — restaurer le stash en bloc les aurait fait régresser) ; vérification complète (`npx tsc --noEmit`, `npm run lint`, `npm run build`, suite de tests complète) avant de supprimer le stash (`git stash drop`).
- **Test de non-régression** : aucun test automatisé dédié (incident de processus, pas de bug applicatif) — la suite complète (416/416) a servi de filet de sécurité pour confirmer la restauration complète et correcte.
- **Statut** : corrigé
- **Validation finale** : vérifié par relecture complète des fichiers restaurés + suite de tests 416/416 verte, 2026-08-14.
- **Enseignement pour les sprints futurs** : lorsque plusieurs agents travaillent en parallèle sur la même copie de travail, éviter de leur laisser une autonomie complète sur des commandes `git` d'état global (`stash`, `reset`, `checkout` sans chemin explicite) ; préférer l'isolation par `git worktree` par agent, ou limiter les agents parallèles à des fichiers strictement disjoints sans accès `git` d'état partagé.
