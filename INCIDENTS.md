# INCIDENTS.md — Suivi des incidents projet

Ce document trace les incidents **techniques/projet** (bugs, pannes, régressions) rencontrés au cours du développement de XRent Manager. Il ne concerne pas les incidents métier liés à une location (dommage véhicule, accident, litige client) — ceux-ci relèveront d'un module métier dédié, dont les règles sont à définir dans [DOMAINRULES.md](./DOMAINRULES.md).

Huit incidents enregistrés à ce jour : INC-1 (process, git stash non restauré, Sprint 15, corrigé) · INC-2 (hydration mismatch icônes SVG, Sprint 21, corrigé) · INC-3 (blocage intermittent du serveur `next dev` de test partagé, identifié Sprint 24-2, **corrigé et clôturé le 2026-08-26** — historique complet d'investigation archivé, voir l'entrée ci-dessous) · INC-4 (test de concurrence intermittent, Sprint 34, corrigé) · INC-5 (désynchronisation `scripts/test-grouped.mjs`, Sprint technique 4, corrigé) · INC-6 (BUG-004, visibilité agence de retour, 2026-08-26, corrigé et vérifié) · INC-7 (BUG-005, kilométrage retour non validé, 2026-08-26, corrigé et vérifié) · INC-8 (BUG-001, modale permissions, 2026-08-26, corrigé et vérifié). Version condensée depuis le 2026-08-26 — les historiques d'investigation les plus longs sont archivés sans perte dans [docs/history/](./docs/history/) (voir [docs/decisions/2026-08-26-documentation-restructuring-plan.md](./docs/decisions/2026-08-26-documentation-restructuring-plan.md)).


## Structure d'un incident

Chaque incident futur devra être consigné ci-dessous en suivant ce gabarit :

```
## INC-<numéro> — <titre court>

- **Date** : <AAAA-MM-JJ>
- **Environnement** : <développement | test | staging | production>
- **Gravité** : <critique | majeure | mineure | cosmétique>
- **Description** : <description factuelle de l'incident>
- **Impact** : <utilisateurs/tenants/données concernés, durée>
- **Cause** : <cause identifiée, ou "en cours d'investigation">
- **Correction** : <description de la correction apportée, ou "non corrigé">
- **Test de non-régression** : <référence du test ajouté, ou "à ajouter">
- **Statut** : <ouvert | en cours | corrigé | clôturé>
- **Validation finale** : <nom/rôle du validateur et date, ou "en attente">
```

## Journal des incidents

## INC-8 — BUG-001 : modale de création d'un groupe de permissions dépassant le viewport (aucun défilement interne)

- **Date** : identifié lors d'une session de campagne de validation manuelle interrompue par la limite de session (rapport jamais écrit sur disque — reconstitué et corrigé le 2026-08-26), corrigé le 2026-08-26.
- **Environnement** : développement (`xrent_dev`), reproduit et vérifié en navigateur réel (Chromium via Playwright) sur un tenant de test jetable créé pour cette seule vérification, supprimé après coup.
- **Gravité** : mineure (aucune donnée ni permission affectée — défaut d'ergonomie/accessibilité, pas un défaut de sécurité), mais bloquant en usage réel : le bouton « Créer » et plusieurs catégories de permissions devenaient inatteignables.
- **Module** : `src/app/dashboard/permission-groups/PermissionGroupsPanel.tsx` (modale de création d'un groupe de permissions).
- **Préconditions** : viewport de hauteur normale (desktop ≤ ~900px de haut, ou tablette/mobile), tenant avec le jeu complet de catégories de permissions (`src/lib/permissions.ts` — une vingtaine de catégories).
- **Données utilisées** : tenant jetable créé pour la vérification (`BUG001 Verif`, supprimé après coup — voir Correction) ; aucune donnée de la campagne RAK/CASA impliquée.
- **Scénario de reproduction** : ouvrir « Créer un groupe » sur `/dashboard/permission-groups` — la grille de permissions (une carte par catégorie, `PermissionCheckboxGrid.tsx`) dépasse largement la hauteur de la modale.
- **Résultat attendu** : en-tête, nom du groupe, la totalité des catégories de permissions (via défilement interne si nécessaire) et les boutons d'action (Annuler/Créer) restent tous accessibles, quelle que soit la hauteur du viewport.
- **Résultat observé (avant correction)** : `DialogContent` (`src/components/ui/dialog.tsx`) n'a par défaut aucune hauteur maximale ni défilement (`grid gap-4 ... p-4`, aucun `max-h`/`overflow-y-auto`) ; la modale de création de groupe n'ajoutait rien de plus (`className="sm:max-w-2xl"` uniquement) — le contenu débordait sans aucun moyen d'atteindre le bas du formulaire ni les catégories situées après la hauteur visible.
- **Cause racine** : absence de toute contrainte de hauteur/défilement sur cette modale spécifique (le composant `DialogContent` de base n'en impose pas non plus, par choix — d'autres dialogues de l'application sont plus courts et n'en ont pas besoin).
- **Correction** : `src/app/dashboard/permission-groups/PermissionGroupsPanel.tsx` seul modifié (pas `dialog.tsx`, pour ne pas changer le comportement des autres dialogues de l'application) — `DialogContent` passé en `flex max-h-[85vh] flex-col overflow-hidden`, `DialogHeader` et `DialogFooter` restent hors de la zone défilante (donc toujours visibles), seule la zone `Nom + grille de permissions` (`flex min-h-0 flex-1 flex-col gap-4 overflow-y-auto`) défile en interne. Aucune règle de permission modifiée, aucune catégorie retirée.
- **Fichiers modifiés** : `src/app/dashboard/permission-groups/PermissionGroupsPanel.tsx`.
- **Tests ajoutés** : aucun test Vitest automatisé dédié (bug de rendu CSS/scroll pur, sans surface HTTP testable — même famille qu'INC-2). Vérification manuelle en navigateur réel documentée ci-dessous.
- **Tests exécutés** : vérification manuelle Playwright (navigateur réel Chromium) sur un tenant jetable — modale ouverte à 1280×800 (desktop) : en-tête et bouton de fermeture visibles, formulaire scrollable indépendamment ; défilement programmatique jusqu'au bas du conteneur interne confirmé (`scrollHeight` 2410px contre `clientHeight` 523px) avec la dernière catégorie (« Déplacements ») et le footer (Annuler/Créer) simultanément visibles ; une permission de la première catégorie (« Agences » → « Voir les agences ») et une de la dernière catégorie (« Déplacements » → « Voir les bons de déplacement ») cochées puis le formulaire soumis avec succès (groupe créé, 2 permissions) ; fiche du groupe rouverte après création — les deux permissions cochées confirmées persistées ; répété à 390×844 (mobile) et 834×1112 (tablette), modale intégralement contenue dans le viewport sans débordement dans les deux cas. `npx tsc --noEmit`/`npm run lint`/`npm run build` verts après correction ; suite complète (`node scripts/test-grouped.mjs`) 1293/1293 verte (aucune régression, ce module n'a pas de test Vitest dédié).
- **Résultat après correction** : conforme au résultat attendu sur les trois tailles de viewport testées.
- **Risques résiduels** : aucun test de non-régression automatisé pour ce défaut précis (CSS/scroll) — une régression future ne serait détectée qu'en vérification manuelle. D'autres dialogues à contenu potentiellement long (non audités dans cette session, hors périmètre du bug signalé) pourraient présenter le même défaut si leur contenu venait à grandir.
- **Statut** : Corrigé et vérifié.
- **Validation finale** : vérifié par la séquence de tests manuels ci-dessus (2026-08-26) ; en attente de validation du propriétaire du projet sur ce rapport.

## INC-7 — BUG-005 : kilométrage de retour non validé sur le flux générique PATCH /api/locations/[id]

- **Date** : identifié et corrigé le 2026-08-26 (campagne de validation, session de reprise après interruption par limite de session).
- **Environnement** : développement/test (`xrent_dev` pour la reconstitution des données, `xrent_test` pour les tests automatisés).
- **Gravité** : mineure à modérée (aucune perte de données, mais une agence de retour sans accès à l'agence de départ pouvait enregistrer un kilométrage de retour incohérent — inférieur, égal, négatif ou non numérique — sur le seul flux qui lui est ouvert).
- **Module** : `src/lib/locations.ts` (`updateLocation`), `src/app/api/locations/[id]/route.ts` (PATCH), utilisé notamment par `LocationActions.tsx` (panneau « Réception du véhicule », agence de retour seule).
- **Préconditions** : un contrat `ACTIVE` avec un `startOdometer` connu ; un utilisateur ayant uniquement accès à l'agence de retour (`dropoffAgencyId`), donc restreint par construction au flux PATCH générique plutôt qu'à `POST /api/locations/[id]/return` (voir `canManageReturnOnly`, `src/lib/authz.ts`).
- **Données utilisées** : véhicule/contrat de test dédiés créés dans `xrent_test` par la suite automatisée (voir Test de non-régression) ; aucune donnée de la campagne RAK/CASA modifiée.
- **Scénario de reproduction** : `PATCH /api/locations/[id]` avec `{ "endOdometer": <valeur inférieure ou égale à startOdometer> }`.
- **Résultat attendu** : `Le kilométrage de retour doit être supérieur au kilométrage de départ.` (HTTP 400), aucune écriture partielle.
- **Résultat observé (avant correction)** : requête acceptée (HTTP 200), `endOdometer` enregistré tel quel en base sans aucune comparaison à `startOdometer` — seul le flux dédié `POST /api/locations/[id]/return` (`src/lib/location-return.ts`, `assertValidOdometer`) appliquait déjà cette règle.
- **Cause racine** : `updateLocation()` (`src/lib/locations.ts`) validait le niveau de carburant (`validateFuelLevel`) mais n'avait jamais reçu l'équivalent pour `endOdometer` ; la validation générique côté route (`PATCH /api/locations/[id]/route.ts`) ne vérifiait qu'un entier positif ou nul, sans comparaison au départ.
- **Correction** : nouvelle classe `InvalidReturnOdometerError` et fonction `assertValidReturnOdometer()` ajoutées dans `src/lib/locations.ts` (dupliquées depuis la même règle que `location-return.ts` plutôt qu'importées — `location-return.ts` dépend transitivement de `locations.ts` via `invoices.ts`, une importation créerait un cycle ; même raisonnement déjà appliqué à `InvalidFuelLevelError`), appelée dans `updateLocation()` dès que `endOdometer` est fourni et non nul, comparée au `startOdometer` résultant (celui fourni dans le même appel s'il change, sinon celui déjà enregistré). Message d'erreur clair, mappé en HTTP 400 dans `src/app/api/locations/[id]/route.ts`. Aucune modification partielle possible (la validation intervient avant toute écriture). Client (`LocationActions.tsx`) complété par un attribut `min` et un message d'erreur immédiat sur les deux champs kilométrage retour (panneau « Réception du véhicule » et formulaire principal), en plus de la validation serveur — jamais à sa place.
- **Fichiers modifiés** : `src/lib/locations.ts`, `src/app/api/locations/[id]/route.ts`, `src/app/dashboard/locations/[id]/LocationActions.tsx`, `src/app/dashboard/locations/[id]/page.tsx`, `src/__tests__/locations.test.ts`.
- **Tests ajoutés** : `src/__tests__/locations.test.ts`, describe « BUG-005 — kilométrage retour doit être strictement supérieur au départ » (7 tests) — kilométrage retour inférieur (rejeté 400, message exact vérifié), égal (rejeté), valeur négative (rejetée), valeur non numérique (rejetée), champ non fourni (accepté, no-op), valeur strictement supérieure (acceptée, persistée en base), et absence de modification partielle en cas de rejet (aucun champ, y compris `notes` envoyée dans la même requête, n'est écrit).
- **Tests exécutés** : fichier isolé (100/100) puis suite complète (`node scripts/test-grouped.mjs`, 1293/1293, 0 échec/timeout/résiduel) ; `npx tsc --noEmit`/`npm run lint`/`npm run build` verts. **Confirmé en direct en navigateur réel** sur le contrat n°00002 réel de la campagne (`qa.agent.casa@fictif.test`) : `endOdometer=100` (< départ) rejeté par le serveur (`PATCH .../cmta4o81l00f5m4n9dy16fif1` → 400, message affiché côté client) sans aucune modification enregistrée ; `endOdometer=28450` accepté (200), contrat passé Terminée, kilométrage/carburant persistés après rechargement — voir docs/test-reports/2026-08-26-bug-001-004-005.md.
- **Résultat après correction** : conforme au résultat attendu sur les 7 scénarios testés.
- **Risques résiduels** : aucun identifié — la règle est désormais appliquée aux deux flux (`POST .../return` et PATCH générique) avec le même message.
- **Statut** : Corrigé et vérifié.
- **Validation finale** : vérifié par la suite automatisée ci-dessus (2026-08-26) ; en attente de validation du propriétaire du projet sur ce rapport.

## INC-6 — BUG-004 : une location visible seulement par son agence de départ, jamais par son agence de retour (dropoffAgencyId), dans les listes/exports

- **Date** : identifié et corrigé le 2026-08-26 (campagne de validation, session de reprise après interruption par limite de session — le rapport de la session précédente ayant identifié ce défaut n'avait jamais été écrit sur disque ; confirmé par relecture directe du code et des données réelles de `xrent_dev`, pas seulement par la description transmise).
- **Environnement** : développement (`xrent_dev`, tenant de campagne réel avec agences RAK/CASA) et test (`xrent_test`, suite automatisée).
- **Gravité** : modérée (défaut de visibilité fonctionnelle affectant le flux transfert d'agence RAK→CASA — pas une fuite de données inter-tenant, mais un accès légitime refusé à tort à l'agence de retour).
- **Module** : `src/app/api/locations/route.ts` (liste), `src/app/dashboard/locations/page.tsx` (liste dashboard), `src/lib/exports.ts` (export CSV des contrats), `src/lib/authz.ts`/`src/lib/locations.ts` (règle de visibilité).
- **Préconditions** : contrat créé à l'agence de départ RAK, agence de retour CASA distincte renseignée (`Location.dropoffAgencyId`) ; agent rattaché uniquement à CASA (`UserAgency`), sans accès à RAK.
- **Données utilisées** : confirmé sur les données réelles de `xrent_dev` — contrat n°00002 (id `cmta4o81l00f5m4n9dy16fif1`), agence de départ RAK (id `cmta2z7xo006am4n9nvq8xx94`), agence de retour CASA (id `cmta2zslq006em4n9hxg43g8p`), tenant `cmta2y6uy0000m4n9xvm0hr18`, agents `qa.agent.rak@fictif.test` et `qa.agent.casa@fictif.test` (rattachements `UserAgency` confirmés un-à-un, chacun sur sa seule agence).
- **Scénario de reproduction** : se connecter en tant qu'utilisateur rattaché uniquement à CASA, ouvrir `GET /api/locations` (ou `/dashboard/locations`) — le contrat n°00002 est absent de la liste bien que `GET /api/locations/[id]` (fiche détail) le montre correctement (celui-ci utilisait déjà `canAccessLocationAgency`, correct depuis le Sprint 19).
- **Résultat attendu** : le contrat apparaît dans la liste de l'agent RAK (départ) **et** de l'agent CASA (retour), jamais pour un agent sans rattachement à l'une des deux agences ni pour un autre tenant ; l'accès direct par identifiant et les actions disponibles (réception du véhicule) restent cohérents avec les permissions du rôle.
- **Résultat observé (avant correction)** : `GET /api/locations` (`src/app/api/locations/route.ts`), la liste `/dashboard/locations` et l'export CSV des contrats (`src/lib/exports.ts`, dont le commentaire documentait explicitement cette portée trop étroite comme « fidélité délibérée » à la route liste) ne filtraient que sur `Location.agencyId`, jamais sur `dropoffAgencyId` — contrairement à `canAccessLocationAgency` (`src/lib/authz.ts`, Sprint 19), utilisée uniquement par les routes de détail/action sur une location.
- **Cause racine** : la règle de visibilité « agence de départ OU agence de retour » (Sprint 19, DOMAINRULES.md section 37) n'avait été appliquée qu'aux routes portant sur une seule location (détail, PATCH, retour) ; chaque liste/export avait reconstruit indépendamment un filtre plus étroit (`agencyId` seul), sans règle centralisée partagée — trois implémentations divergentes de la même intention.
- **Correction** : nouvelle fonction centralisée `locationAgencyScopeWhere(accessibleAgencyIds)` (fragment Prisma `OR: [{agencyId}, {dropoffAgencyId}]` ou `null`=ADMIN), définie dans `src/lib/locations.ts` (et non dans `src/lib/authz.ts` malgré la parenté avec `canAccessLocationAgency` : `authz.ts` importe `@/lib/auth`/`next-auth`, et la placer là aurait forcé tout module qui importe `@/lib/locations` — y compris les tests unitaires qui l'importent directement, hors serveur `next dev` — à charger `next-auth` transitivement, ce qui casse la résolution ESM de `next-auth/lib/env.js` hors du runtime Next.js ; découvert et corrigé pendant cette session, voir Fichiers modifiés). Adoptée par `getLocations()` (nouveau filtre `accessibleAgencyIds` sur `LocationFilters`, utilisé par `GET /api/locations`), `getContractsOverview()` (refactorée pour réutiliser la même fonction plutôt qu'une clause `OR` dupliquée), `/dashboard/locations` (liste), le widget « Retours » du dashboard (refactorée, comportement déjà correct, désormais partagé) et l'export CSV des contrats (`runLocationsExport`, dont le commentaire obsolète a été corrigé). Filtre explicite `agencyId` (paramètre d'URL, déjà vérifié par `canAccessAgency`) toujours prioritaire, comportement inchangé. Aucune règle de permission ni d'isolation tenant modifiée — `tenantId` reste systématiquement appliqué en amont dans chaque requête.
- **Fichiers modifiés** : `src/lib/authz.ts`, `src/lib/locations.ts`, `src/app/api/locations/route.ts`, `src/app/dashboard/locations/page.tsx`, `src/app/dashboard/page.tsx`, `src/lib/exports.ts`, `src/__tests__/locations.test.ts`.
- **Tests ajoutés** : `src/__tests__/locations.test.ts`, describe « BUG-004 — visibilité d'une location par l'agence de RETOUR (dropoffAgencyId) » (6 tests, tenant/agences/agents dédiés reproduisant exactement le scénario RAK→CASA) : agent de l'agence de départ voit le contrat (liste + fiche) ; agent de l'agence de retour le voit désormais (liste + fiche) ; agent de retour peut enregistrer la réception (`endOdometer`) mais reste refusé sur un champ hors périmètre (`notes`, HTTP 403) ; un agent sans rattachement à aucune des deux agences ne le voit pas (liste : absent : fiche : 404) — pas d'accès général élargi ; un tenant différent ne le voit jamais (isolation confirmée, liste et accès direct) ; un ADMIN du même tenant le voit toujours.
- **Tests exécutés** : fichier isolé (93/93 avant l'ajout des tests BUG-005, 100/100 après) puis suite complète (`node scripts/test-grouped.mjs`, 1293/1293, 0 échec/timeout/résiduel) ; `npx tsc --noEmit`/`npm run lint`/`npm run build` verts. **Confirmé en direct en navigateur réel** sur les comptes et le contrat n°00002 réels de la campagne (`qa.agent.rak@fictif.test`, `qa.agent.casa@fictif.test`) : agent RAK voit les contrats 00001/00002 avec un sélecteur véhicule limité à ses 5 véhicules ; agent CASA voit désormais aussi le contrat 00002 (liste et fiche, « Agence : RAK (retour : CASA) »), avec un sélecteur véhicule limité à ses 2 véhicules — reproduction exacte du bug signalé sur la donnée réelle, pas seulement sur des fixtures de test synthétiques. Voir docs/test-reports/2026-08-26-bug-001-004-005.md.
- **Résultat après correction** : conforme au résultat attendu sur les 6 scénarios testés, y compris l'isolation tenant et l'accès direct par identifiant.
- **Risques résiduels** : aucun identifié — la même fonction centralisée est désormais utilisée par toutes les listes/exports connus portant sur `Location`. Un futur endpoint listant des locations devra explicitement réutiliser `locationAgencyScopeWhere` plutôt que reconstruire un filtre.
- **Statut** : Corrigé et vérifié.
- **Validation finale** : vérifié par la suite automatisée ci-dessus (2026-08-26) ; en attente de validation du propriétaire du projet sur ce rapport.

## INC-5 — `scripts/test-grouped.mjs` désynchronisé de `src/__tests__/`, 36 tests jamais exécutés par le runner recommandé (identifié et corrigé Sprint technique 4)

- **Date** : identifié et corrigé à la source 2026-08-24 (Sprint technique 4, phase d'audit)
- **Environnement** : test (infrastructure/outillage de test, aucun code applicatif ni donnée réelle concernée)
- **Gravité** : mineure (aucun impact utilisateur/tenant — les 36 tests concernés passaient bien en exécution directe `npx vitest run`, jamais désactivés ni cassés ; l'incident est une désynchronisation documentaire/outillage, pas une régression fonctionnelle)
- **Description** : `scripts/test-grouped.mjs` (désigné dans ce document et dans TESTREPORT.md comme la commande recommandée pour valider la « suite complète ») maintient un tableau `GROUPS` listant explicitement chaque fichier de `src/__tests__/` à exécuter. Ce tableau n'avait jamais été mis à jour lors de l'ajout de 4 fichiers de test (36 tests au total) introduits à des sprints différents : `credit-notes-ui.test.tsx` (sous-phase 2c2-D), `damage-invoices-route.test.ts` et `damage-invoices-ui.test.tsx` (Sprint 33), `icon-hydration.test.tsx` (Sprint 34 étape 1, complément INC-2). Conséquence : chaque exécution de `node scripts/test-grouped.mjs` depuis l'introduction de ces fichiers a silencieusement ignoré 36 tests, tout en rapportant un résultat « N/N, 0 échec » présenté comme la suite complète — y compris dans les entrées « Suite complète » consignées dans TESTREPORT.md pour plusieurs sprints ultérieurs.
- **Impact** : aucun impact produit (les 36 tests étaient et sont restés fonctionnels, exécutés normalement par `npx vitest run` sans groupement). Impact uniquement sur la fiabilité des affirmations « suite complète validée » de ce projet — un audit de préparation de sprint aurait pu (comme celui-ci l'a fait) découvrir cet écart, mais rien dans l'outillage ne le détectait automatiquement avant cette correction.
- **Cause** : absence de tout mécanisme reliant `GROUPS` à la liste réelle des fichiers de `src/__tests__/` — l'ajout d'un nouveau fichier de test à ce répertoire ne déclenchait aucune vérification, aucun avertissement, ni aucune obligation de mettre à jour `scripts/test-grouped.mjs` en conséquence. Une note historique du script (Sprint 24-5) affirmait qu'une vérification de cohérence avait été faite une fois, mais rien ne la maintenait dans la durée à mesure que de nouveaux fichiers de test étaient ajoutés.
- **Correction** : (1) les 4 fichiers manquants ajoutés à `GROUPS` (répartis dans les groupes 3 et 4 par thème) ; (2) nouvelle fonction pure `computeGroupsDrift()` et nouveau garde-fou `verifyGroupsMatchDirectory()` dans `scripts/test-grouped.mjs`, appelé en tout premier dans `main()` avant tout lancement de groupe — fait échouer immédiatement le script (avec le détail des fichiers manquants/orphelins/dupliqués) si `GROUPS` s'écarte du contenu réel de `src/__tests__/`, empêchant toute récidive silencieuse ; (3) nouveau test dédié `src/__tests__/test-grouped-integrity.test.ts`, qui exerce la même vérification à chaque exécution de la suite normale (`npx vitest run`), sans dépendre d'une exécution manuelle du script groupé.
- **Test de non-régression** : `src/__tests__/test-grouped-integrity.test.ts` (2 tests) — échoue dès qu'un fichier de `src/__tests__/` est ajouté/renommé/supprimé sans mise à jour correspondante de `GROUPS`. Voir TESTREPORT.md « Tests Sprint technique 4 » pour le détail complet et les résultats de la suite complète recomptée (`1244/1244`, 2 exécutions consécutives).
- **Statut** : corrigé, clôturé
- **Validation finale** : correction vérifiée par les 2 exécutions complètes consécutives ci-dessus et par l'exécution normale de `src/__tests__/test-grouped-integrity.test.ts` ; en attente de validation du propriétaire du projet sur ce rapport.

## INC-4 — Test de concurrence intermittent en exécution isolée (`locations.test.ts`, Sprint 34 étape 3, corrigé Sprint 34 tâche 1)

- **Date** : identifié 2026-08-17, corrigé à la source 2026-08-17 (Sprint 34, tâche 1)
- **Environnement** : test (suite Vitest locale, serveur `next dev` de test dédié sur le port 3811)
- **Gravité** : mineure (aucun code applicatif ni utilisateur final concerné, aucune donnée réelle — même famille qu'INC-3, incident d'infrastructure/timing de test, pas un bug métier)
- **Description** : `src/__tests__/locations.test.ts`, describe « Sprint 23 — correctif de concurrence sur updateLocation », test « scénario reproductible : 5 itérations indépendantes de la même course, [200, 409] à chaque fois, jamais [200, 200] ni [200, 403] » (Sprint 31A) — échouait de façon reproductible (9/10 tentatives) en exécution isolée (`npx vitest run src/__tests__/locations.test.ts -t "..."`), avec deux signatures distinctes selon l'exécution : `[200, 403]` (`LocationCancellationRequiresAdminError`) ou `[200, 200]` (double écriture) au lieu du `[200, 409]` (`LocationStatusConflictError`) attendu pour le perdant de la course. N'échouait pas de façon systématique en exécution du fichier complet ni de la suite groupée (`node scripts/test-grouped.mjs`, ~867-868/868 selon l'exécution).
- **Impact** : aucun impact produit — bug de timing d'un test de concurrence, jamais un défaut du code applicatif (`updateLocation`, dont le mécanisme de verrouillage — `lockLocationForUpdate`/`updateMany` conditionné — s'est révélé correct dans tous les cas de chevauchement réel, voir Cause ci-dessous). Impact sur la fiabilité de la suite de tests uniquement.
- **Cause** (investigation complète Sprint 34 tâche 1, par instrumentation directe temporaire — `console.log`/`process.hrtime.bigint()`, entièrement retirée après investigation) : deux causes distinctes, cumulées.
  1. Le serveur `next dev` (Turbopack, Next 16.3.0) sérialise en pratique les requêtes HTTP concurrentes adressées à une même route dynamique de l'App Router (`/api/locations/[id]/route.ts`) — confirmé par une expérience de contrôle sur deux ressources différentes (deux `Location` sur deux véhicules distincts, sans possibilité de conflit applicatif) : zéro chevauchement réel observé, la deuxième requête ne débutant son exécution qu'après l'envoi complet de la réponse de la première. Comportement du serveur de développement lui-même, pas du code de ce projet ; la route statique `POST /api/locations` n'est pas affectée.
  2. Une fois ce facteur neutralisé (appel direct au service, chevauchement réel garanti), un second facteur, plus fin, est apparu : `adminOverride: true` (utilisé par le test d'origine car l'acteur de test était `adminA`) permet à un ADMIN de forcer n'importe quelle transition de statut (comportement voulu, Sprint 19) — si les deux appels se retrouvent malgré tout exécutés séquentiellement (possible même en appel direct, ~1 fois sur 13, par accident de planification/pool de connexions), le deuxième appel lit un statut exact (non périmé, l'autre a déjà committé) et `adminOverride` le laisse passer légitimement, produisant un double succès **par conception**, pas par bug — ce n'est pas la garantie que Sprint 22/23/31A ont construite.
- **Correction** : appliquée à la source, dans les tests uniquement, aucune modification du code applicatif. (1) Les deux tests de concurrence existants appellent désormais directement `updateLocation()` (le service exporté, celui qu'utilise la route) via `Promise.allSettled`, garantissant un chevauchement réel indépendant du dispatch HTTP de `next dev`. (2) Les deux tests n'utilisent plus `adminOverride: true` — `PENDING → CONFIRMED`/`PENDING → CANCELLED` restant valides sans override, la perdante est désormais refusée dans les trois entrelacements possibles (`LocationStatusConflictError`, `LocationCancellationRequiresAdminError`, ou `InvalidStatusTransitionError`), jamais un double succès. (3) Un troisième test, HTTP cette fois (MEMBER dédié avec les permissions granulaires `locations.confirm`/`locations.cancel`, toujours sans `adminOverride`), a été ajouté lors de la revue (Sprint 13E tâche 1) pour couvrir la chaîne complète route→service (authentification, permissions, mapping erreur→code HTTP) — INC-4 ne concernait que le service, la route n'ajoutant aucune logique de concurrence propre, mais ce test ferme explicitement la question. Voir DOMAINRULES.md section 51 pour le détail complet, y compris l'analyse d'`adminOverride` et un effet de bord de test découvert et corrigé en cours de correction (collision de dates avec deux tests indépendants du même fichier).
- **Test de non-régression** : les trois tests (2 directs + 1 HTTP) sont le test de non-régression permanent de cette garantie. **Chiffres exacts, revus et corrigés le 2026-08-21** (le rapport initial du 2026-08-17 citait « 15 exécutions → 13/13 », un chiffre exact mais ambigu — le filtre `-t "Sprint 23"` capturait 13 tests par sous-chaîne, dont 11 sans rapport avec la concurrence ; et « 50 itérations sur 5 exécutions », qui provenait d'un test de sondage temporaire depuis supprimé, distinct du test livré) : filtre resserré ne capturant que les 3 tests de concurrence (`npx vitest run src/__tests__/locations.test.ts -t "conflit explicite pour l'autre|scénario reproductible|HTTP — deux PATCH concurrents"`, vérifié : 3 tests capturés, 84 ignorés) — **10 exécutions consécutives, 3/3 à chaque fois, 0 échec**, soit 70 scénarios de concurrence réellement exercés (50 itérations du test « scénario reproductible », 10 courses du test « deux transitions... », 10 courses du test HTTP). Complété par 5 exécutions du fichier complet (87/87 à chaque fois, 0 échec) et une régression ciblée élargie de 13 fichiers, 328/328 (retours, dégâts, `DamageInvoice`, paiements, maintenances, réservations, isolation tenant/agence). Voir TESTREPORT.md « Tests Sprint 34 tâche 1 » et « Tests Sprint 13E tâche 1 ».
- **Statut** : corrigé, clôturé
- **Validation finale** : correction vérifiée par les exécutions répétées ci-dessus (revues et rendues précises le 2026-08-21) ; en attente de validation du propriétaire du projet sur ce rapport.

## INC-3 — Blocage intermittent et total du serveur `next dev` de test partagé (suite de tests) — RÉSOLU

- **Date** : identifié 2026-08-15, résolu définitivement le 2026-08-26.
- **Environnement** : test (suite Vitest locale, serveur `next dev` de test dédié sur le port 3811, base `xrent_test`) — jamais observé en développement ni en production.
- **Gravité** : mineure pour le produit (aucun code applicatif ni utilisateur final concerné), gênante pour la fiabilité de la suite de tests.
- **Description** : lors de l'exécution de la suite complète, certaines exécutions échouaient avec des timeouts répartis sur un ensemble de fichiers différent à chaque run, sans jamais d'échec d'assertion métier.
- **Cause racine, démontrée avec certitude (2026-08-26)** : Vitest (pool `forks`, `isolate: true`) démarre un processus OS neuf pour chaque fichier de test (confirmé par sonde dédiée : 8 fichiers → 8 PID distincts, aucun état partagé). Sous le parallélisme par défaut (`maxWorkers: 4`), jusqu'à 4 fichiers de test actifs simultanément pouvaient chacun déclencher un pic de création de workers `jest-worker` Next.js (`getStaticPathsWorker()`, jamais réutilisé par conception, comportement interne non configurable de `next dev`/Turbopack) contre le même serveur partagé — c'est ce **pic de créations concurrentes**, pas le volume total de workers, qui saturait le serveur et provoquait les blocages.
- **Correction finale** : `fileParallelism: false` ajouté à `vitest.config.mts` — sérialise l'exécution des fichiers de test (un seul actif à la fois contre le serveur partagé), éliminant structurellement tout pic de charge concurrente. Une architecture alternative (« un serveur `next dev` persistant par worker Vitest ») a été étudiée puis écartée par la preuve (voir détail archivé) avant d'aboutir à ce correctif.
- **Historique d'investigation complet** (causes écartées, mesures intermédiaires, correctifs partiels des Sprints 24-2 à 24-5 et compléments du 2026-08-25/26) : [docs/history/INC-3-full-investigation.md](docs/history/INC-3-full-investigation.md).
- **Résultat mesuré** : `npx vitest run` (commande officielle, sans flag ni wrapper) — **10 exécutions consécutives, 1280/1280 à chaque fois**, 0 échec, 0 timeout, 0 `ECONNREFUSED`/`ECONNRESET`/`SocketError`, 0 redémarrage watchdog, 0 processus résiduel. `node scripts/test-grouped.mjs` reconfirmé fiable (2/2, 1280/1280) — reste une option documentée, plus une nécessité de fiabilité.
- **Test de non-régression** : sans objet pour la cause elle-même (incident d'infrastructure de test, pas un bug de code applicatif) — non-régression confirmée par les 10 exécutions ci-dessus et par les tests ciblés `/dashboard/users`/responsive (114/114, 2 exécutions).
- **Statut** : **corrigé, clôturé** — `npx vitest run` fiable en usage direct pour la première fois depuis l'origine de l'incident (Sprint 24-2).
- **Validation finale** : vérifiée par les 10 exécutions consécutives ci-dessus (2026-08-26) et `npx tsc --noEmit`/`npm run lint`/`npx prisma validate`/`npm run build` tous verts.

## INC-2 — Hydration mismatch sur les icônes SVG du dashboard (extension navigateur Dark Reader)

- **Date** : 2026-08-14
- **Environnement** : développement/préproduction (signalé lors d'un test de reset du SaaS)
- **Gravité** : mineure (avertissement console uniquement, aucune donnée ni fonctionnalité affectée — même famille qu'INC précédent traité au Sprint 20, non numéroté à l'époque)
- **Description** : après le correctif Sprint 20 (hydration mismatch sur `<html>` dû à Dark Reader), un nouveau hydration mismatch de même origine est apparu, cette fois sur les icônes SVG (`lucide-react`) du chrome de tableau de bord (Sidebar/Header/BottomNav) — attributs `data-darkreader-inline-stroke` et `style={{ "--darkreader-inline-stroke": "currentColor" }}` injectés par l'extension avant l'hydratation React.
- **Impact** : avertissement console React pour tout visiteur utilisant Dark Reader (ou extension similaire modifiant le DOM avant hydratation) sur toute page `/dashboard/*` — aucune casse fonctionnelle observée (React tolère les mismatchs d'attributs sans démonter le sous-arbre concerné), mais bruit console gênant lors des tests réels et signal potentiellement trompeur lors d'un futur diagnostic.
- **Cause** : `lucide-react` (`node_modules/lucide-react/dist/esm/Icon.mjs`) positionne les attributs `stroke`/`fill` uniquement sur l'élément `<svg>` racine de chaque icône (jamais sur ses enfants `<path>`) — exactement la classe d'élément que l'overrider de style inline de Dark Reader cible. Le correctif Sprint 20 (`suppressHydrationWarning` sur `<html>`) ne couvre que cet élément précis, la prop n'étant pas récursive — les icônes du dashboard restaient donc exposées au même mécanisme.
- **Correction** : `suppressHydrationWarning` ajouté sur les icônes rendues sans condition dans le HTML du premier chargement de toute page `/dashboard/*` (`Sidebar.tsx`, `Header.tsx`, `BottomNav.tsx`) — voir docs/history/handoff-sprint-log-archive.md (« Sprint 21 ») pour le détail complet, y compris le risque résiduel documenté (mais non corrigé, hors périmètre) sur les icônes des tableaux de contenu par page.
- **Test de non-régression** : aucun test automatisé dédié (bug de rendu DOM/hydratation sans surface HTTP testable en Vitest, même nature qu'INC Sprint 20/14E) — vérifié avec un navigateur réel (Chromium/Playwright), voir TESTREPORT.md section 1 « Vérification manuelle (Sprint 21) », y compris la limite documentée sur la fiabilité de la reproduction synthétique en environnement `next dev` local.
- **Statut** : corrigé pour le chrome persistant du dashboard (Sprint 21) ; **risque résiduel sur les icônes de contenu par page corrigé au 2026-08-17** (voir complément ci-dessous) — INC-2 est désormais intégralement clôturé.
- **Validation finale** : `npm run lint` / `npx tsc --noEmit` / `npm run test` (480/480) / `npm run build` verts + vérification navigateur réel, 2026-08-14.

**Complément du 2026-08-17 — risque résiduel corrigé (commit `8363979`, découvert non documenté lors de l'audit fonctionnel Sprint 34 étape 1)** : le risque résiduel explicitement documenté au Sprint 21 (icônes `lucide-react` des 27 fichiers `*Table.tsx`/`page.tsx` de contenu, hors chrome persistant du dashboard, restées exposées au même mécanisme Dark Reader) a été traité par un commit local (`8363979`, `fix: prevent extension hydration mismatches`) présent dans l'historique Git au moment de l'audit Sprint 34 mais **jamais reflété dans INCIDENTS.md/HANDOFF.md/TESTREPORT.md** — écart de processus (CLAUDE.md règle 9/section 10 point 6 : toute décision structurante doit être écrite dans les documents, pas seulement dans Git) corrigé rétroactivement ici. **Contenu du commit** : nouveau composant partagé `src/components/ui/icon.tsx` (`suppressHydrationWarning` centralisé, exporté via `src/components/ui/index.ts`) substitué aux icônes `lucide-react` directes dans `DataTable.tsx` et 27 fichiers de tables/pages de contenu (`AgenciesTable`, `ClientsTable`, `InvoicesTable`, `VehiclesTable`, `VehicleTransfersTable`, `VehicleTripsTable`, `UsersTable`, `AuditLogTable`, `AlertsList`, `EntriesTable`/`ExpensesTable`, `InvitationsPanel`, `TenantsTable`, `AuditPurgeCard`, `DataResetCard`, `ExportCsvButton`, pages `page.tsx` associées), plus `layout.tsx`/`Header.tsx`/`Sidebar.tsx`/`BottomNav.tsx` alignés sur le même composant partagé. Nouveau test dédié `src/__tests__/icon-hydration.test.tsx`. **Vérification (Sprint 34 étape 1)** : navigateur réel (Chrome via `playwright-core`, installé temporairement hors du projet comme aux sprints précédents) — `/dashboard`, `/dashboard/reports` (page explicitement visée par le signalement à l'origine du Sprint 34), `/dashboard/vehicles`, `/dashboard/reservations` chargées sans aucune erreur/avertissement console, desktop (1440×900) et mobile (390×844). **Limite documentée inchangée** : comme au Sprint 21, la simulation synthétique d'injection Dark Reader reste peu fiable en environnement `next dev`/Turbopack local (hydratation trop rapide) — cette vérification porte sur l'absence d'erreur en usage normal, pas sur une reproduction forcée de l'extension elle-même. **Statut** : corrigé et vérifié ; reste seulement à confirmer que ce commit soit explicitement revu/validé par le propriétaire du projet avant tout `git push` (aucun push effectué à ce jour).

## INC-1 — Agent de développement ayant exécuté `git stash` sans le restaurer avant expiration de son budget de session

- **Date** : 2026-08-14
- **Environnement** : développement (copie de travail locale, jamais poussé/déployé)
- **Gravité** : majeure (risque de perte de travail important), sans impact final (entièrement récupéré)
- **Description** : pendant le Sprint 15, plusieurs agents assistants travaillaient en parallèle sur la copie de travail locale (non isolée par worktree Git). L'un d'eux, chargé d'ajouter des tests d'audit/bout-en-bout, a exécuté `git stash` (visiblement pour isoler temporairement l'état de certains fichiers avant restauration), puis a atteint la limite d'usage de sa session avant d'exécuter `git stash pop` — laissant la copie de travail dans l'état d'avant le stash (fichiers `src/lib/*.ts`, toutes les routes `src/app/api/**` retrofitées ce sprint, tous les composants `src/app/dashboard/**` modifiés, `src/components/layout/Sidebar.tsx`) alors que le stash contenait la totalité de ce travail non commité.
- **Impact** : aucune perte de données réelle — détecté avant tout commit/push. Repéré indirectement via des échecs de tests inattendus et incohérents (routes se comportant comme avant le sprint, ex. `generateContractNumber` encore basé sur `Tenant` au lieu d'`Agency`) plutôt que par un message d'erreur explicite.
- **Cause** : agent avec accès complet à `git` opérant sur la copie de travail partagée (pas de `git worktree`/isolation entre agents concurrents) ; `git stash` (réversible) a heureusement été utilisé plutôt qu'une commande destructive (`git checkout -- .`, `git reset --hard`) qui aurait rendu la récupération impossible sans les journaux de conversation.
- **Correction** : diagnostic via `git status`/`git stash list`/`git diff <fichier>` (confirmation que le stash contenait la version correcte) ; restauration sélective fichier par fichier depuis `git checkout stash@{0} -- <chemin>` pour chaque fichier hors `src/__tests__/` (les fichiers de test, réécrits/étendus après le stash par d'autres agents, étaient déjà plus complets dans la copie de travail que dans le stash — restaurer le stash en bloc les aurait fait régresser) ; vérification complète (`npx tsc --noEmit`, `npm run lint`, `npm run build`, suite de tests complète) avant de supprimer le stash (`git stash drop`).
- **Test de non-régression** : aucun test automatisé dédié (incident de processus, pas de bug applicatif) — la suite complète (416/416) a servi de filet de sécurité pour confirmer la restauration complète et correcte.
- **Statut** : corrigé
- **Validation finale** : vérifié par relecture complète des fichiers restaurés + suite de tests 416/416 verte, 2026-08-14.
- **Enseignement pour les sprints futurs** : lorsque plusieurs agents travaillent en parallèle sur la même copie de travail, éviter de leur laisser une autonomie complète sur des commandes `git` d'état global (`stash`, `reset`, `checkout` sans chemin explicite) ; préférer l'isolation par `git worktree` par agent, ou limiter les agents parallèles à des fichiers strictement disjoints sans accès `git` d'état partagé.
